# Statement manifest — the frozen denominator for Paper A coverage claims

**Purpose.** Every coverage claim about this development ("N of 36") cites *this* file for
what the 36 are. Without a frozen list, a renumbering, a split lemma, or a promoted remark
silently moves the target and makes the count unfalsifiable. Created 2026-08-08 as rung
**5.0** of the ARC-5 orders (`LEDGER.md`; superseded as campaign SSOT by ARC-6, 2026-08-08,
then by ARC-7, 2026-08-09).


> ## ★★★ SCOPE DECISION 2026-08-23: THE EXTERNAL PRE-REGISTRATION IS REVERSED
>
> Bryan, verbatim: **"I want to 'reach ground' on everything / So formalize anything you need
> that's imported."**
>
> The six pre-registered EXTERNAL rows (1, 2, 4, 10, 14, 21) and row 8, whose residue is now
> purely its three cited clauses, are **IN SCOPE from this date**. `WallCertificates/
> external-rows.md` calls closing them "out of scope *by design*"; that design decision is
> superseded, by the author, on the record.
>
> What this commits to, named so nobody discovers it later: van de Wetering's representation
> theorem (row 4, which `main.tex` says the paper "never claims to reprove"), Jordan-von
> Neumann-Wigner (row 2), Yokota's Spin(8) triality (row 21), Koecher / Alfsen-Shultz at f.d. EJA
> generality (row 14), and vdW Thm A.6 + Props 4.19-4.20 (row 8).
>
> ★ **The pre-registration is not deleted.** ARC-5/6 registered these as imports and that record
> stands as what was believed then. This note is the reversal, dated, with its authority named.
> A row that reaches FORMALIZED under it must say so in its own cell.

## The pin

| | |
| --- | --- |
| Source | `landing/papers/twist-normal-form/main.tex`, blog repo `ehrlich-b/blog` |
| Blob hash | **`4b0dba301f75f7e49936607692df1a2e9bac3b2a`** (re-pinned 2026-08-22; previously `205fdf5a548c70744cd38cccc25cb8a201cc9771`) |
| Frozen tag | `paperA-jpa-submitted` @ blog commit `035c337` — the JPA submission, kept untouched as the historical record |
| Working copy | 2026-08-22 — **no longer byte-identical to the frozen tag.** The manuscript freeze was lifted by Bryan on 2026-08-22 and `main.tex` was brought to AIP/JMP submission form (blog commits `f97cfe1`..`6db50eb`): footnotes inlined, abstract cut to 245 words, author-declaration sections added in AIP order, numeric bibliography, deposit re-pointed at the public `ehrlich-b/sequential-products-lean` mirror at `9c6d92f5`, and the supplementary rewritten around the current artifact. |
| **Denominator impact** | ★★★ **NONE, and this is verified rather than assumed.** The extraction below was re-run at the new blob: still exactly **36** rows with the identical breakdown (2 maintheorem, 5 theorem, 17 lemma, 9 proposition, 2 corollary, 1 definition), and `diff` of the full `\label{...}` list between `205fdf5a` and `4b0dba30` is **empty** — the label set is byte-identical. So no row moved, no row was added or dropped, and **every existing "N of 36" claim carries over unchanged.** The re-pin is a pointer update, not a denominator change. ★★★ **ONE THING THE RE-PIN DID NOT CARRY, found 2026-08-22 and recorded rather than silently patched: the `§` column below is now STALE.** The label set is byte-identical but the *line numbers* are not — every row's `\begin{…}` sits **+2** lines lower at blob `4b0dba30` than at `205fdf5a`, and **+3** from row 36 onward; the excluded-remark line numbers in the next section are likewise +2. Verified by re-running this file's own extraction command at the new blob. **No coverage claim depends on the `§` column** — it is a locator, not a denominator — so nothing is retracted, but a reader who opens `main.tex` at a `§` value lands two lines above the environment. ★ This is precisely what the protocol sentence below asks for ("record the new blob hash *plus a diff of this table*") and it is the part the re-pin skipped: the diff was run on the `\label` list, which is invariant, rather than on the table, which is not. |

Because the two agree, this manifest pins one object, not two. If `main.tex` is ever edited,
re-run the extraction (below) and record the new blob hash *plus a diff of this table*
before quoting any coverage number.

## How the 36 is derived (reproducible)

The article's `remark` environment shares the `theorem` counter (`preamble.sty`:34–49), so
the numbered-environment count is 43 and the numbered-*result* count is 43 − 7 remarks = 36.
Extraction:

```
grep -n '\\begin{\(maintheorem\|theorem\|lemma\|proposition\|corollary\|definition\)}' main.tex
```

yields exactly the 36 rows below (2 `maintheorem`, 5 `theorem`, 17 `lemma`, 9 `proposition`,
2 `corollary`, 1 `definition`). The 7 excluded remarks are `rem:S2-scope` (597),
`rem:import-hyps` (811), `rem:no-unitary` (878), `rem:moduli` (1416), `rem:n2-selection`
(1550), `rem:qubit-attribution` (1930), `rem:twist-detection` (1941).

**The count is a LaTeX artifact, not a unit of mathematical content.** `def:sp` is a
definition (encoding it is not "proving" it); `thm:vdw1` and `prop:bridge` are cited
external results the paper never claims to reprove; `mthm:master`/`mthm:omnibus` are stated
over an *abstract* simple Euclidean Jordan algebra, so proving either "at the article's own
generality" requires the Jordan–von Neumann–Wigner classification — the campaign's one
pre-registered permanent import. Read the count with `THEOREM-MAP.md`, never instead of it.

## Status vocabulary

* **FORMALIZED** — a Lean theorem states the article's statement at the article's own
  generality and proves it: no located hypothesis standing in for a cited result, no
  restriction to a special case.
* **PARTIAL** — a Lean declaration covers part of it: one clause of several, a concrete
  carrier where the article is abstract, the statement with one of its own inputs carried as
  a hypothesis, a generator-level rather than operation-level version, or a certified
  concrete example in place of the general lemma.
* **ABSENT** — no Lean counterpart. *ABSENT means nobody has built it.* It does **not** mean
  blocked; a wall gets called BLOCKED only with named evidence for the wall (see the
  `H₃(𝕆)` correction in `LEDGER.md`).

**Baseline at manifest creation: 5 FORMALIZED, 17 PARTIAL, 14 ABSENT** — immediately
corrected to **6 / 19 / 11** by the audit the manifest itself provoked (2026-08-08, rung
5.1), then to **6 / 20 / 10** when `cor:selectors` clause (ii) was proved, then to
**7 / 19 / 10** when `lem:adjacent` closed at the article's own adjacency: three rows had
been carried as ABSENT while Lean declarations for them existed.
`lem:aone` is FORMALIZED and had been since the S1–S7 derived-lemma layer was written;
`lem:span` and `prop:pseudo-transfer` are PARTIAL on the concrete carrier. **No proof was
written for those three corrections — the tree was undercounted.** The lesson is the
mirror image of the `H₃(𝕆)` over-claim: a coverage table maintained by prose drifts in
*both* directions, so every row now names its declaration or says ABSENT.
**Count at end of ARC-6's landed work (2026-08-08): 8 FORMALIZED / 19 PARTIAL / 9 ABSENT.**
**Count 2026-08-24 (current): 32 FORMALIZED / 2 PARTIAL / 2 ABSENT** — machine-derived by the script below. ★★★ **Row 22 (`lem:orientation`) closed later the same day**: the residue was that $\Ad_{a^{it}}$ rotated the cross-coherence space only for a **diagonal** $a$. `HermitianMat.mat_cfc_mul_of_eigen` now gives $f(a)q=f(\lambda)q$ for every real $f$ and every $q$ with $aq=\lambda q$ — by Lagrange interpolation on the finite matrix spectrum, with $\lambda$ added to the nodes so that no $\lambda\in\sigma(a)$ hypothesis is needed — and `cfcPhase` assembles $e^{ig(a)}$ from two **real** functional calculi, so the scalar action transfers summand by summand and `adU_cfcPow_eq_rotation` delivers the article's angle $t(\log\lambda-\log\lambda_k)$ at an arbitrary spectral decomposition. ★★★ **AXIOM CENSUS GAP FOUND AND CLOSED, 2026-08-24.** `AxiomAudit.lean`'s tracked surface was pinned to an exact 197-name manifest, and **six modules already on disk and already imported were absent from it** — `EJA.Fundamental`, `OrthFamily`, `EJA.PseudoTransfer`, `EJA.HermMatCarrier`, `EJA.AlbertBridge`, `Composition.HermInner`. Those are not minor files: `EJA.Fundamental` carries the fundamental formula and `OrthFamily` the vdW 5.2 layer. So the Layer-1 claim — *"visits every persisted declaration of every tracked module"* — was true of the list and **false of the tree**, for as long as those modules have existed. The list now carries them plus this session's four (`Normality`, `DirectSumRestrict`, `EJA.ConeTopology`, `EJA.ThetaAbstract`), and the gate reports **Census PASS at 207 modules, custom axioms exactly `[]`, every closure ⊆ `[propext, Classical.choice, Quot.sound]`.** ★ This is the ninth defect of the species this file tracks — a claim true on the page and false in the tree — and the first one found in the *audit* rather than in a row. FORMALIZED = rows 3, 5–17, 19, 20, 21, 22–36; PARTIAL = 1, 18; ABSENT = 2, 4. ★★★ **Rows 20 and 21 both closed the same day, and NEITHER took the article's route.** The article classifies the frame stabilizer and then applies `Z(C) ∩ Im C = 0`; `thm:albert` additionally needs Yokota's Spin(8) triality, which `MasterTheorem/Branches/Albert.lean` carries as a citation it says cannot be derived internally. **No classification and no triality were used.** Row 17's `ρ_{ij}(dχ(r)) = (rᵢ−rⱼ)T_{ij}` is *linear in `r`*; read at `r = e_i` and `r = e_j` against the cross-block relation, it exhibits the block coordinate as both left and right multiplication by one element — hence by a **central** one — and the derivation property forces it imaginary. The whole argument is **associativity-free**, which is why `𝕆` came along: the only thing `[Ring C]` was doing was finding the `EuclideanJordanAlgebra` instance, and replacing it by the fidelity class `EJA.HermMatFid` lets `ℝ`, `ℂ`, `ℍ` and `𝕆` share every proof. ★★★ **Row 20 (`thm:quaternionic`) closed the same day, and its recorded obstruction was FALSE**: the quaternionic carrier has been in the tree since `EJA/HermMatCarrier.lean`. The route is **not** the article's — no stabilizer classification was needed, because row 17's `(rᵢ−rⱼ)T_{ij}` is *linear in `r`*, and reading the cross-block relation at two standard basis vectors exhibits the block coordinate as both left and right multiplication by one element, hence central. ★★★ **The four PARTIAL rows were re-priced against the tree on 2026-08-24 and one of their stated obstructions was FALSE.** Row 20 recorded "no concrete quaternionic carrier"; `instEuclideanJordanAlgebraHermMat` has carried `EuclideanJordanAlgebra` on `HermMat ι C` for associative composition coefficients — `ℝ`, `ℂ`, `ℍ` — and `#synth` fires all four required instances at `HermMat (Fin n) (Quaternion ℝ)`. `EJA/CarrierInstances.lean` instantiates the abstract layer at both the quaternionic and the exceptional carrier so the correction is machine-checked. ★★ **What all four rows now share is one classification**: `EJA.dChi_jordanDeriv` and `EJA.dChi_frameProj` identify row 17's `dχ(r)` as a derivation of the Jordan algebra annihilating every frame atom, and rows 18/20/21 need that derivation's action on `V_{ij}` — `ξ_i x − x ξ_j` for `ℍ` (elementary, long, not done) and triality for `𝕆` (Yokota, a citation the tree deliberately carries). Rows 2 and 4 are van de Wetering's Theorem 1 and what sits downstream: Koecher–Vinberg plus the Jordan–von Neumann–Wigner classification, neither of which is in Mathlib or in this tree. ★★★ **Row 26 (`lem:frame-connectivity`) closed the same day** by the Givens sweep in `Necessity/Givens.lean`. ★★ **What the four remaining PARTIAL rows now share** is no longer a Lie-theoretic gate — that one is open — but **carriers**: rows 20 and 21 need quaternionic and exceptional carriers (row 20's cell records that `RCLike` is *commutative*, so ℍ can never be an instance and the field-general `Gen` layer cannot reach it), row 18 needs the ℍ and 𝕆 stabilizers plus identity-component packaging for ℝ and ℂ, and row 1 is the one-theorem-over-abstract-EJA form of the same. Rows 2 and 4 are van de Wetering's Theorem 1 and what sits downstream of it. ★★★ **Row 17 (`lem:homomorphism`) closed the same day**, and with it the gate its own cell said held four rows: *"a continuous one-parameter subgroup of a matrix group is differentiable"*. It is now `OneParam.hasDerivAt_of_continuous`, proved for any real Banach algebra by the integral smoothing argument — no Lie theory, no local logarithm, and differentiability **deduced** from continuity rather than assumed, which is what the unknown product's `L_a` forces. ★★★ **Row 31 (`thm:qubit-boundary`) returns to FORMALIZED the same day**, closing clause (i)'s coherence half — `RankTwo.n2Sp_coherence_action`, plus `n2Sp_coherence_action_singular` for the Lüders end. It is the first row this file has demoted and then re-promoted, and it is worth recording that the demotion was **not** an error corrected: the 2026-08-22 audit was right that the tree lacked the statement, right about all three reasons why, and right that the residue was assembly rather than research. The assembly is what was done. ★★★ **Eight rows moved on 2026-08-24, and seven of the eight were unlocked by one theorem.** `prop:bridge` (row 10) — priced as research by its own cell — is a *Frobenius certificate* transcribed into Jordan vocabulary as the **Peirce defect** `Φ(v,x) = ⟪v∘v,x∘x⟫ − ‖v∘x‖²`; the hypothesis kills it, and `Φ = 0` forces operator commutation because `4L_{q_k}L_{q_l}` is a self-adjoint idempotent, which sidesteps the joint Peirce decomposition entirely. That built the **`SequentialProductOnEJA` inhabitant** (`ludersSequentialProduct`, S1–S7 plus S2), which closed `lem:simple-bridge` (8); the same bridge gave `prop:theta` (14), whose fixing clause **is** vdW Prop. 5.5 and so closed `lem:coalescence` (16) as well; and `prop:theta` then closed `lem:frame-fix` (15) once `Θ` was made a function and paper S2 was upgraded from pointwise to operator continuity. Independently: `prop:pseudo-transfer` (13), `lem:normality` (9) — with monotone convergence and order-unit norm equivalence proved rather than assumed — and `prop:central` (12), at the article's `⊕_{α=1}^m` generality with every citation discharged. ★★ **Three closures came from noticing a residue named in one cell was already a theorem stated in another** (`map_jordan_of_orderIso` proved and consumed by nothing; row 8's clause (iii) being row 13's whole statement; vdW 5.5 serving rows 14 and 16 at once) — which is the same defect species this file has recorded eight times before, running in the favourable direction. ★ Superseded record follows. **Count 2026-08-22: 16 FORMALIZED / 17 PARTIAL / 3 ABSENT** — machine-derived by the script below, re-run after each move. ★★ **Two later moves the same day, from an independent audit, and they run in OPPOSITE directions.** `thm:qubit-boundary` (row 31) **FORMALIZED → PARTIAL**: clause (i)'s coherence-action half is part of the article's *statement*, it is not in the tree, and the promotion had inherited a certificate line written before the row's own encoding split was known. `lem:simple-bridge` (row 8) **ABSENT → PARTIAL**: the Jordan spectral theorem that the article's own proof of its one interior clause invokes is now proved at EJA generality in this tree. ★ **Neither move wrote or deleted a proof.** Row 8 is the seventh row whose status was wrong on the page rather than in the tree; **row 31 is the same defect running the other way** — the direction the `H₃(𝕆)` correction in `LEDGER.md` warned about — and it is the first time this file has had to demote a row it promoted. Earlier the same day: **17 FORMALIZED / 15 PARTIAL / 4 ABSENT** — five promotions and one ABSENT→PARTIAL, all machine-derived by the script below rather than asserted. `lem:homog` PARTIAL → FORMALIZED (clause (i)'s abstract port, `seqLeftMulAbs`); `prop:n2-necessity` PARTIAL → FORMALIZED (gap (b), stated in the article's own $\mathcal{J}_n$); `thm:qubit-boundary` PARTIAL → FORMALIZED (clause (iii) in the literal Jordan-automorphism form, plus the cocycle misattribution corrected); `cor:qubit-classification` PARTIAL → FORMALIZED (the bijection at the effect-operation encoding, `badP` dissolved); `cor:selectors` PARTIAL → FORMALIZED (clause (i)); `lem:orientation` ABSENT → PARTIAL. ★★ **`def:sp` deliberately did NOT move**: its clause-for-clause restatement and the restriction / extension-by-zero equivalence both landed, which refutes the reason the cell had been giving, but S2 cannot be a field at abstract generality without an order-unit norm the tree lacks — so the row keeps its status word and gets a new residue. ★ **`THEOREM-MAP.md` was NOT updated in the same commit**, against this file's standing rule, because it was uncommitted in another agent's working tree at the time; that debt is recorded here rather than silently carried.

**Count at end of ARC-8 (2026-08-10): 12 FORMALIZED / 19 PARTIAL / 5 ABSENT.** ★★ **This line read "12 FORMALIZED / 3 EJA-GATED / 16 PARTIAL / 5 ABSENT" until 2026-08-12 (ARC-9 block 9.1), which mixed the two vocabularies the very next paragraph separates**: EJA-GATED is a *terminal state*, not a status word, so subtracting rows 13/16/17 out of PARTIAL made the headline count disagree with the table beneath it, with `STATE.md`, and with the file's own rule. The terminal states are the block below; the status census is this line. EJA-GATED was briefly claimed for six rows and **withdrawn the same day for rows 5, 6 and 15** (their residues include non-EJA clauses); ★★★ **row 17 was withdrawn on 2026-08-22 for the same reason**, so it stands for rows **13 and 16** — and row 16's label is itself flagged OPEN on that test (see the terminal-state ledger). ★ The census on this line is unaffected by all of it: terminal states are not status words.

★ **The census is now machine-derivable from the table rather than maintained by prose** (ARC-9 block 9.1). Every one of the 36 rows renders exactly seven cells with a status word first in cell six, so this counts it:

```
python3 - <<'PY'
import re
c={}
for ln in open("STATEMENT-MANIFEST.md",encoding="utf-8"):
    if re.match(r'^\| \d+ \|', ln):
        s=re.split(r'(?<!\\)\|',ln)[1:-1][5].strip()
        m=re.match(r'^\**[\s★]*\**\s*(FORMALIZED|PARTIAL|ABSENT)',s); assert m, ln[:60]
        c[m.group(1)]=c.get(m.group(1),0)+1
print(c, sum(c.values()))
PY
```

It had to be built to *be* derivable: six rows (9, 16, 18, 22, 29, 34) carried unescaped `\|` inside their cells — from LaTeX like `$\Theta_a\|_{W_n}$` and from grep alternations — so in rendered Markdown their Status column showed a fragment of the statement and the real status was pushed into the `T` column or dropped. **Row 34 is FORMALIZED and did not render as such.** This is the same field-count defect the ARC-8 diff audit found once in `EJA-DIVIDEND.md` and never swept across this file.

### TERMINAL-STATE LEDGER (ARC-8, 2026-08-10) — all 36 rows

The ARC-8 orders' terminal condition is over four states. This block makes it auditable at a glance.
★ **The status words in the table below stay FORMALIZED / PARTIAL / ABSENT**, because the coverage
count and the census depend on that taxonomy; a terminal state is a *separate* fact about a row and is
recorded here, exactly as the row-20 note insists ("status words stay inside the taxonomy; the
obstruction goes in prose").

| terminal state | rows | count | evidence |
| --- | --- | --- | --- |
| **FORMALIZED** at the article's own generality | 6, 7, 11, 19, 23, 24, 25, 27, 28, 29, 30, 32, 33, 34, 35, 36 | 16 | in-tree, census-covered; equals the FORMALIZED cell of the `Count 2026-08-22 (current)` line, and must be re-checked against it on every move. ★★ **Row 31 removed 2026-08-22** on its demotion (see its cell) and now sits in WALL-CERTIFIED |
| **EXTERNAL by pre-registration** (not re-litigated) | 1, 2, 4, 10, 14, 21 | 6 | `WallCertificates/external-rows.md` |
| **EJA-GATED** | 13, 16 | 2 | ★★★ **ROW 17 WITHDRAWN 2026-08-22, resolving a contradiction that stood at HEAD for twelve days.** `WallCertificates/eja-gated.lean`'s own row-17 line has said since 2026-08-10 that row 17 "should be read as WALL-CERTIFIED, not EJA-GATED", and this table carried it as EJA-GATED anyway. Resolved in the certificate's favour, because the residue is checkable and is non-EJA on three counts, none of them a gate: `DiagonalHomSetup` (`MasterTheorem/DiagonalHom.lean:180-193`) carries `ρ`, `ρ_skew`, `dχAdd`, `dχAdd_cont`, `coalescence_diff` as fields its own docstrings call "interface data … not constructed here"; the article's `Stab(F)^∘` clause needs identity-component vocabulary absent from the tree; and `Θ_r` is the comparison map of the unknown sequential product, whose order-unit/effect layer no gate supplies. Row 17 moves to WALL-CERTIFIED (evidence: `differential-trio.lean`, which covers rows 16, 17, 18 with zero gaps). ★★ **Row 16's label is now OPEN on the same test and is deliberately NOT changed here**: its residue includes `Θ_fix`, which is vdW Prop 5.5 span-extended by `lem:span` (row 5) and is non-EJA — see the row-16 cell. Flagged for decision rather than inherited. ★ **No status word and no coverage count moves on any of this** — that is a claim about the row-17 withdrawal, and it stands. ★★ **But the census figure this sentence carried, "still 12 FORMALIZED / 19 PARTIAL / 5 ABSENT", was STALE and is struck 2026-08-22**: five rows had already been promoted earlier the same day, and two more moved that evening. **The census lives on one line, the `Count 2026-08-22 (current)` line above, and is derived by the script there — it is 16 / 17 / 3.** A terminal-state cell must never restate it; this one did, and pinned a number that six other edits had already moved. — gates (E1)/(E2)/(E3). ★★★ **"`sorry` at each gap" no longer describes the file (2026-08-22): (E2) is a proved theorem since 2026-08-20 and (E1) since 2026-08-22, so only (E3) carries a `sorry`. ★★ **Scope on (E2), added 2026-08-22: "the gate is discharged" is not "(E2) is done."** `gate_E2_peirce` binds its `J2`/`ScalarOn` existentially, which makes its conclusion provably equivalent (machine-checked in both directions) to a sentence with no Peirce vocabulary — so it certifies row 16's clause (1) and **not** clause (2); the strong statement is proved in `EJA/InterfaceInstance.lean`, not in the certificate. And the frame-level `⊕_{i≤j} J_{ij}` is still not a declaration anywhere. **No row moves** — a gate is an ingredient, and row 13's residue is the spectral *inverse*, which is built ON the (E1) resolution and is not built at EJA generality. ★★ **"which is a functional calculus" struck 2026-08-22**: a *polynomial* functional calculus at EJA generality exists (`EJA.jeval`, `EJA/Spectral.lean:98`) and a spectral inverse over a finite spectrum is a polynomial value — see the row-13 cell for what is genuinely absent. ★★ **But row 13's EJA-GATED evidence is now incomplete IN FORM**: the ARC-8 definition requires the article-generality residue stated in Lean with a `sorry` at the gap, and the statement that carried it for row 13 is now proved. Writing that narrower `sorry` — the spectral inverse from a resolution — is an open task, recorded here rather than papered over. ★★ Rows 5, 6, 15 were claimed here and **WITHDRAWN the same day**. ★★ And the claim that rows 16/17 *cannot* reach FORMALIZED by axiomatization alone is **now OPEN, not settled**: `gate_E3` as stated assumes `Φ` LINEAR, making it the classical Koecher/Alfsen–Shultz theorem rather than vIR's JB-generality version, so a Mathlib-grade f.d. EJA layer could discharge it in-tree. ★★★ **SETTLED 2026-08-22, and it settles nothing about the rows.** `gate_E3_theta_jordan`'s `Φ : J ≃ₗ[ℝ] J` is linear **by type, not by hypothesis**, and `ComparisonSetup.Θ : J → (J ≃ₗ[ℝ] J)` (`MasterTheorem/Interface.lean:264`) makes the same commitment — both read at source. So it is Koecher / Alfsen–Shultz and a Mathlib-grade f.d. EJA layer **would** discharge it in-tree. **It unlocks neither row**: row 16 never consumed `Theta_jordan` and row 17 is no longer EJA-GATED. The question that ran for twelve days had a clean answer and the answer was not load-bearing |
| **WALL-CERTIFIED with attack evidence FROM THIS ARC** | 3, 5, 8, 9, 12, 15, 17, 18, 20, 22, 26, 31 | 12 | ★★★ **THIS ROW WAS NOT A PARTITION AND IS REPAIRED 2026-08-22.** Rows **6, 29, 35, 36** stood in *both* this cell and the FORMALIZED cell above, and row **31** did too until its demotion. A terminal state is one state per row: a row that reached FORMALIZED has left the wall, and its certificate survives as **provenance** for how it got there, not as its terminal state. Rows 6, 29, 35 and 36 are removed here and keep their certificates (`abstract-tier.lean` for 6, `frame-geometry.lean` for 29 and 36, `prop-n2-sufficiency.lean` for 35); row 31 is removed from FORMALIZED and **stays here**, where `frame-geometry.lean` has carried it all along — that file's own header (`:7`) never stopped saying "row 31, PARTIAL". Current attributions: `abstract-tier.lean` (3, 8, 9, 12 — **and 5**), `frame-geometry.lean` (22, 26, 31 — **and 15**), `differential-trio.lean` (18 — **and 17**, added 2026-08-22 when row 17 was withdrawn from EJA-GATED; that certificate already covered rows 16, 17 and 18 and has zero gaps), `thm-quaternionic.lean` (20) — each refreshed 2026-08-09/10. ★ **The four terminal states now sum: 16 FORMALIZED + 6 EXTERNAL + 2 EJA-GATED + 12 WALL-CERTIFIED = 36, with no row in two cells.** ★★ Rows **5, 6, 15** had NO certificate named here until the diff audit caught it (2026-08-10): after the EJA-GATED withdrawal they were claimed WALL-CERTIFIED while both candidate certificates still said they had "MOVED OUT … now EJA-GATED", so three rows pointed at a file whose own header disclaimed them. Their attributions are restored to the certificates that actually carry their evidence, and those files' move-out notes are annotated |

**★★ What "attack evidence from this arc" bought, per row, beyond a date stamp:** row 3 — the cost is
transcription, not difficulty (`extendByZero` landed); row 8 — **that entry read "blocked on a missing DEFINITION from
vdW, not on Lean", and it is RETRACTED 2026-08-22: the article states vdW's definition itself, at
`main.tex:507–515` (a simple effect is one with a finite spectral decomposition into orthogonal sharp
effects; sharp is `p ∧ (e − p) = 0`), so the reading task was already done in the manuscript and the
certificate that declined to guess it had the definition on the page it was pinned to**; row 9 — clause landed, and the S2 asymmetry
between its two clauses recorded; row 12 — a refutation attempt FAILED (unlike row 35's, the unit axiom
reaches both summands), so the row is genuinely open rather than mis-stated; row 18 — the ℂ converse
was already in-tree and the ℝ row came free at `RCLike` generality; row 20 — the carrier finally
carries a product; row 22 — absence re-confirmed, restatement deliberately refused; row 26 — **stated
for the first time**, and the ingredient re-confirmed absent from the tree AND Mathlib; row 29 — a
`True` placeholder **replaced by the Θ-level conclusion itself**; row 31 — clause (ii) closed, and the
row-30/31 *encoding split* found and closed **for clause (iii) only, which is the gap the 2026-08-22
demotion fell into: clause (i) was left in the entry-level encoding and this entry did not say so**;
row 35 — **my own residue claim refuted** by the tree's
own `badP`; row 36 — a near-miss recorded and the restatement deliberately refused because the obvious
one would be inert a second time.

★ **Still owed for the terminal condition:** the 8.6 dry pass must be run over every non-FORMALIZED
row until a full round produces **zero movement** (one partial round so far, and it moved three rows'
prose), and the certificate-refutation review must be applied to **every** standing certificate with
the three defect-kind tests (FALSE, VACUOUS, SELF-DEFEATING). Two vacuous gaps were already found and
replaced this arc (rows 29 and, by refusal, 36).
 — EJA-GATED is the ARC-8 orders' fourth terminal state, and it stands for rows **13 and 16** only (certificate `WallCertificates/eja-gated.lean`). ★★ It was briefly claimed for rows 5, 6, 15 too and **withdrawn the same day** — their residues include non-EJA clauses — and **row 17 was withdrawn 2026-08-22** on exactly the same test, resolving a contradiction with the certificate that stood at HEAD for twelve days. ★ It is a TERMINAL state, not a FORMALIZED one; and whether rows 16/17 could ever reach FORMALIZED was recorded here as **OPEN** on the ground that `gate_E3` as stated assumes `Φ` linear (the classical Koecher/Alfsen–Shultz theorem) rather than vIR's JB-generality version. ★★★ **That question is SETTLED 2026-08-22 — it is Koecher/AS, `Φ` being linear by type — and it decides nothing about these rows, because neither was gated on (E3): row 16 does not consume `Theta_jordan`, and row 17 is no longer EJA-GATED.** Earlier the same day: 12 FORMALIZED / 19 PARTIAL / 5 ABSENT** — `prop:n2-sufficiency` ABSENT → **FORMALIZED** (8.1(b): S1, S3–S7 *and* S2, for an arbitrary continuous `t` on ℝP², at the article's own operation); `lem:n2-descent` PARTIAL → **FORMALIZED** (8.1(d)); `cor:qubit-classification` ABSENT → PARTIAL (8.1(f)). ★ The wall certificate `WallCertificates/prop-n2-sufficiency.lean` now has **zero gaps**.

**Count at end of ARC-7 (2026-08-09): 10 FORMALIZED / 19 PARTIAL / 7 ABSENT** — `lem:n2-bounded` ABSENT → FORMALIZED (7.1a), `lem:n2-continuity` PARTIAL → FORMALIZED (7.1b), `lem:normality` ABSENT → PARTIAL (7.5). Primed rows (32′, 33′, 34′, 36′) are provenance only and are NOT counted; the denominator is still the 36 numbered rows 1–36.
`lem:cone-ext` moved PARTIAL → FORMALIZED (rung 6.3, abstract, with a norm-free *admissibility condition*; the row itself still carries S2, which is a norm-topology hypothesis — ARC-6 review correction). Earlier in the arc: 
`lem:homomorphism` moved ABSENT → PARTIAL — and, exactly like the arc-5 corrections, *the
row was wrong before any proof was written*: the reason recorded for its ABSENT status
("Θ is never differentiated") was false, describing the abstract skeleton and generalizing
to the whole tree. The arc's proof then closed its one genuinely missing clause. That is
now **five** rows in two arcs whose status was wrong on the page rather than in the tree, in
both directions (`lem:aone`, `lem:span`, `prop:pseudo-transfer`, `lem:homomorphism`, `lem:homog`(i)). Every row must name a declaration or say ABSENT *with a scope and a date*.

★★ **AXIOM-FIDELITY AUDIT (2026-08-09, checkpoint-1 verification pass, against the PINNED `main.tex:363–392`).** Every field of `SequentialProductOn` was compared to the article's `def:sp`: S1 → `sp_add_right` (same `b + c ≤ 𝟙` rider), S2 → carried separately as `FirstArgContinuous` and proved equivalent to the ouNorm form, S3 → `sp_unit_left`, S4 → `sp_zero_symm`, S5 → `sp_assoc_of_compatible`, S7 → `compatible_sp`, S6 → split across `compatible_ortho` and `compatible_add` (a conjunction split, not an addition), and `sp_effect` is the article's own codomain declaration. **VERDICT: no field is stronger than the article's axioms, so no row is a special case.** In one respect the Lean class is strictly **weaker**, which makes the rows *more* general: the article's operation exists only on effects, while Lean's is total with every axiom guarded by `IsEffect` — which the tree exploits deliberately in `badP`. Weaker hypotheses, stronger theorem; no risk in that direction. ★ Scope: this certifies **Lean-vs-article**. The article itself claims to restate vdW's Definition 2 "with the domain riders made explicit"; vdW's paper was not opened, so Lean-vs-vdW rests on the article's own restatement claim.

`THEOREM-MAP.md` governs per statement and wins wherever it and this file disagree.

---

## The 36

`§` = line in the pinned `main.tex`. `T` = target rung, where one is assigned — ARC-5 rungs
(`5.x`) as executed, retargeted to ARC-6 rungs (`6.x`) where the work carried forward;
`—` = not targeted; `ext` = cited external result, out of scope by design.

| # | Label | § | Kind | Statement (one line) | Status | T |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | `mthm:master` | 124 | maintheorem | On a f.d. simple EJA of rank $n\geq3$, any S1–S7 (+S2) sequential product is Lüders on the real, quaternionic and exceptional types, and $a^{1/2+it}b\,a^{1/2-it}$ for a unique $t$ on the complex type; block-diagonality of the update is derived, not assumed. | PARTIAL — ★★★ **THREE OF THE FOUR TYPE ROWS ARE NOW CLOSED, 2026-08-24.** The ℝ and ℂ rows were already outright; `thm:quaternionic` (row 20) and `thm:albert` (row 21) closed the same day at the concrete carriers `HermMat (Fin n) (Quaternion ℝ)` and `HermMat (Fin 3) Octonion`, by an argument that needed neither the stabilizer classification nor Yokota triality. ★★★ **What is left is exactly one theorem, and it is not this development's**: **Jordan–von Neumann–Wigner**. This row quantifies over *an abstract simple EJA of rank ≥ 3*, so reading off which of the four types it is — the step that lets the per-type results be combined into one statement — is the classification. `LocalTomography.lean:43` has the type *enum* and `Octonions.lean` states JvNW in prose; **no declaration proves it, and Mathlib has nothing**. ★ Row 2 needs the same theorem plus Koecher–Vinberg (row 4). ★ Superseded record: PARTIAL (ℝ and ℂ rows outright; ℍ, 𝕆, and the one-theorem-over-abstract-EJA form open) | — |
| 2 | `mthm:omnibus` | 187 | maintheorem | For a f.d. EJA whose rank-two summands are the real or complex qubit, the product decomposes over simple summands and is Lüders / constant-twist / frame-dependent-twist per summand, giving a bijection $\mathcal{M}_{\mathrm{SP}}(J)\cong\prod\mathbb{R}\times\prod C(\mathbb{RP}^2,\mathbb{R})$. | ABSENT — ★★★ **RE-PRICED 2026-08-24.** Of the four steps this cell records, the situation is now: **(3) the decomposition over simple summands is DONE** — `prop:central` (row 12) at the article's `⊕_{α=1}^m` generality; **(4) the per-summand results** are rows 1 and 31, and row 31 is FORMALIZED while row 1's four *type* rows all closed 2026-08-24 (ℝ, ℂ already; ℍ as row 20, 𝕆 as row 21); **(1) is row 4**, van de Wetering's Theorem 1, whose proof is not in this tree; and **(2) is Jordan–von Neumann–Wigner**, which is what turns "an abstract simple EJA" into one of the four named types so the per-type results can be combined. `LocalTomography.lean:43` has the JvNW type *enum* and `Octonions.lean` states the classification in prose; **no declaration proves it and Mathlib has nothing**. ★ So two of the four steps are finished and two are external classification theorems. That is the honest price, and it is the same two theorems row 1 and row 4 are waiting on. Superseded record: ABSENT — ★★★ **ROUTE NAMED 2026-08-24**, replacing the bare parenthetical "(needs JvNW at article generality)", which named a theorem but not what would have to be built. Four steps: **(1) row 4**, to have a Euclidean Jordan algebra at all — Koecher–Vinberg, absent from Mathlib and from this tree. **(2) Jordan–von Neumann–Wigner**: every finite-dimensional simple formally real Jordan algebra is `H_n(ℝ)`, `H_n(ℂ)`, `H_n(ℍ)`, `H₃(𝕆)` or a spin factor. ★★ **The tree has the type *enum* and none of the theorem** — `LocalTomography.lean:43` defines "the EJA types from the Jordan-von Neumann-Wigner classification" as a datatype and `Octonions.lean` states the classification in prose; no declaration proves it, and Mathlib has nothing. **(3) the decomposition over simple summands**, which is **done**: `prop:central` (row 12, FORMALIZED) at the article's `⊕_{α=1}^m` generality, with `DirectSumRestrict`'s componentwise product. **(4) the per-summand results**, i.e. rows 1 and 31 — row 31 is FORMALIZED, row 1 is PARTIAL on its ℍ and 𝕆 rows. ★ So of the four steps, one is finished, one is finished except for two carrier-level classifications, and **two are classification theorems that neither Mathlib nor this tree contains**. That is the honest price of this row, and it is not a session's work | — |
| 3 | `def:sp` | 363 | definition | A sequential product space: an order unit space with a binary operation on effects satisfying S1 additivity, S2 order-unit-norm continuity in the first argument, S3 unitality, S4 symmetry of orthogonality, S5 compatible associativity, S6 additivity of compatibility, S7 multiplicativity of compatibility. | FORMALIZED — ★★★ **THE ONE REMAINING CLAUSE, S2, IS WRITTEN 2026-08-23.** The prior cell named the residue as sharply as it could be named: "**What blocks the row is S2, alone** … Making it one needs an abstract `ouNorm`, which the tree does not have." `OrderUnitSpace.ouNorm` now exists (landed with row 5 the same day), and with it `OrderUnitSpace.ContinuousOnOu` — ε–δ continuity on a set with **both** distances in the order-unit norm, which is how the manuscript states S2 and which `ContinuousOn` cannot express, since the carrier's single `TopologicalSpace` comes from the `NormedAddCommGroup` parent. `PaperA.SpFirstArgContinuousOu` is the article's S2 for a pinned product; `PaperA.EffectSequentialProduct.FirstArgContinuousOu` is it as a clause of `def:sp`; and `PaperA.EffectSequentialProductS2` is the article's definition with **all eight clauses in one object**. The equivalence survives the eighth clause: `restrictSpS2` and `extendSpS2` carry S2 both ways (`restrictSp_firstArgContinuousOu` transports it across restriction, using only that `extendSp (restrictSp P)` agrees with `P` on effects and that `ContinuousOnOu` looks nowhere else), `restrictSpS2_extendSpS2` is the round trip, and `restrictSpS2_surjective` inhabits it. ★ **What this does NOT claim.** `SpFirstArgContinuousOu` and the tree's `SequentialProductOn.FirstArgContinuous` are **different conditions at this generality** and nothing here says otherwise — the latter is `ContinuousOn` in the carried topology, and relating the two needs a two-sided norm comparison that only a specific carrier supplies (`Necessity.firstArgContinuousOu_iff`, on `HermitianMat`, unchanged). The article's S2 is the order-unit one, and that is the one now carried as a clause. ★ One collateral edit: adding `OrderUnitSpace.ContinuousOnOu` made the bare name ambiguous against the concrete `HermitianMat.ContinuousOnOu` at `Necessity/OrderUnitS2.lean:119`, which is now qualified; no statement there changed. Axioms: all eight new declarations `[propext, Classical.choice, Quot.sound]`, no `sorryAx`. Prior record: PARTIAL — ★★★ **STATUS UNCHANGED 2026-08-22, AND THE REASON HAS CHANGED COMPLETELY; the old reason is refuted by work landed today.** This cell said the only machine-checkable content of a *definition* is "an equivalence with a clause-for-clause restatement". That restatement now exists: `PaperA.EffectSequentialProduct`, the article's clauses written over `PaperA.Effect V` — the effect subtype already in the library and audit-pinned at `PaperA/AuditPins.lean:97` — with effect-closure as the **codomain**, exactly as `main.tex:367` types the operation. And the equivalence is proved: `PaperA.restrictSp` and `PaperA.extendSp`, with `restrictSp_extendSp` an **equality of structures** and `extendSp_restrictSp_on_effects` agreement on effects, which is the most that can be true — the axioms are `IsEffect`-guarded and cannot see off the effects. So the totality of Lean's `sp` is a *convention*, now proved to be one rather than argued to be one. ★★ **What blocks the row is S2, alone.** `EffectSequentialProduct` carries S1 and S3–S7; the article lists S2 — continuity in the **order-unit** norm — as a clause of `def:sp`, and it cannot be a field at abstract generality because `OrderUnitSpace`'s carried norm is independent structure and not the order-unit norm. Making it one needs an abstract `ouNorm`, which the tree does not have; on the concrete carrier the gap is already closed by `firstArgContinuousOu_iff`. **The residue is exactly one clause and exactly one missing object** — materially sharper than "a definition has no machine-checkable content", and the one to quote. Superseded record follows: PARTIAL (encoded as `SequentialProductOn`/`SequentialProduct`; effect-closure carried as the codomain condition `sp_effect` rather than as a clause) — ★★ **but the FAITHFULNESS of that encoding is now independently verified at source (2026-08-09, checkpoint-1 axiom-fidelity audit): every field was compared clause-by-clause against the pinned `main.tex:363–392`, and no field is stronger than the article's axioms; `sp_effect` is the article's own codomain declaration, unavoidable because Lean's `sp` is total; S6 is split across two fields, a conjunction split rather than an addition.** In one respect the class is strictly *weaker* (the operation is total with every axiom `IsEffect`-guarded), which makes every downstream row *more* general, not less — and `badP` exploits exactly that. ★ The row nevertheless stays **PARTIAL, deliberately**: the audit is verification by an outside reader at source, not a machine-checked theorem, and for a *definition* the only machine-checkable content is an equivalence with a clause-for-clause restatement — which is not written. `WallCertificates/abstract-tier.lean` called this "the single highest row-movement-per-unit-effort item remaining"; that is now better informed — the *verification* is done, what remains is the Lean *packaging* (a structure with the article's eight clauses, plus restriction and extension maps; `badP` is the template for the extension direction). | — |
| 4 | `thm:vdw1` | 399 | theorem | A finite-dimensional sequential product space is order-isomorphic to a Euclidean Jordan algebra. | ABSENT — ★★★ **ROUTE CORRECTED 2026-08-24; THE PREVIOUS ROUTE WAS ASSERTED, NOT CHECKED.** The cell written earlier the same day named three steps — self-duality of the cone, homogeneity, then **Koecher–Vinberg** — and attributed that path to this row. That is a plausible proof of *some* theorem of this shape, but it is **not verified to be van de Wetering's**, and naming a classical input one has not checked against the source is the same defect this file tracks in the other direction. What is checkable: this row is `thm:vdw1`, i.e. **Theorem 1 of van de Wetering, *Sequential product spaces are Jordan algebras*, arXiv:1803.11139** — the paper `SequentialProduct.lean` already cites for the S1–S7 interface itself. Its proof is not in this tree and is not a step of this development. ★★★ **What the tree has, and why it points the WRONG WAY.** `OrderUnitSpace.lean:461` defines vdW's class `E₀` (`IsSimpleEffect`) order-theoretically, and `EJA/Order.lean:869` proves **`E = E₀`, every effect is simple** — but `isSimpleEffect_of_isEffect` takes `hcomm`, `hjordan`, `hfr`, `hassoc` as hypotheses, i.e. it runs **from** a Jordan structure. This row needs the converse: manufacture the Jordan structure from S1–S7 alone. So the tree's spectral machinery is not partial progress on this row; it is the same statement read in the direction this row cannot use. ★ That is the precise gap, and it is a paper, not a step. Superseded record: ABSENT — ★★★ **ROUTE NAMED 2026-08-24.** This row and row 2 were the only two carrying a bare status word with no route, which is exactly what row 32's rule forbids ("ABSENT-with-a-named-route, never BLOCKED"); the rule was being kept for the *vocabulary* and broken for the *content*. The route, in three steps: **(1) self-duality of the cone.** For a Euclidean Jordan algebra this is `EJA.isSoS_iff_forall_inner_nonneg` (proved 2026-08-24), but row 4 needs it *before* any Jordan structure exists — i.e. for the order-unit space of an abstract sequential product space, off S1–S7. **(2) homogeneity of the cone**: the quadratic maps `Q_a` built from the product act transitively on the cone's interior. **(3) Koecher–Vinberg**: a homogeneous self-dual cone in a finite-dimensional real inner product space is the cone of squares of a Euclidean Jordan algebra. ★★ **(3) is the wall, and it is not a wall of this development**: Mathlib has no Koecher–Vinberg, and this tree has no fragment of one — grepping `Koecher\|Vinberg` over `RadicalRelativity/` returns prose references only. The standard proof constructs the Jordan product from the cone's characteristic function (the Vinberg construction), which is a formalization project in its own right, not a step of this one. ★ Row 2 sits downstream of this row | ext |
| 5 | `lem:span` | 418 | lemma | In an order unit space the effects contain the ball of radius $\tfrac12$ about $\tfrac12\id$, hence span $V$, so linear maps agreeing on effects are equal; likewise $[0,q]$ spans the Peirce subalgebra $J_2(q)$. | FORMALIZED — ★★★ **BOTH OPEN CLAUSES CLOSED 2026-08-23.** **Ball clause:** `OrderUnitSpace.ouNorm` is the abstract order-unit norm `sInf {t ≥ 0 \| -t•𝟙 ≤ x ≤ t•𝟙}` (an unbundled `def`, for the reason `Hermitian/OrderUnit.lean` gives at the concrete carrier: the carried `Norm` slot belongs to the `NormedAddCommGroup` parent and a second bundled norm would make instance resolution ambiguous); `ouNorm_mem_ouBound` proves the infimum is **attained**, which is the one step that consumes `IsArchimedean` rather than the class's weaker order-unit boundedness, and is exactly why the article's ball is a *closed* ball; `isEffect_half_smul_unit_add` and `closedBall_half_subset_isEffect` are then the article's `main.tex:420-431` route verbatim. ★ **Two things this does NOT claim.** (i) The clause is stated in the **order-unit** norm — which is the article's `‖·‖`, so this is the article's own statement — and the interface's carried `Norm` is independent structure that is **not** asserted equal to `ouNorm` here; the two-sided comparison exists only at the concrete carrier (`ouNorm_le_norm`, `norm_le_sqrt_card_mul_ouNorm`). (ii) It is proved under an explicit `IsArchimedean V` hypothesis, not from the bare class, on the same footing as abstract `lem:homog`(ii) and `lem:cone-ext` — see `OrderUnitSpace.IsArchimedean`'s docstring for why a row proved under it counts at the article's own generality, and `arch_iff` for the machine-checked equivalence with the textbook `ℕ` form. **Peirce clause:** `peirceOneSubOrderUnitSpace` makes `J₂(c)` an order unit space by feeding `EJA/Class.lean`'s `jmulₗ` tuple to `orderUnitSpaceOfBilinear`, and `span_isEffect_peirceOneSub_eq_top` is the article's own derivation — instantiate the spanning lemma at `(J₂(q), q)`; `coe_ousUnit_peirceOneSub` pins the order unit to `c`, which is what makes the effect interval the article's `[0,q]` and not some other interval. ★ The prior cell's reason for this clause — "it follows *once* the tree knows $J_2(q)$ is an order unit space, which it does not" — **went stale on 2026-08-22** when `instEJAPeirceOneSub` and `orderUnitSpaceOfBilinear` landed, and nobody re-read the row against them: another instance of the wrong-on-the-page-not-in-the-tree pattern this file tracks. Axioms: all eight new declarations `[propext, Classical.choice, Quot.sound]`, no `sorryAx`. Prior record: PARTIAL — ★★★ **EJA-GATED CLAIM WITHDRAWN 2026-08-10, the same day it was made**, by the certificate-refutation review. EJA-GATED requires that *every non-EJA clause* be closed in-tree first, and this row's residue includes the **ball clause** (`the effects contain the ½-ball about ½·e`), which the manifest itself says needs the norm to *be* the order-unit norm — an order-unit-norm fact, NOT the axiomatization. ★★ **The error was one error made three times (rows 5, 6, 15): I classified each row by its BIGGEST residue and let that stand for its WHOLE residue.** EJA-GATED is a claim about the complement — that nothing else remains — and a claim about a complement cannot be checked by inspecting the largest item in it. Same failure as row 35's "and that is the whole residue". The row is **WALL-CERTIFIED with this arc's attack evidence** instead (`WallCertificates/eja-gated.lean`'s WITHDRAWAL block names the non-EJA clause; the gate statements there remain the honest pricing of the *EJA part* of the residue). Prior record: **EJA-GATED 2026-08-10 (ARC-8 block 8.6)** — certificate `WallCertificates/eja-gated.lean`: every non-EJA clause is closed in-tree (itemized there per row), and the article-generality residue is **gate (E2)** stated in Lean with `sorry` exactly at the axiomatization gap. Prior record: PARTIAL, **upgraded to abstract 2026-08-08**: the spanning and extensionality clauses are now proved at the *full generality of the `OrderUnitSpace` interface* (`OrderUnitSpace.span_isEffect_eq_top`, `linearMap_eq_of_eq_on_effects`), with the **proof** using order-unit boundedness alone — no norm and no Archimedean property enter it. Wording corrected 2026-08-08 after the cold review: the *interface* extends `NormedAddCommGroup`, so the statement still presupposes a norm exists even though the argument never touches it; "full order-unit-space generality" would be a mathematician's order unit space, which carries none. Open: the ball clause (needs the norm to *be* the order-unit norm plus Archimedean; it is the article's route, not its content) and the Peirce clause (which the article gets by instantiating this lemma at $(J_2(q),q)$ — so it follows *once* the tree knows $J_2(q)$ is an order unit space, which it does not) | 5.1 |
| 6 | `lem:homog` | 446 | lemma | For each effect $a$: (i) $L_a$ is additive and order bounded, hence extends uniquely to a positive linear map on $J$; (ii) $(\lambda a)\cdot b=\lambda(a\cdot b)$ for $\lambda\in[0,1]$. | **FORMALIZED 2026-08-22** — clause (i)'s abstract port, which this cell names as the row's whole residue, is done at order-unit generality. `SequentialProductOn.seqLeftMulAbs` is the unique positive linear extension of $b\mapsto P.sp\,a\,b$ from the effect interval to all of $V$, with `seqLeftMulAbs_apply_effect`, `_nonneg`, `_mono`, `_one` and `_unique` — the last from `linearMap_eq_of_eq_on_effects`, which is the article's own `lem:span` route. It is built on `spConeRight_zero` / `_nonneg` / `_smul` / `_add` / `_sub_congr`, the second-slot cone extension already in the tree, plus `negBound`, which supplies the article's "extension by differences" splitting $x=(x+r\cdot\mathbb{1})-r\cdot\mathbb{1}$ from the class's own `archimedean` field. ★★ **`IsArchimedean` is the only ambient hypothesis and S2 is not used**; the concrete route's `posPart`/`negPart` are neither needed nor available abstractly. `OrderUnitSpace.lean` records `IsArchimedean` as part of the article's *own definition* of an order unit space rather than a located stand-in for a cited result, and that is what makes this article generality. ★★★ **THIS CELL WAS DEFENDING A DEPENDENCY THAT CAN NO LONGER FAIL, AND WAS SILENT ON THE ONE THAT CAN. Corrected 2026-08-22; the status word does not move.** The sentence that stood here said "if that reading is ever revised, this row falls back to PARTIAL". **That fallback can no longer trigger**: `EJA.isArchimedean_ofBilinear` (`EJA/Order.lean:468`) *proves* `OrderUnitSpace.IsArchimedean` for the order-unit structure of any finite-dimensional formally real Jordan algebra with a unit and an associative inner product — i.e. of any Euclidean Jordan algebra on the standard definition — so `IsArchimedean` no longer rests on a reading of a definition at all. ★ Honest scope: the associativity hypothesis `hassoc` (`⟪x∘y,z⟫ = ⟪y,x∘z⟫`) is carried explicitly and is **not** derived from formal reality in this tree (that equivalence is Faraut–Korányi VIII.4.2, certificated in `WallCertificates/eja-order.lean`); it is part of "Euclidean", not an extra assumption on the article's `J`. ★★ **The live dependency is different and is named here for the first time: `FirstArgContinuous`.** It unfolds to `ContinuousOn` in the **carrier's** norm topology (`SequentialProduct.lean:104`, `:286`, and that definition's own docstring says so), which at abstract `OrderUnitSpace` generality is **incomparable** with the order-unit norm the article's S2 names — the same independence row 3's cell records for `def:sp`'s S2 clause. The two agree exactly where `ouNorm_le_norm` / `norm_le_sqrt_card_mul_ouNorm` (`Hermitian/OrderUnit.lean`) apply, i.e. on `HermitianMat n 𝕜`. **Clause (i) carries no S2 at all** — `seqLeftMulAbs`'s own docstring says "Only `IsArchimedean` is used; S2 is not" — so it is clean; **clause (ii) carries it**, `sp_smul_left` taking `(harch : IsArchimedean V) (hS2 : P.FirstArgContinuous)`. ★ **The row stays FORMALIZED**, because the article's `J` is a *finite-dimensional* EJA and the article invokes norm equivalence as background where it needs it (`main.tex:1797–1799`, working copy: "all norms on that space are equivalent, so which one is used affects no statement below" — stated in `lem:n2-bounded`'s proof, not at this row, so it is the article's background convention rather than a clause of `lem:homog`), so at the article's own generality the distinction is immaterial. `THEOREM-MAP.md`'s `lem:homog`(ii) section has carried this caveat since the ARC-6 cold review and this cell had not. Clause (ii) was already abstract (`sp_smul_left`). Nothing about matrices enters, so this covers the article's statement, whose ambient $J$ is an EJA and hence an order unit space — the same argument this cell already accepts for clause (ii). Superseded record follows: PARTIAL — ★★★ **EJA-GATED CLAIM WITHDRAWN 2026-08-10, the same day it was made**, by the certificate-refutation review. EJA-GATED requires that *every non-EJA clause* be closed in-tree first, and this row's residue includes clause (ii) is **already proved at abstract order-unit generality** as `SequentialProductOn.sp_smul_left`, so the residue is clause (i)'s abstract port via the **order-unit route**, not the Jordan spectral theorem — the EJA-GATED claim rested on citing `HermitianMat.twistSeq_smul_left`, which is about ONE product (the constant-parameter twist), not an arbitrary S1–S7 one. ★★ **The error was one error made three times (rows 5, 6, 15): I classified each row by its BIGGEST residue and let that stand for its WHOLE residue.** EJA-GATED is a claim about the complement — that nothing else remains — and a claim about a complement cannot be checked by inspecting the largest item in it. Same failure as row 35's "and that is the whole residue". The row is **WALL-CERTIFIED with this arc's attack evidence** instead (`WallCertificates/eja-gated.lean`'s WITHDRAWAL block names the non-EJA clause; the gate statements there remain the honest pricing of the *EJA part* of the residue). Prior record: **EJA-GATED 2026-08-10 (ARC-8 block 8.6)** — certificate `WallCertificates/eja-gated.lean`: every non-EJA clause is closed in-tree (itemized there per row), and the article-generality residue is **gate (E1)** stated in Lean with `sorry` exactly at the axiomatization gap. Prior record: PARTIAL — **clause (ii) is now proved at ABSTRACT order-unit-space generality (2026-08-08, ARC-6 rung 6.3): `SequentialProductOn.sp_smul_left`, carrying only S1–S7, the article's own S2, and `OrderUnitSpace.IsArchimedean` supplied as an explicit `Prop`.** Nothing about matrices enters, so it covers the article's statement, whose ambient `J` is an EJA and hence an order unit space; the article itself only cites this clause (vdW Prop. 3.9) where Lean proves it. The full eight-step ladder is abstract: `sp_natSmul_right` → `sp_divNat_smul_right` → `sp_ratSmul_right` → `sp_smul_right_of_unitInterval` (the Archimedean squeeze) → `sp_comm_natSmul` → `sp_comm_ratSmul_self` → `sp_comm_ratOneSmul` → `sp_ratOneSmul_left` → `sp_smulOne_left` (the single S2 use) → `sp_smul_left`. The row stays PARTIAL only because **clause (i)** remains on the concrete carrier. **And the record for clause (i) was wrong until 2026-08-08 (ARC-6).** ★★ **2026-08-09 (ARC-7 block 7.3): the `IsArchimedean` hypothesis is no longer merely assumed — it is PROVED for the paper's own carrier** (`HermitianMat.isArchimedean`, in `Hermitian/OrderUnit.lean`, by the quadratic-form characterization plus one real ε-argument; no spectral theorem, no topology). ARC-6 defended this FORMALIZED label on the ground that the Archimedean squeeze is part of the *definition* of the article's order unit space rather than a located stand-in — sound, but it left the gap that **no carrier in the tree was known to satisfy it**, so the abstract result applied, as far as the machine knew, to nothing. That gap is closed: `H_n(𝕜)` satisfies it, so the abstract tier is instantiated exactly where the paper uses it. Clause (ii): `Necessity.sp_smul_left` (`Necessity/FirstArgument.lean`). Clause (i): `Necessity.seqLeftMul` (`Necessity/LeftMultiplication.lean`) is the positive linear extension — a genuine `→ₗ[ℝ]`, with `seqLeftMul_apply_effect`, `seqLeftMul_nonneg`, `seqLeftMul_one`, and uniqueness from `OrderUnitSpace.linearMap_eq_of_eq_on_effects`. `THEOREM-MAP.md` had said clause (i) was "not in the tree in any form"; that is retracted. The only gap in either clause is the article's EJA generality | 6.3 |
| 7 | `lem:cone-ext` | 477 | lemma | The product extends from effect to arbitrary positive-cone first arguments by $v\cdot b:=\mu((v/\mu)\cdot b)$, independently of the normalization $\mu\geq\lVert v\rVert$, positively homogeneously, agreeing with the original on effects. | **FORMALIZED 2026-08-08 (ARC-6 rung 6.3), at abstract order-unit-space generality and norm-free.** All three clauses the lemma asserts, and — after a self-audit found the first version stated homogeneity about normalized *expressions* rather than about the extension — **about an actually defined extension**: `SequentialProductOn.spCone` is the article's `μ(v/μ · b)` at a chosen admissible normalization, `spCone_eq` proves it agrees with *every* admissible normalization (so it is well defined, not a family of representatives), `spCone_of_isEffect` is agreement with the original operation on effects, and `spCone_smul` is positive homogeneity in the first argument. The normalization-level lemmas `sp_coneNorm_indep` / `sp_coneNorm_smul` / `sp_coneNorm_eq_of_isEffect` and `exists_isConeNorm` remain as the ingredients. Carries S1–S7 + the article's S2 + `OrderUnitSpace.IsArchimedean`, nothing else. ★ **Improves on the article's route**: the article's admissibility condition is `μ ≥ ‖v‖` and it obtains `v ≤ μ𝟙` from that "in an order unit space" — which presupposes the carrier's norm *is* the order-unit norm, a fact this tree's class does not carry. `IsConeNorm v μ := 0 < μ ∧ IsEffect (μ⁻¹ • v)` states — **not the same requirement, but a strictly weaker one, hence a strictly LARGER admissible set, making the independence clause STRONGER than the article's** (ARC-6 review correction; e.g. `μ = 1` is admissible here for `v = 𝟙` in `H₂` while the article demands `μ ≥ ‖𝟙‖ > 1`). Article-admissible `μ` remain admissible, so the article's statement follows. The class's own order-unit boundedness then supplies an admissible `μ` outright, so no norm hypothesis is needed at all. The concrete `Necessity.thetaNorm` / `theta_smul` remain as the carrier-level instance. ★★ **2026-08-09: the `IsArchimedean` hypothesis this row carries is now PROVED for `H_n(𝕜)`** (`HermitianMat.isArchimedean`), and the extension is *used*: `Necessity.spCone_specInv_eq_one` consumes it to put `prop:pseudo-transfer` into the article's own coefficient-free form (row 13). A cone extension nobody could instantiate would have been a formality; it is now load-bearing | 6.3 ✓ |
| 8 | `lem:simple-bridge` | 505 | lemma | On a f.d. EJA: the standard product makes the effects a convex $\sigma$-sequential effect algebra hence an SES; every effect is simple ($E=E_0$), so vdW's simplicity hypotheses hold for the unknown product too; every invertible effect is order preserving for both products; and the standard product is invariant under unital order isomorphisms. | FORMALIZED — ★★★ **2026-08-24. All four clauses proved at f.d. EJA generality; the row's last cited clause is now a theorem.** Against `main.tex:507-530`: **(i) "the standard product makes eff(J) a convex σ-sequential effect algebra, hence an SES"** (vdW Thm. A.6) — `EJA.ludersSequentialProduct_isConvexSigmaSEA`. Its four components: the S1/S3–S7 structure is `EJA.ludersSequentialProduct`, a genuine `SequentialProductOn J` for `a·b = Q_{√a}b`; paper S2 is `ludersSequentialProduct_firstArgContinuous`; normality is `Normality`'s `sp_isGLB_of_isGLB`, which holds for **every** pinned product on a f.d. Archimedean order-unit space; convexity is `isEffect_convexCombo`. ★ "hence an SES" is vdW's *name* for a convex σ-SEA on an order-unit space, so nothing further is asserted by it — stated here rather than left implicit. **(ii) `E = E₀`** — `EJA.isSimpleEffect_of_isEffect`, proved 2026-08-23. **(iii) every invertible effect order preserving for both products** — this is manifest row 13's entire statement, FORMALIZED 2026-08-24 (`prop_pseudoTransfer` plus `lam_ne_zero_of_invertible`). **(iv) invariance under unital order isomorphisms** — `EJA/OrderAuto.lean`'s `map_jordan_of_orderIso`, which had been proved and consumed by nothing. ★★★ **What closed (i) was the `SequentialProductOnEJA` inhabitant**, and building it took the whole S-axiom chain: **S2** from compactness of the effect interval and continuity of the Jordan square root (`luders_continuousOn_isEffect`), which needed self-duality of the cone, closedness, and **uniqueness** of the positive square root; and **S5's own hypothesis, S6a, S6b and S7** from `prop:bridge` (row 10, FORMALIZED the same day), with S7 additionally needing the operator-commutant to be a subalgebra — `opCommute_mul`, via `opCommute_spectralIdem` (commutation passes to the spectral idempotents, through the Lagrange polynomial) and `opCommute_idem_mul` (at an idempotent the commutant is a subalgebra, because operator commutation there *is* the vanishing of the Peirce half-part). ★★ **The `~3/4-cited` pricing from ARC-6 rung 6.0 is retired**, not overridden: under the 2026-08-23 scope reversal the cited clauses were in scope, and all three of them — (i), (iii), (iv) — are now in-tree theorems rather than citations. ★ Axioms on every declaration named here: `[propext, Classical.choice, Quot.sound]`. No `sorryAx`. Prior record: PARTIAL — ★★★ **S2 CLOSED 2026-08-24** (`EJA/ConeTopology.lean`): `luders_continuousOn_isEffect` proves `a ↦ Q_{√a}b` continuous on the effects, i.e. **paper S2 for the Lüders product at EJA generality**. The chain built for it, none of which the tree had: self-duality of the Jordan cone (`isSoS_iff_forall_inner_nonneg`), hence the cone is **closed** and the effect interval **compact** (`isCompact_effectSet`); **uniqueness of the positive square root** (`jsqrt_mul_self_of_isSoS`, `eq_of_mul_self_eq_of_isSoS`) — `jsqrt` was known to *be* a square root, never to be *the* one; continuity of the Jordan product (`continuous_jmul`); and then `continuousOn_jsqrt`, because squaring is a continuous bijection of a compact effect interval onto itself and such a map is a homeomorphism. ★ So the inhabitant now has **S1, S2, S3, S4** outright and S5 under operator commutation. **What remains is S6, S7 and S5's own hypothesis, and all three reduce to one thing: row 10's bridge** (`a·b = b·a ⟺ ⁅L_a,L_b⁆ = 0`). Given the bridge, S6a and S6b are immediate (`T_{1-b} = id - T_b`, `T_{b+c} = T_b + T_c`) and S7 needs the operator-commutant of `a` — the Peirce-diagonal subalgebra of `a`'s spectral idempotents — to be closed under the product. ★★ **The bridge's converse is not a formality**: on the matrix carrier it is exactly "a normal product of two positive operators is self-adjoint", proved there by "the product of two PSDs has nonnegative real spectrum, and a normal operator with nonnegative real spectrum is positive". That spectral argument has no direct EJA transcription, and inventing one is the open problem. Prior record: PARTIAL — ★★★ **CLAUSE (iii) CLOSED 2026-08-24; the residue is now clause (i) ALONE.** Clause (iii) reads "every invertible effect is order preserving for both products", cited to vdW Props. 4.19–4.20. That is **manifest row 13's entire statement**, and row 13 reached FORMALIZED on 2026-08-24: `EJA/PseudoTransfer.lean`'s `sp_orderReflection` + `sp_lowerIntervalSurj` give both clauses of vdW's Def. 4.17 for the unknown product, `luders_reflectsNonneg` + `luders_lowerInterval_surj` give them for the standard one, `prop_pseudoTransfer` assembles all four, and `lam_ne_zero_of_invertible` supplies the "every invertible effect" quantifier. ★ **This is the eighth time a clause of this row was closed in the tree before its cell said so** — recorded rather than quietly fixed. ★★ **Residue, exactly: clause (i) — vdW Thm. A.6, "the standard product makes the effects a convex σ-sequential effect algebra, hence an SES".** Its σ half is no longer missing machinery: `Normality.lean`'s `sp_isGLB_of_isGLB` (2026-08-24) proves normality — `b_k ↓ b ⇒ a·b_k ↓ a·b` — for **any** `SequentialProductOn` on a f.d. Archimedean order-unit space, which is what "σ-" asks for. ★★★ **What clause (i) still needs is the `SequentialProductOnEJA` inhabitant**: the Lüders product `a·b = Q_{√a}b` packaged as a `SequentialProductOn`. S1, S3, S4 and S5 are proved (`EJA/Class.lean`), S5 under operator commutation rather than the axiom's own compatibility hypothesis; **S2, S6 and S7 remain**, and S5's hypothesis needs row 10's bridge. That inhabitant is the shared blocker of rows 8, 16 and 21 — it is the single highest-leverage open object in this file. Clauses (ii) and (iv) were already proved (`isSimpleEffect_of_isEffect`, `map_jordan_of_orderIso`). Prior record: PARTIAL — ★★★ **CLAUSE (iv) IS ALSO PROVED IN THE TREE, found 2026-08-23 by an unused-theorem sweep.** Clause (iv) reads "the standard product is *invariant*: for every unital order isomorphism `Φ` of `J`, `Φ(a∘b) = Φa∘Φb`". That is `EJA/OrderAuto.lean`'s `map_jordan_of_orderIso` verbatim — `Φ (x * y) = Φ x * Φ y` for a unital linear order isomorphism of a finite-dimensional formally real Jordan algebra — sorry-free, and consumed by nothing. The same theorem is row 14's supposedly-open Koecher/Alfsen–Shultz input; **one unused declaration was carrying the residue of two rows.** ★ **Clause status now: (ii) interior and PROVED here (`isSimpleEffect_of_isEffect`, 2026-08-23); (iv) PROVED (`map_jordan_of_orderIso`, and it was proved before this row was last priced). Open: (i) vdW Thm A.6 — the effects are a convex σ-sequential effect algebra, hence an SES; (iii) vdW Props 4.19–4.20 — every invertible effect is order preserving for both products.** Under the 2026-08-23 scope reversal both are in scope rather than external, so this row's residue is two named cited theorems and nothing else. Prior record: PARTIAL — ★★★ **THE ROW'S ENTIRE INTERIOR CLAUSE IS CLOSED 2026-08-23; the status word is deliberately NOT moved.** The prior cell stated the residue exactly — "define `E₀`, prove `idempotent ⟹ IsSharp` for the effect order, and state 'every effect is simple'" — and all three are now done. `OrderUnitSpace.IsSimpleEffect` is vdW's class `E₀` transcribed from `main.tex:507-516` at source ("an effect with a finite spectral decomposition `a = ∑ₖ λₖ pₖ` into orthogonal sharp effects"), with `IsOrthogonalFamily` taking the **subset-sum** reading of orthogonality, which is strictly stronger than pairwise and is what a spectral decomposition actually satisfies. `EJA.isSharpOrderUnit_of_idem` proves `idempotent ⟹ IsSharp`, and `EJA.isSimpleEffect_of_isEffect` is `E = E₀` at the article's own generality — the article's one-sentence proof (`main.tex:549-550`) made into `spectral_resolution_bilinear` plus sharpness of its idempotents plus completeness read through the cone. Supporting: `inner_idem_isSoS_nonneg` (an idempotent pairs nonnegatively with the cone — extracted from `nonneg_coeff_of_isSoS`, which proved it en route and discarded it) and `eq_zero_of_isSoS_of_inner_unit_eq_zero`. ★★ **A near-duplication, recorded because it is a live trap.** `EJA/OrderAuto.lean` already has `isSharp_of_idem`/`idem_of_isSharp`/`isSharp_iff_idem` — **both** directions — but against its own `EJA.IsSharp e c`, a sums-of-squares spelling at class generality, and that file says in as many words that "no lemma below relates the two". The new theorem targets `OrderUnitSpace.IsSharp`, the article's order-theoretic sharpness and the one `E₀` is defined against, at bilinear-map generality. `EJA.IsSharp` is the **stronger** of the two spellings (its quantifier asks only `IsSoS x` where the order-theoretic one asks for a full effect), so the bridge should run `EJA.IsSharp ⟹ OrderUnitSpace.IsSharp` and would make the new proof redundant — **that bridge still does not exist**, and building it is now the cheapest cleanup on this row. ★★★ **Why the word stays PARTIAL.** The row has four clauses and the article assigns (i) to vdW Thm. A.6, (iii) to vdW Props. 4.19–4.20 and (iv) to a vdW remark; only (ii) is interior, and the ~3/4-cited pricing from ARC-6 rung 6.0 stands. Clause (ii) closing does not close the row, and letting it stand for the row would be the biggest-residue error this file has already recorded four times, run forwards. **The residue is now exactly the three cited clauses and nothing else** — which makes this row a candidate for the EXTERNAL category alongside 1/2/4/10/14/21, and that is a manifest judgement, not a build's to make (row 10's cell sets the precedent verbatim). Axioms: all six new declarations `[propext, Classical.choice, Quot.sound]`, no `sorryAx`. Prior record: **PARTIAL — PROMOTED FROM ABSENT 2026-08-22, and the reason this cell had been giving is dead rather than merely amended.** It read "clause (ii) at EJA generality needs the Jordan spectral theorem, which Mathlib lacks". **Mathlib still lacks it; this tree no longer does.** `RadicalRelativity.EJA.spectral_resolution_bilinear` (`EJA/Spectral.lean:580`) proves, for a finite-dimensional `J` with a commutative Jordan bilinear map `m`, formal reality, and a unit `e`, that every `x` is $\sum_i \lambda_i q_i$ over pairwise-`m`-orthogonal idempotents $q_i$ summing to `e` — the article's own stated proof of clause (ii) ("holds by the Jordan spectral theorem: every element is a finite real combination of orthogonal idempotents", `main.tex:549–550`), at the article's own generality and in `ComparisonSetup`'s vocabulary. ★★★ **ADJUDICATION, because this is a judgment call and the reasoning must be inspectable.** The row has four clauses; the article's own proof assigns (i) to vdW Thm. A.6, (iii) to vdW Props. 4.19–4.20 and (iv) to a vdW remark, so **only (ii) is interior** — that pricing from ARC-6 rung 6.0 stands and this row is still to be read as **~3/4 cited**. The question is whether the interior clause's mathematics being in the tree, while the clause itself is unstated, is PARTIAL or ABSENT. **PARTIAL, on three grounds.** (a) This file's ABSENT gloss is "*nobody has built it*", and the mathematics is built. (b) PARTIAL's own bracket already admits declarations that are not the row's literal statement — "a generator-level rather than operation-level version", "a certified concrete example in place of the general lemma" — and this is a version of clause (ii) in that sense. (c) The gap between the Lean theorem and the clause is **sharp ⟺ idempotent**, which the article itself attributes to a citation (`main.tex:511–515`, vdW Prop. 3.15), so the part of clause (ii) that is not covered is external by the article's own attribution too. ★ Line numbers for `main.tex` in this cell are the **working copy**, not the pinned blob; the pinned `§` column is unaffected. ★★ **What PARTIAL must NOT be read as here, stated so nobody overreads the word.** Clause (ii) is not stated in Lean. There is **no definition of a simple effect or of vdW's class $E_0$ anywhere in the checkout** — `grep -r "IsSimpleEffect\|SimpleEffect"` over every `.lean` file returns **0 of 9700**, and the only hits for `E₀` are matrix units and an unrelated filtration chain (2026-08-22) — and `OrderUnitSpace.IsSharp` (`OrderUnitSpace.lean:161`), which is the article's own order-theoretic sharpness, occurs on exactly **1** line of the whole checkout — its own definition, across 9700 `.lean` files including vendored Mathlib. ★ **The residue, exactly:** define $E_0$, prove `idempotent ⟹ IsSharp` for the effect order, and state "every effect is simple". The carrier link the bridge needs landed the same day — `EJA.orderUnitSpaceOfBilinear` (`EJA/Order.lean:252`) makes an abstract EJA an `OrderUnitSpace`, so `IsSharp` and `IsEffect` are now expressible where the spectral resolution lives; before it, the two halves were over different carriers. ★ `EJA-DIVIDEND.md`'s row-8 cell independently reached the same identification ("(E1) gives clause (ii) — 'every effect is simple' IS the Jordan spectral theorem") and (E1) is now a proved theorem. Superseded record follows: ABSENT — **priced per clause 2026-08-08 (ARC-6 rung 6.0): this row is mostly by-design-external, like `prop:bridge`.** Read at source, the article's own proof assigns (i) to vdW Thm. A.6, (iii) to vdW Props. 4.19–4.20, and (iv) to a vdW remark; only (ii) ("every effect is simple") is interior, and it is the Jordan spectral theorem. So the honest target is clause (ii) *on the concrete carrier*, where Mathlib's spectral theorem applies — cheap; clause (ii) at EJA generality needs the Jordan spectral theorem, which Mathlib lacks. This is the row the ceiling arithmetic flagged as needing honest pricing, and the price is: it should be read as ~3/4 cited | 6.4 (clause (ii), concrete) |
| 9 | `lem:normality` | 568 | lemma | On a f.d. order-unit space, any operation satisfying S1 and S2 is normal in vdW's sense: $b_k\downarrow b\Rightarrow a\cdot b_k\downarrow a\cdot b$, and compatibility passes to order-infima. | FORMALIZED — ★★★ **2026-08-24, at the article's own abstract f.d. order-unit generality (`RadicalRelativity/Normality.lean`). Both clauses of the article's sentence are now theorems, and the two items this cell recorded as open are closed by proof, not by hypothesis.** **Clause 1, `b_k ↓ b ⇒ a·b_k ↓ a·b`** — `sp_isGLB_of_isGLB`: `IsGLB` in, `IsGLB` out, with **no convergence hypothesis in the statement**, so both `↓`s are the article's order-theoretic ones. **Clause 2, compatibility passes to order-infima** — `compatible_of_isGLB_of_firstArgContinuous`, stated with `SequentialProductOn.FirstArgContinuous`, i.e. **paper S2 exactly as the tree declares it**. ★★★ **The missing analytic input was built, not assumed:** `tendstoOu_of_antitone_isGLB` proves that a decreasing sequence of effects converges in the order-unit norm to its greatest lower bound — the "decreasing sequence converges to its infimum" this cell recorded as *absent from the tree*. **This is the only place finite-dimensionality is used anywhere in the row.** ★★★ **And `OuNormEquiv` is a theorem, not a hypothesis** (`ouNormEquiv_of_finiteDimensional`): `ouNorm` is positive-definite (`eq_zero_of_ouNorm_eq_zero`), subadditive (`ouNorm_add_le`) and absolutely homogeneous (`ouNorm_smul`), hence a genuine norm, hence equivalent to the carried norm in finite dimension — easy direction by basis expansion, hard direction by minimising the (now continuous) `ouNorm` over the compact carried-norm unit sphere. The `OrderUnitSpace` class relates its order to its carried norm by **no field at all**, so without this the two clauses could not have been connected. ★★ **`continuousOnOu_iff_continuousOn` / `firstArgContinuousOu_iff_firstArgContinuous` then collapse the ε–δ order-unit continuity notion into Mathlib's `ContinuousOn`,** which is why clause 2 can carry the paper's own S2 rather than an order-unit twin of it. Conflating the two without that bridge would have been a real error. ★★ **The previous cell's asymmetry survives and sharpens.** Clause 1 needs neither S2 **nor finite-dimensionality**: `ouNorm_seqLeftMulAbs_le` shows `x ↦ a·x` is a **contraction for the order-unit norm** (`ouNorm (a·x) ≤ ouNorm x`), straight from positivity plus `a·𝟙 = a ≤ 𝟙`, so second-argument continuity is `δ := ε` outright — strictly stronger than the concrete carrier's "linear on a f.d. space, hence continuous". Clause 2 does need S2, because it moves the **first** argument. ★ **Honest scope notes.** (i) Statements quantify over a full `P : SequentialProductOn V` (S1, S3–S7); the claim "only S1" is about what the *proof* consumes, not about the statement's hypotheses. (ii) `IsArchimedean` is assumed throughout, which this file's own convention (`OrderUnitSpace.lean`, `IsArchimedean` docstring) treats as the article's own generality. (iii) The article writes `b_k`, a sequence, and these are ℕ-indexed sequences — not nets. ★ Axioms on every declaration named here: `[propext, Classical.choice, Quot.sound]`. No `sorryAx`. Prior record: PARTIAL — **repriced ABSENT → PARTIAL 2026-08-09 (ARC-7 block 7.5), and this row REFUTED THIS ARC'S OWN WALL CERTIFICATE within the hour it was written.** `WallCertificates/abstract-tier.lean` priced it "plausibly cheap; never attempted"; it was cheap. `Necessity.sp_tendsto_of_tendsto` proves the convergence clause on the concrete carrier. ★★ **And the result is STRONGER than the article's statement: S2 is not used at all.** Additivity alone (S1) extends $b\mapsto a\cdot b$ to the linear map `seqLeftMul`, and a linear map on a finite-dimensional normed space is automatically continuous — so second-argument continuity, which S2 does *not* supply, is free on this carrier. ★★ **The compatibility clause landed 2026-08-09 (ARC-8 block 8.2)**: `Necessity.compatible_of_tendsto` — if `a` is compatible with every term of a convergent sequence of effects it is compatible with the limit. ★ **And it needs S2, which the convergence clause did not** — that asymmetry is the content: `a·b_k → a·b` is free from `seqLeftMul`'s linearity, but `b_k·a → b·a` is *first*-argument continuity, i.e. exactly S2. A reader who carried the previous clause's "S2 is not used at all" forward to this one would be wrong. Open: the article's **order-infimum** form (deriving it from the sequential form additionally needs "a decreasing sequence of effects converges to its infimum" for the Loewner order — absent from the tree — grep scope recorded at the theorem and **corrected on 2026-08-10** when dry-pass round 3 caught it undercounting: `iInf\|⨅` has ~38 non-comment hits, of which exactly one touches `HermitianMat` and it is an infimum over real eigenvalues, not over the Loewner order), and the article's abstract f.d. order-unit generality (`⨅` for this order is not developed in the tree, so both clauses here use a sequence and an explicit limit) | 5.1 |
| 10 | `prop:bridge` | 612 | proposition | For EJA effects under the standard product, $\lprod{a}{b}=\lprod{b}{a}\iff[Q_a,Q_b]=0\iff[T_a,T_b]=0$: standard-product compatibility is exactly Jordan operator commutation. | FORMALIZED — ★★★ **2026-08-24, in full and at EJA generality** (`EJA/ConeTopology.lean`). `bridge_tfae` proves the article's three conditions equivalent for cone elements of a finite-dimensional Euclidean Jordan algebra: **`Q_{√a}b = Q_{√b}a  ⟺  ⁅Q_a,Q_b⁆ = 0  ⟺  ⁅L_a,L_b⁆ = 0`**. Stated for the whole cone, which strictly contains the article's effects. ★★★ **The two converses this cell recorded as the reason the row was not FORMALIZED are the ones now proved.** **(1) `opCommute_of_luders_comm`** — standard-product compatibility implies operator commutation, the hard direction, previously available only on the matrix carrier as `Necessity.commute_of_twistSeq_comm`. **(2) `opCommute_of_quadJ_comm`** — evaluating `⁅Q_a,Q_b⁆ = 0` at the unit turns it into Lüders compatibility of `a²` and `b²`, converse (1) commutes those, and `a`, `b` are their square roots hence resolved by the same family. The forward legs are `luders_comm_of_opCommute` (simultaneous resolution, both orders equal `∑ λₖμₖqₖ`) and `quadJ_comm_of_opCommute_resolution`. ★★★ **How converse (1) goes, since it was priced as research and is not.** The matrix proof is a *Frobenius certificate*; its four traces reassemble in Jordan vocabulary into one scalar, the **Peirce defect** `Φ(v,x) = ⟪v∘v, x∘x⟫ − ‖v∘x‖²`. Four steps, each machine-checked: **(a)** the hypothesis forces `Φ(√b,a) = 0` (`luders_comm_peirceDefect_eq_zero`), on the identity `⟪Q_v x,x⟫ = 2‖v∘x‖² − ⟪v∘v,x∘x⟫` plus `Q_u(u²) = (u²)²` plus `(√a)² = a`; **(b)** `2Φ = ∑_{k,l}(μₖ−μₗ)² ⟪qₖ∘(qₗ∘x),x⟫` over the spectral resolution (`two_peirceDefect_eq_sum`), every term nonnegative because **`4L_{q_k}L_{q_l}` is a self-adjoint idempotent** (`blockProj_idem`) — so `Φ ≥ 0` always (`peirceDefect_nonneg`, itself a Cauchy–Schwarz-shaped EJA inequality that appears to be new) and `Φ = 0` kills every off-diagonal block (`blockPairing_eq_zero_of_peirceDefect`); **(c)** block-diagonality is the vanishing of the Peirce half-part, by summing against `∑ qₖ = 1` (`peirceHalf_eq_zero_of_blockDiagonal`); **(d)** a vanishing half-part *is* operator commutation, since `a = P₁a + P₀a` and `L_c` commutes with both (`opCommute_of_peirceHalf_eq_zero`). ★ **No joint Peirce decomposition is used anywhere** — the block projection identity replaces it. ★★ **Verified numerically before being built**, on random Hermitian pairs: `Φ = ¼‖⁅v,x⁆‖²_F` to 2.8e-14, `Φ > 0` off the commuting locus, and `‖P²−P‖ ≤ 1.3e-15` with block ranks exact. ★★★ **What this unlocks:** S5's own hypothesis, S6a, S6b and S7 for the Lüders product all reduce to this bridge, so it is the gate on the `SequentialProductOnEJA` inhabitant that **rows 8, 16 and 21** wait on. S2 for that product was closed the same day (`luders_continuousOn_isEffect`). ★ Axioms on every declaration named here: `[propext, Classical.choice, Quot.sound]`. No `sorryAx`. ★ The pre-registered EXTERNAL terminal state is moot: the row is interior now, in this tree. Prior record: PARTIAL — ★★★ **2026-08-24: THE CONVERSE HAS A VERIFIED EJA PROOF STRATEGY AND HALF OF IT IS MACHINE-CHECKED.** The cell below records the converse out of the standard product as the reason the row is not FORMALIZED, and prices it as research. It is no longer open-ended, and this entry states exactly what is proved and what is not. ★★ **Where the strategy came from.** `Necessity`'s `commute_of_twistSeq_comm` (`Hermitian/Sequential.lean`) already proves the converse **on the matrix carrier**, by a *Frobenius certificate*: for `C = √b·a − a·√b`, expand `tr(C Cᴴ)` into four traces, show each equals `tr(b a²)` — one of the four by the hypothesis — so the certificate vanishes and `C = 0`. That proof is not matrix-specific in the way it looks: the four traces reassemble in Jordan vocabulary into a single scalar. ★★★ **The scalar is `Φ(v,x) := ⟪v∘v, x∘x⟫ − ‖v∘x‖²` (`EJA.peirceDefect`), and the two halves are:** **(A) the hypothesis forces `Φ(√b, a) = 0`** — PROVED, `EJA.luders_comm_peirceDefect_eq_zero` (`EJA/ConeTopology.lean`), on the identity `⟪Q_v x, x⟫ = 2‖v∘x‖² − ⟪v∘v, x∘x⟫` (`EJA.inner_quadJ_self`) plus `Q_u(u²) = (u²)²` (`EJA.quadJ_self_sq`, off the fundamental formula) plus `(√a)² = a` (`EJA.jsqrt_sq_of_isSoS`), and the hypothesis is consumed exactly once. **(B) `Φ(v,x) = 0 ⟹ ⁅L_v, L_x⁆ = 0`** — OPEN, and it is the whole remaining content. ★★ **Why (B) is true, worked out and recorded so it is not re-derived.** `Φ(v,x) = ⟪D x, x⟫` with `D := L_{v∘v} − L_v²`. For the spectral resolution `v = ∑ μₖqₖ` with `∑ qₖ = 1`, substituting `∑_l L_{q_l} = id` and symmetrising in `k ↔ l` (legal because `L_{q_k}` and `L_{q_l}` commute for orthogonal idempotents) gives **`D = ½ ∑_{k,l} (μₖ−μₗ)² L_{q_k}L_{q_l}`**. Each `L_{q_k}L_{q_l}` is a product of *commuting* positive operators, hence positive, so `Φ ≥ 0`; and if `Φ = 0` then every term with `μₖ ≠ μₗ` — which by distinctness of a spectral resolution means every `k ≠ l` — has `⟪L_{q_k}L_{q_l}x, x⟫ = 0`, hence `L_{q_k}L_{q_l}x = 0`, hence `x` is Peirce-diagonal for `{qₖ}`, which is `⁅L_v,L_x⁆ = 0`. On `J_{ij}`, `L_{q_k}L_{q_l}` acts by `((δ_{ik}+δ_{jk})/2)((δ_{il}+δ_{jl})/2)`, so for `k ≠ l` it is `¼` on the block `J_{kl}` and `0` elsewhere. ★★ **Verified numerically before being recorded** (200 random Hermitian pairs, sizes 2–5): `Φ(v,x) = ¼‖⁅v,x⁆‖²_F` to 2.8e-14, `0` on commuting pairs, and `Φ > 0` on 500 random pairs — so the identity, the sign and the equality case are all confirmed, not conjectured. ★★★ **(B)'s CRUX IS NOW PROVED TOO, 2026-08-24** (`EJA/ConeTopology.lean`): for orthogonal idempotents `p, q`, **`4 L_p L_q` is a self-adjoint idempotent** — `blockProj_idem` — so `⟪L_pL_q x, x⟫ = 4‖L_pL_q x‖²` (`inner_blockProj_self`), hence `≥ 0` (`inner_blockProj_nonneg`) and **vanishing forces `L_pL_q x = 0`** (`blockProj_eq_zero_of_inner_eq_zero`). That is exactly the positivity *and* the equality case (B) consumes, with **no joint Peirce decomposition anywhere**. The chain: `q` annihilates `J₁(p)` (`mul_peirceOne_eq_zero_of_orth`, off the tree's `eigen_one_mul_zero`), so `L_q Q_p = 0`, and since `Q_p = 2L_p² − L_p` at an idempotent that reads `L_q L_p² = ½ L_q L_p` (`mul_mul_mul_eq_half`); with `⁅L_p,L_q⁆ = 0` (`opCommute_of_orthogonal`) it gives `(L_pL_q)² = ¼ L_pL_q`. ★★ **What is left of (B) is now bookkeeping, not mathematics**: the algebraic expansion `D_v = ½ ∑_{k,l} (μₖ−μₗ)² L_{q_k}L_{q_l}` (substitute `∑_l L_{q_l} = id` into `L_{v∘v} − L_v²` and symmetrise), and the final passage from `L_{q_k}L_{q_l}x = 0` for all `k ≠ l` to `⁅L_v,L_x⁆ = 0` — for which the cheaper target is a **shared resolution** of `v` and `x` (resolve each `xₖ` inside `J₁(qₖ)` via `EJA/PeirceSubalgebra.lean`), since that is what S5–S7 consume anyway. ★ Prior route note: **the joint Peirce decomposition is NOT needed.** For *orthogonal* idempotents `qₖ, qₗ` the operator `4 L_{q_k}L_{q_l}` is a **self-adjoint idempotent** — the Peirce block projection onto `J_{kl}` — so `⟪L_{q_k}L_{q_l}x, x⟫ = ¼‖P_{kl}x‖² ≥ 0` and vanishing forces `L_{q_k}L_{q_l}x = 0` outright, with no block algebra at all. **Verified numerically** on `H₅(ℂ)` with a three-part spectral partition: `‖P² − P‖ ≤ 1.3e-15`, `‖P − Pᵀ‖ ≤ 4.4e-16`, `‖⁅L_{q_k},L_{q_l}⁆‖ ≤ 1.1e-16`, and ranks `4, 8, 4` matching `2·\|pₖ\|·\|pₗ\|` exactly. So (B) reduces to four in-tree-shaped facts: `⁅L_{q_k},L_{q_l}⁆ = 0` for orthogonal idempotents (cf. `EJA/Block.lean`'s `opCommute_of_orthogonal`), `4(L_{q_k}L_{q_l})² = L_{q_k}L_{q_l}`, self-adjointness of `L_q`, and the algebraic expansion `D_v = ½ ∑ (μₖ−μₗ)² L_{q_k}L_{q_l}`. ★★ **One further simplification worth having**: `L_{v∘v} − L_v² = L_v² − Q_v`, since `Q_v = 2L_v² − L_{v∘v}`, so `Φ(v,x) = ‖v∘x‖² − ⟪Q_v x, x⟫` and the whole defect is expressible without `L_{v∘v}`. In the associative case `D_v x = ¼⁅v,⁅v,x⁆⁆`, which is why the numerics return `¼‖⁅v,x⁆‖²_F`. ★ **The last step after (B)** — from `L_{q_k}L_{q_l}x = 0` for all `k ≠ l` to `⁅L_v,L_x⁆ = 0` — is the one place blocks still appear: `x` Peirce-diagonal means `x = ∑ xₖ` with `xₖ ∈ J₁(qₖ)`, and both `L_v` and `L_x` preserve every block with `L_v` scalar on each, so they commute blockwise. `EJA/PeirceSubalgebra.lean` is the natural home; alternatively resolve each `xₖ` inside `J₁(qₖ)` to build a **shared resolution** of `v` and `x` directly, which is what S5–S7 consume anyway and skips `⁅L_v,L_x⁆` entirely. ★ **What (B) was previously thought to need:** the joint Peirce decomposition of an *orthogonal idempotent family* summing to `1`. `EJA/FramePeirce.lean` has `frameBlock`, `blockProj` and `frame_peirce_span` for a `JordanFrame` — a family of **primitive** idempotents — and a spectral resolution is not primitive, so the machinery needs generalising, or the argument needs routing through `L_{q_k}L_{q_l}` positivity directly, which avoids blocks entirely. ★★★ **What (B) unlocks, which is why it is the highest-leverage open object in this file:** given the bridge, S5's own hypothesis, S6a, S6b and S7 all follow for the Lüders product — S6a and S6b immediately (`T_{1−b} = id − T_b`, `T_{b+c} = T_b + T_c`), S7 from closure of the commutant — which completes the `SequentialProductOnEJA` inhabitant and moves **rows 8, 16 and 21**, as well as this row. Prior record: **PARTIAL — 2026-08-23, from ABSENT.** The row's terminal state stays **EXTERNAL by pre-registration** and that decision is untouched; what changes is the *status word*, because this file's own gloss for ABSENT is "**nobody has built it**" and that is now false, while PARTIAL's own bracket already admits "a concrete carrier where the article is abstract". **Built:** `Necessity.bridge_of_commute`, `bridge_of_opCommute`, `bridge_of_opCommute_effect`, `quadOp`, `quadOp_eq_jordan` (`Q_a = 2T_a² − T_{a²}`), `quadOp_comm_of_commute`, `quadRep_comm_of_commute` — `RadicalRelativity/PaperA/CompatibilityBridge.lean`, at **`RCLike 𝕜`** generality, core axioms. The pre-existing `T` leg is `opCommute_iff_commute(G)`, both directions. ★ **All eight `prop:bridge` citations in `main.tex` were read at source** (671, 738, 793, 853, 858, 922, 961, 2076): seven substantive, **every one running the same direction** — operator commutation is established first, then the bridge yields standard-product compatibility — and the eighth is related-work attribution. **No use site consumes a converse, and none mentions $[Q_a,Q_b]$**; the `Q` leg is proved here for statement fidelity, not because the article needs it. **Not proved, and this is why the row is not FORMALIZED:** both converses out of the standard product, and everything at EJA generality — ★★★ **"none, by design" was FALSE and is corrected 2026-08-22; the EXTERNAL pre-registration is NOT re-litigated.** The direction the article consumes is now proved in-tree on the concrete carriers at `RCLike 𝕜` generality: `Necessity.bridge_of_opCommute` (`RadicalRelativity/PaperA/CompatibilityBridge.lean`) takes Jordan-operator commutation to BOTH of the remaining legs — `quadRep a b = quadRep b a` for the standard product (`Necessity.quadRep a` *is* $Q_{\sqrt a}$, `Necessity/Theta.lean:46`) and `[Q_a, Q_b] = 0` — while the `T` leg was already an honest `↔` in both directions (`Necessity.opCommute_iff_commuteG`, `Necessity/ComparisonInstanceGen.lean:164`). Its hypotheses are `0 ≤ a`, `0 ≤ b`, which the article's *effects* imply, so it applies at every use site; `bridge_of_opCommute_effect` states the effect-level form. `Necessity.quadOp_eq_jordan` pins $Q_a = 2T_a^2 - T_{a^2}$, so the $Q$ leg is about the article's $Q$ and not a bare conjugation. Axioms exactly `[propext, Classical.choice, Quot.sound]`; `AxiomAudit` census PASS at 169 modules, custom axioms `[]`. ★★ **All eight citations of `prop:bridge` were read at source** (working-copy lines 671, 738, 793, 853, 858, 922, 961, 2076 — the working copy is the pinned blob `4b0dba30`, so these are current line numbers, not `§`-column ones). Seven are substantive — five inside proofs, two in the import ledger discharging the compatibility hypothesis of vdW Props. 5.2 and 5.5 — and **all seven run the same direction**: operator commutation is established first, from simultaneous diagonalisability or centrality, and the bridge then supplies standard-product compatibility. The eighth (2076) is related-work attribution. **No use site consumes a converse, and none mentions $[Q_a,Q_b]$ at all.** ★★ **What is NOT proved, which is why the row stays cited:** the two converses out of the standard product, and everything at EJA generality. On this carrier the standard-product converse is exactly "$\sqrt b\,\sqrt a$ normal $\Rightarrow$ $\sqrt b\,\sqrt a$ self-adjoint", because $(\sqrt b\sqrt a)^{*}(\sqrt b\sqrt a) = Q_{\sqrt a}b$ and $(\sqrt b\sqrt a)(\sqrt b\sqrt a)^{*} = Q_{\sqrt b}a$. ★ **RECOMMENDATION, deliberately NOT acted on here: ABSENT → PARTIAL.** This file's ABSENT gloss is "*nobody has built it*" and that is now false, and PARTIAL's own bracket already covers "a concrete carrier where the article is abstract". The word is left at ABSENT because moving it moves the census (16 / 17 / 3 → 16 / 18 / 2) and that is a manifest judgement, not a build's to make | ext |
| 11 | `lem:aone` | 637 | lemma | $a\cdot\id=a$ for every effect $a$. | **FORMALIZED** — `SequentialProduct.sp_unit_right`, abstract `[SequentialProductCore V]`, by the article's own S1/S3/S4/S6 route. *Was listed ABSENT until 2026-08-08; the row was already true.* | — |
| 12 | `prop:central` | 650 | proposition | For $J=\bigoplus J_\alpha$ and an S1–S7 product, $a\cdot b=\sum_\alpha a_\alpha\cdot b_\alpha$ with each restriction an S1–S7 product on the summand; conversely summand products assemble. Distinct simple summands cannot couple. | FORMALIZED — ★★★ **2026-08-24, at the article's own `m`-fold generality and with every citation discharged** (`RadicalRelativity/DirectSumRestrict.lean`, `RadicalRelativity/DirectSum.lean`, `RadicalRelativity/OrthFamily.lean`). Clause by clause against `main.tex:652-668`: **(1) `a·b = ∑_α a_α·b_α`** — `sp_componentwise_pi`. **(2) `a_α·b_α ∈ J_α`** — immediate in the Pi encoding, with `isEffect_pi_iff` giving that each component is an effect of its summand. **(3) "each restriction is a sequential product satisfying S1–S7 on `(J_α, ≤, e_α)`"** — `restrictPi`, all eight fields. **(4) "a general product is thus the direct sum of its summand products"** — `sp_eq_pi_restrict`. **(5) "distinct simple summands cannot couple"** — `sp_single_apply_ne`, and more sharply `sp_apply_single`. **(6) the converse** — `pi` assembles summand products, and `restrictPi_pi` proves it restricts back to them, so the two halves are one bijection and not two constructions. ★★★ **The three bridge/vdW-5.2 citations `central_decomposition` carried as hypotheses are now THEOREMS.** `OrthFamily`'s new `sp_sharp_value_le` (a sharp effect is the identity on everything below it — **S2-free**, unlike the spectral value law) and its dual `sp_le_sharp_right` combine into `sp_sharp_split_left`/`_right`: if `x ≤ p` and `y` is orthogonal to a sharp `p`, then `(x+y)·p = x = p·(x+y)` for **any** S1–S7 product. ★ That yields `a·e_α = e_α·a` and `a·e_α = a_α` — the two facts the row previously imported — because a direct sum splits every effect in exactly that shape. `sp_central_decomposition` is the resulting citation-free `n`-ary restatement of `MasterTheorem.Central.central_decomposition`. ★★ **Proved at strictly GREATER generality than the article asks**: the carrier is any finite direct sum of order-unit spaces (`instPi`), with **no** Euclidean-Jordan structure, **no** `IsArchimedean`, **no** finite-dimensionality and **no** S2 anywhere in the row. Signatures verified by `#check`. ★ **Two scope notes, stated rather than buried.** (i) The article writes `a_α := e_α ∘ a` using the *Jordan* product; here the component is the direct-sum projection. The article's own equation is what gets proved — `sp_central_single` gives `a ◦' e_α = a_α` — but the separate identification "`e_α ∘ a` = the projection in an EJA direct sum" is not formalized, because the tree carries no `EuclideanJordanAlgebra` instance on a Pi type. It is a fact about the Jordan product, not about the sequential product this row is about. (ii) `sp_eq_prod_restrict` / `sp_eq_pi_restrict` hold **on effects**, which is sharp, not weak: the `sp` field is total while every S1–S7 axiom constrains it only at effects, so two products can differ off the effect interval and both satisfy the axioms — the unrestricted equality is false, not merely unproved. ★ The binary case (`instProd`, `restrictFst`/`restrictSnd`, `sp_componentwise`) is kept beside the `m`-fold one; it is where the argument was found. ★ Axioms on every declaration named here: `[propext, Classical.choice, Quot.sound]`. No `sorryAx`. Prior record: PARTIAL (componentwise identity `MasterTheorem.Central.central_decomposition`; summand inheritance of S1–S7 and the converse assembly open) — ★★ **DE-PRICED 2026-08-09**: `WallCertificates/abstract-tier.lean` recorded this row's blocker as "no direct sum of order unit spaces in the tree", and that claim is **FALSE** — `RadicalRelativity/DirectSum.lean:38` is `instance instProd : OrderUnitSpace (V × W)` with `prod_ousUnit` and `isEffect_prod_iff`, and that file's header calls it "the M7 foundation … the direct sum of order-unit spaces". The supporting grep was accurate; the inference was invalid, because the pattern needed both words on one line and the instance is `instProd` in a file named `DirectSum.lean`. ★★★ **AND THE ATTEMPT IMMEDIATELY FOUND MORE: the CONVERSE ASSEMBLY IS ALREADY BUILT.** `SequentialProductOn.prod` (`DirectSum.lean:84`) assembles a `SequentialProductOn (V × W)` from summand products — all eight fields — with `prod_sp`, `prod_fst`, `prod_snd` ("the restriction is literally a projection") and `sp_eq_of_prod_eq` (a split product determines its factors). Its own docstring reads "This is the *sufficiency* half of the omnibus's factorwise assembly". So the row's recorded gap was wrong twice over: not only did the carrier exist, the assembly did. **EIGHTH false absence claim.** ★★ **And this one was caught by applying, within minutes, the rule the certificate-refutation review had just handed over** — `grep -n "^def \|^theorem \|^instance \|^abbrev " <the file>` rather than grepping for a guessed name. The reviewer itself found `instProd` in that file and stopped there, missing `prod` twenty lines later; listing the declarations found it at once. **The rule validated itself on the first use.** **What actually remains of row 12: the RESTRICTION direction only** — that an *arbitrary* product on `V × W` restricts to S1–S7 products on each summand (grep for `restrict`/`toSummand`/`ofProd` in `DirectSum.lean` returns only prose, 2026-08-09). That is the genuinely open half; the converse is done | — |
| 13 | `prop:pseudo-transfer` | 719 | proposition | For an invertible effect $a$ with spectral inverse $a^{-1}$, $a\cdot a^{-1}=a^{-1}\cdot a=\id$ for the unknown product as well as the standard one, so $a$ is order preserving for both and vdW Prop. 5.3's hypotheses hold at every invertible effect. | FORMALIZED — ★★★ **2026-08-24, at EJA generality. Every assertion of `main.tex:719-733` is a theorem; the promotion is auditable clause by clause.** **(1)** `b·b⁻¹ = b⁻¹·b = 1` for the **unknown** product — `EJA/PseudoTransfer.lean`'s `sp_pseudoTransfer`, for *every* `P : SequentialProductOn J` with `FirstArgContinuous`. **(2)** The same for the **standard** product — `quadJ_jsqrt_jinv_cancel` (`Q_{√b}` and `Q_{√b⁻¹}` mutually inverse); the earlier `luders_jsqrt_jinv` is the unnormalised form. **(3)** `b` **order preserving for the unknown product** — *both* clauses of van de Wetering's Def. 4.17: order reflection with injectivity (`sp_orderReflection`, vdW 4.19) **and** lower-interval surjectivity (`sp_lowerIntervalSurj`). **(4)** The same for the standard product — `luders_reflectsNonneg` and `luders_lowerInterval_surj`. Assembled as the single theorem `prop_pseudoTransfer`. Consequently vdW Prop. 5.3's hypotheses hold at every invertible effect, which is the use the article makes of the row. ★★ **The "every invertible effect" quantifier is discharged, not assumed:** `lam_ne_zero_of_invertible` proves that a Jordan inverse forces every *present* eigenvalue nonzero (pair `cₖ` against `1`: `⟪cₖ,1⟫ = ⟪w, b∘cₖ⟫ = 0` while `⟪cₖ,1⟫ = ‖cₖ‖² > 0`), which is exactly the hypothesis `prop_pseudoTransfer` takes. Statements are resolution-relative — the same shape the standard half has always had here — and `spectral_resolution_complete` supplies the resolution. ★★★ **What this rests on, all proved 2026-08-23/24 and none of it previously in the tree:** the **fundamental formula** `U_{U_x y} = U_x U_y U_x` (`EJA/Fundamental.lean`, **not in Mathlib**), the crux identity `V_{x,y}U_x = U_x V_{y,x}`, the linearised Jordan identity, `exists_simultaneous_resolution` (+ converse), `quadJ_mul_of_opCommute` (`Q_{x∘y} = Q_x Q_y`, no positivity), `quadJ_isSoS` (**`Q_v` preserves the cone** — proved via a primitive-idempotent atom, `Q_v c = t•d` with `d` idempotent and `t ≥ 0`, after the FF-only routes were shown to cycle), and the abstract ports of the vdW 5.2 value law and the whole pseudo-inverse chain off `HermitianMat`. ★ Axioms on every declaration named here: `[propext, Classical.choice, Quot.sound]`. No `sorryAx`. Prior record: PARTIAL — ★★★ **ORDER REFLECTION LANDED 2026-08-24 AND THE ROW STILL DOES NOT MOVE. Checked, not assumed.** `OrthFamily.lean`'s `seqLeftMulAbs_reflectsNonneg` / `seqLeftMulAbs_injective` / `seqLeftMulAbs_pseudoInv_comp` port vdW 4.19 to abstract `OrderUnitSpace` generality, and `EJA/PseudoTransfer.lean`'s `sp_orderReflection` discharges every hypothesis at EJA generality by name. Conclusions are literal — order reflection and injectivity, not something weaker. ★★★ **Why the row is still PARTIAL: vdW's "order preserving" (Def. 4.17) has TWO clauses**, as row 8's cell states — (a) **lower-interval surjectivity**, `L_a` carries the effect interval *onto* the order interval below `a`, and (b) order reflection relative to the support. Only (b) is proved. **(a) appears nowhere in the tree** — `grep -rniE "lower.interval \|surject.*effect interval\|orderPreserving"` over `RadicalRelativity/` returns **0** (2026-08-24). Clause (iii) also says order preserving for **both** products, and what is proved is for the *unknown* one; the standard-product side should follow from `Q_{√a}` having positive inverse `Q_{√a⁻¹}` — reachable now that the fundamental formula gives `Q_a = Q_a Q_{a⁻¹} Q_a` via `quadJ_jinvOfResolution` — but it is **not proved**. And clause (iv), "vdW Prop. 5.3's hypotheses hold at every invertible effect", is not stated in Lean at all. ★ **Status of the row's three assertions:** `a·a⁻¹ = a⁻¹·a = 1` for the standard product — DONE (`luders_jsqrt_jinv`); for the unknown product — DONE (`sp_pseudoTransfer`); order preservation and the Prop. 5.3 consequence — **half of one clause of one of two products**. Promoting here would be the biggest-residue error this file records five times. Prior record: PARTIAL — ★★★ **BOTH HALVES OF THE IDENTITY ARE NOW PROVED AT EJA GENERALITY 2026-08-24; the row still does NOT move, and the reason is the third clause.** The row asserts three things: `a·a⁻¹ = a⁻¹·a = 1` **(i)** for the standard product, **(ii)** for the unknown product, and **(iii)** "so `a` is order preserving for both and vdW Prop. 5.3's hypotheses hold at every invertible effect". **(i) DONE** — `EJA/Spectral.lean`'s `luders_jsqrt_jinv`. **(ii) DONE** — `EJA/PseudoTransfer.lean`'s `sp_pseudoTransfer`: for a complete resolution of `b` with cone certificates `IsSoS b`, `IsSoS (1−b)` and `cᵢ ≠ 0 → λᵢ ≠ 0`, **every** `P : SequentialProductOn J` with `FirstArgContinuous` satisfies `P.spCone b⁻¹ b = 1 ∧ P.spConeRight b b⁻¹ = 1`. Every abstract hypothesis is discharged **by name** — sharpness by `isSharpOrderUnit_of_idem`, Archimedean by `isArchimedean_ofBilinear`, both eigenvalue guards by `nonneg_coeff_of_isSoS`. The general form is `OrthFamily.lean`'s `sp_pseudoInv_eq_smul_one` / `sp_pseudoInv_comm` / `sp_pseudoInv_cancel` / `spCone_specInv_both`, ported off `HermitianMat`. ★★★ **(iii) IS NOT PROVED and is the whole residue.** "Order preserving" is the `seqLeftMul` layer — `seqLeftMul_reflectsNonneg` and `seqLeftMul_injective` (vdW 4.19), which `Necessity/PseudoInverse.lean:26` calls "**the payoff**" — and it exists only over `HermitianMat`. Porting it needs an abstract `seqLeftMul` bundling first. **Promoting this row on (i)+(ii) would be the biggest-residue error this file records five times: naming the largest closed item and letting it stand for the whole statement.** ★ What the eigenvalue guards buy, recorded because it nearly dead-ended: the hypotheses are `∀ i, cᵢ ≠ 0 → …`, **not** universally quantified. A universal nonnegativity hypothesis is **unsatisfiable** from the cone — `nonneg_coeff_of_isSoS` speaks only at nonzero idempotents — so the unguarded shape would have failed at the last step. ★ Separately, S5 for the Lüders product landed the same night (`Class.lean`'s `quadJ_jsqrt_assoc_of_opCommute`), resting on `quadJ_mul_of_opCommute` (`Q_{x∘y} = Q_x Q_y` for operator-commuting `x,y`, **no positivity needed**) and `exists_simultaneous_resolution`. Clause status for the candidate product: **S1, S3, S4, S5 proved; S2, S6, S7 open.** S5 carries an *operator-commutation* hypothesis, not sequential-product compatibility — bridging those is row 10 and is neither assumed nor discharged. S7 does **not** fall out: it needs `⁅L_a, L_{Q_{√b}c}⁆ = 0` for an element outside the shared family, which is structure theory. Prior record: PARTIAL — ★★★ **THE STANDARD-PRODUCT HALF OF THIS ROW IS PROVED 2026-08-24, at EJA generality.** The row asserts `a·a⁻¹ = a⁻¹·a = 1` **for the unknown product as well as the standard one**. `EJA/Spectral.lean`'s `luders_jsqrt_jinv` gives the standard half: for the Lüders product `a·b = Q_{√a}b`, the coefficients of `a·a⁻¹` are `(√λᵢ)²·λᵢ⁻¹ = 1` and the sum is the completeness relation `∑ᵢcᵢ = e`. Supporting: `quadJ_of_resolution` (`Q` acts **coefficientwise** on a shared resolution, squaring the subscript's eigenvalues and leaving the argument's alone) and `quadJ_jinvOfResolution` (`Q_x(x⁻¹) = x`). ★★★ **What made this reachable was the fundamental formula, proved the same night.** `EJA/Fundamental.lean`'s `quadJ_quadJ_quadJ` — `U_{U_x y} = U_x U_y U_x` — is **not in Mathlib** (whose only Jordan file carries `IsJordan`/`IsCommJordan` and nothing else) and was not in this tree. It rests on `tripleJ_quadJ_comm` (`V_{x,y}U_x = U_x V_{y,x}`), and its first corollary `quadJ_sq` (`Q_{x²} = Q_x²`, the formula at `y = 1`) is what ties `Q_{√a}` back to `Q_a` — the link S5 needs and which did not exist before. ★ **What is still open, and the row does NOT move on the above:** the *unknown*-product half. It needs the vdW 5.2 value law (`Necessity.sp_orthFamily_value`) at abstract generality; that theorem exists only over `HermitianMat` and its orthogonality hypothesis is the **matrix** product `(pᵢ).mat * (pⱼ).mat = 0`, which is not a statement an `OrderUnitSpace` can make. Porting it (and `sum_smul_proj_isEffect`, `sp_comm_sum`, `sp_smul_of_mem_unitInterval`) to Jordan orthogonality is the whole remaining residue; `sp_sum_right` and `sp_smul_left` are already carrier-agnostic. Also still open: S2/S5/S6/S7 for the candidate product, of which S1/S3/S4 are proved. Prior record: PARTIAL — ★★★ **S4 PROVED 2026-08-23; three of the seven clauses now hold for the candidate product `a · b := quadJ (jsqrt a) b`.** `EJA/Class.lean`'s `quadJ_jsqrt_zero_symm` is symmetry of orthogonality: the asymmetric hypothesis routes through a symmetric one, because `Q_{√a}` is self-adjoint (`quadJ_inner_self_adjoint`) and sends `1` to `a` (`quadJ_jsqrt_one`), so `a·b = 0` yields `⟪b,a⟫ = 0`, and on the cone inner-product orthogonality **is** Jordan orthogonality (`mul_eq_zero_of_inner_eq_zero_of_isSoS`, with the per-idempotent strengthening `smul_resolution_mul_eq_zero_of_inner_eq_zero` supplying `√b ∘ a = 0` for the other term). ★ **Clause status:** S1 `quadJ_add` (free — `quadJ a` is a linear map); S3 `quadJ_unit_left` and `quadJ_jsqrt_one`; S4 `quadJ_jsqrt_zero_symm`. **S2, S5, S6, S7 are open**, and S5–S7 (compatible associativity, the two multiplicativity clauses) are the substantive Jordan identities — this cell does **not** price them as cheap. ★★ **Nothing in S4 was new mathematics**, which is worth recording as a pattern: every ingredient was already in the tree or a strengthening of something already there — the *vanishing* case of `inner_mul_self_nonneg_of_idem`'s positivity proof, the *per-idempotent* form of the orthogonality proof, the *observable-nonnegativity* form of the square-root identity (`jsqrt_mul_self'`, needed because a coefficient at a zero idempotent is unconstrained and `nonneg_coeff_of_isSoS` only speaks at `qᵢ ≠ 0`). The tree had the pieces; nobody had asked them these questions. Prior record: PARTIAL — ★★ **RESIDUE NARROWED 2026-08-23; the status word does not move.** Two of the three objects this row was waiting on now exist at EJA generality, both in `EJA/Spectral.lean`. (1) **The spectral inverse is built**: `mul_jinvOfResolution` proves `(∑ λᵢcᵢ) · (∑ λᵢ⁻¹cᵢ) = e` for a complete family with no vanishing eigenvalue — the `∑ μ⁻¹ P_μ` this cell named as the smaller object the residue had become. (2) **The evaluation identity is proved**: `jeval_of_resolution` gives `jeval x p = ∑ᵢ λᵢ·p(λᵢ) cᵢ`, which this cell recorded as absent. (3) ★★★ **And the square root is now a genuine function of the element** — `jsqrt`, with `jsqrt_eq_of_resolution` (it agrees with every admissible resolution) and `jsqrt_mul_self`. That closes the *canonicity* problem, which was the real blocker: `isSoS_iff_exists_sq` has exhibited *a* square root since 2026-08-22, but `a·b = Q_{√a}b` needs `√a` to be a **function** of `a`, and `spectral_resolution_complete` hands out an existential. The chain is `coeff_eq_zero_of_sum_smul_eq_zero` → `jeval_eq_zero_iff_of_resolution` → `exists_idem_iff_forall_jann_eval` → `nonzero_spectrum_eq_of_resolutions` → `idem_unique_of_nonzero_spectrum` → `sqrt_sum_eq_of_resolutions` → `jsqrt`, all sorry-free. `quadJ` (the **Jordan** quadratic representation `2L_a²−L_{a²}`, distinct from `Necessity.quadRep`'s matrix conjugation, which cannot transfer to `h₃(𝕆)`) is defined, with S1 and S3 discharged (`quadJ_add`, `quadJ_unit_left`). ★ **What still blocks the row:** S4–S7 for `a·b := quadJ (jsqrt a) b` — orthogonality symmetry, compatible associativity and the two multiplicativity clauses — plus lifting `jsqrt` across the `ringOfBilinear` crossing into `EJA/Order.lean`'s vocabulary so `0 ≤ x` supplies the nonnegative eigenvalues via `nonneg_coeff_of_isSoS`, plus the pseudo-inverse chain ported off `HermitianMat`. S4–S7 are substantive Jordan identities and are **not** cheap; an earlier note this session called the assembly 'ordinary', which was true for S1 and S3 only. Prior record: PARTIAL — ★★★ **STATUS UNCHANGED 2026-08-22, and this note exists so that it stays unchanged.** Gate (E1), named below as this row's residue, is now a **proved theorem** (`RadicalRelativity.EJA.spectral_resolution` and its bilinear-map transfer; `WallCertificate.gate_E1_spectral` is discharged). **The row does not move**, because its residue was never the spectral resolution: a spectral inverse is `∑ μ⁻¹ P_μ`, built **on** the resolution rather than being it. What changed is that the residue is now a named, smaller object rather than the whole spectral theorem. ★★★ **THE REASON THIS CELL GAVE WAS FALSE AND IS CORRECTED 2026-08-22; the conclusion above survives untouched.** It read "a **functional calculus on** the resolution, and no functional calculus exists at EJA generality (`grep` of the EJA declaration list for `calculus\|cfc\|sqrt\|inverse` → 0 hits, 2026-08-22)". **The grep is accurate and the inference from it is not.** ★ (The alternation is written with escaped separators here; unescaped, it splits this row into ten table cells and breaks the census script three sections above — which is what it had been doing since the cell was written, and is the seventh instance of the defect that section already names.) A polynomial functional calculus at EJA generality is declared at `RadicalRelativity/EJA/Spectral.lean:98` — `jeval (x : J) : Polynomial ℝ →ₗ[ℝ] J`, with multiplicativity as `jeval_mul` (`:128`) — and its name contains none of the four search strings. Moreover a spectral inverse over a *finite* spectrum is itself a polynomial value (interpolate `λ ↦ λ⁻²`), so no general calculus is needed for it. ★ **What is genuinely absent, and is the row's true residue at EJA generality:** the evaluation identity `jeval x p = ∑ᵢ λᵢ p(λᵢ) qᵢ` on a resolution is not in the tree (no `jpow`-of-a-diagonal-family lemma exists; `EJA/Frame.lean` and `EJA/Orthogonal.lean` declaration lists checked 2026-08-22), and no declaration produces an inverse. ★★ **And there is a constraint under that one which no gate touches**: this row quantifies over the unknown sequential product, `SequentialProductOn` is declared `(V : Type*) [OrderUnitSpace V]` (`SequentialProduct.lean:244`), and `RadicalRelativity/EJA/` contained **zero** occurrences of `OrderUnitSpace` and **zero** of `IsEffect` (per-file measurement across all 15 files, 2026-08-22) — so the row was not statable at EJA generality at all. See `EJA-DIVIDEND.md`'s 2026-08-22 retraction. ★★★ **THE PREDICTED FALSIFICATION HAPPENED, THE SAME EVENING, AND IS RECORDED HERE RATHER THAN QUIETLY OVERWRITTEN.** The sentence above forecast its own death ("that measurement has a shelf life and is being falsified on purpose … re-run the count before quoting it"), and `RadicalRelativity/EJA/Order.lean` landed. **Re-measured 2026-08-22, same command, same scope, now 16 files: `OrderUnitSpace` on 19 lines / 20 occurrences and `IsEffect` on 4 lines / 4 occurrences, every one of them in `Order.lean` and none in the other 15.** So "not statable at EJA generality at all" is dead: `EJA.orderUnitSpaceOfBilinear` (`EJA/Order.lean:252`) builds the instance and `EJA.SequentialProductOnEJA` (`:327`) is the type of an S1/S3–S7 product on a Euclidean Jordan algebra, which could not previously be *written down*. ★★ **The status word does not move, and the reason is the new file's own**: `EJA/Order.lean:325` says "**Statability is the entire claim.** No inhabitant is constructed here and none is claimed; rows 13 and 16 need an inhabitant *and* the ports named in the module docstring." This row's residue is unchanged and is stated two sentences above — the spectral **inverse** from a resolution, plus the `jeval x p = ∑ᵢ λᵢ p(λᵢ) qᵢ` evaluation identity. What the new file removed is the *statability* obstruction stacked underneath that residue, not the residue. ★ **The forecast is worth more than the number was**: an absence measurement that names the thing that will falsify it converts a decayed claim into a dated one, and this is the first time on this project that the falsification arrived while the warning was still legible. ★ **Standing rule, a further time: an accurate grep is evidence about a string, not evidence of absence.** Prior record: **terminal state EJA-GATED, 2026-08-10 (ARC-8 block 8.6)** — certificate `WallCertificates/eja-gated.lean`: every non-EJA clause is closed in-tree (itemized there per row), and the article-generality residue is **gate (E1)** stated in Lean with `sorry` exactly at the axiomatization gap. Prior record: PARTIAL (concrete carrier, in the *normalized* form `Necessity.sp_pseudoInv_eq_smul_one`/`sp_pseudoInv_cancel`: Lean's `pseudoInv` is rescaled into the effects, so the identity carries `pseudoInvCoef b •` where the article has $\id$ — the article's form needs `lem:cone-ext` to divide it out) — ★★ **and that division is now DONE (2026-08-09, ARC-7 block 7.3): `Necessity.spCone_specInv_eq_one` proves the article's literal `a⁻¹ · a = 𝟙`**, with the *true* spectral inverse `specInv b = ∑ μ⁻¹ P_μ` (not an effect — its eigenvalues are ≥ 1) and **no coefficient**, via the cone extension `spCone` at the admissible normalization `1/c`. This is precisely what `lem:cone-ext` was built for in ARC-6, and it needed one further thing that did not exist until today: **`OrderUnitSpace.IsArchimedean` for the concrete carrier** (`HermitianMat.isArchimedean`), so the Archimedean input is *discharged, not assumed*. ★★★ **CORRECTED BY AN INDEPENDENT REVIEW (2026-08-09): WHAT LANDED IS FIDELITY, NOT REACH — and this row has THREE clauses, where my own brief described one.** Read at the pinned `main.tex:720`, `prop:pseudo-transfer` asserts (i) $a\cdot a^{-1}=a^{-1}\cdot a=\id$ for the unknown product as well as the standard one; (ii) consequently $a$ is order preserving for both; (iii) **vdW Prop. 5.3's hypotheses hold at every invertible effect** — which is the payload the proposition exists to deliver. ★ **(ii) and (iii) were already in the tree by a different route**, through the *normalized* `pseudoInv`: `seqLeftMul_reflectsNonneg` / `seqLeftMul_injective` (vdW 4.19) and `Theta.lean`'s `theta_one` + order-iso (vdW 5.3) — which works because a positive scalar affects neither order-reflection nor injectivity. So `specInv`/`spConeRight` buy the article's **literal coefficient-free form of clause (i)**, a fidelity upgrade; they do not extend reach. Do not read the two docstrings as reach. ★ **Carrier scope, which is now the whole residual**: the extension machinery (`IsConeNorm`, `spCone`, `spConeRight`, both independence lemmas) is at **abstract order-unit generality**; only the `specInv` layer is concrete, because it uses the spectral resolution. So these theorems live on $H_n(\mathbb{k})$ for `RCLike 𝕜` — more general than $H_n(\mathbb{C})$ alone, less general than an EJA. ★ `specInv` is confirmed to be `b.cfc (1/\cdot)`, i.e. the spectral inverse in the functional-calculus sense. **Hygiene warning now in its docstring:** the def is total and `1/0 = 0`, so at a *singular* `b` it is the Moore–Penrose pseudo-inverse and `b\cdot\text{specInv}\,b = \id - P_0`; every theorem carries `PosDef`, but the *name* invites dropping it. ★★ **BOTH SLOTS NOW HOLD (2026-08-09, later the same day).** The scope note that stood here said the companion `a · a⁻¹ = 𝟙` "puts the non-effect in the *second* slot, which nothing in the tree covers" — true of the *object*, and **wrong about the cost**. The certificate-refutation review discharged the second-argument extension in ~25 lines: its only ingredient, `sp_smul_right_of_unitInterval`, was already in the tree, and it needs `IsArchimedean` but **no S2 at all**. ★★ **Scope correction (independent review, same day), and it matters because the looser form went into the ledger as a general fact about the axioms:** what is established is that the right-slot extension is *derivable without* S2 and that the tree's *route* to the left slot uses S2. That is **not** an independence result — "the left slot needs S2" would require a counterexample (an S1,S3–S7 + Archimedean product where left-slot homogeneity fails), and none is banked. Say "proved without S2", never "needs". ★ And `spConeRight_specInv_eq_one` **itself does consume S2**, via `sp_pseudoInv_comm`: the no-S2 property belongs to the extension, not to the identity. Landed as `SequentialProductOn.spConeRight` (+ `sp_coneNorm_indep_right`, `spConeRight_eq`, `spConeRight_of_isEffect`), and `Necessity.spConeRight_specInv_eq_one` proves `a · a⁻¹ = 𝟙`; `spCone_specInv_both` states the article's identity as it is written. The row stays PARTIAL for **EJA generality alone** now. The other open part is unchanged: EJA generality | 5.1 |
| 14 | `prop:theta` | 759 | proposition | For every invertible effect $a$ there is a Jordan automorphism $\Theta_a$ with $a\cdot b=Q_{\sqrt a}\Theta_a(b)$; $\Theta_a$ fixes everything operator-commuting with $a$, and $\Theta$ is multiplicative on operator-commuting invertibles. | FORMALIZED — ★★★ **2026-08-24. All three clauses proved at abstract EJA generality, for any S1–S7+S2 product** (`EJA/ThetaAbstract.lean`). Against `main.tex:763-773`: **(i) existence and the defining identity** — `exists_theta`: for an invertible effect `b` there is a **Jordan automorphism** `Θ` of `J` with `Θ 1 = 1` and `L_b = Q_{√b} ∘ Θ`, i.e. `b · y = Q_{√b}(Θ y)`. **(ii) fixing** — `theta_fixes_of_opCommute`: `Θ_b` fixes every effect operator-commuting with `b`. **(iii) multiplicativity** — `theta_mul` gives `Θ_{b•b'} = Θ_b Θ_{b'}` and `theta_comm` gives `Θ_b Θ_{b'} = Θ_{b'} Θ_b`, for operator-commuting invertible effects. ★★★ **Why it became reachable, all of it landing the same day.** `Θ := Q_{√b}⁻¹ ∘ L_b` is a linear *equivalence* because row 13's `sp_orderReflection` makes `L_b` injective, hence bijective in finite dimension, and `quadJ_jsqrt_jinv_cancel` inverts `Q_{√b}`; it is **unital** (`L_b 𝟙 = b = Q_{√b}1`) and an **order** isomorphism (`L_b` preserves *and reflects* the cone; `Q_{√b}`, `Q_{√b⁻¹}` preserve it); and `EJA/OrderAuto.lean`'s `map_jordan_of_orderIso` — the Koecher / Alfsen–Shultz theorem this cell had found **proved and consumed by nothing** — upgrades that to a Jordan automorphism. `isSoS_jmulₗ_iff_mulLₗ` was needed to feed it: the class's cone vocabulary and `OrderAuto`'s describe the same cone in different terms. ★★ **Clause (ii) runs on `sp_eq_quadJ_of_opCommute` — vdW Prop. 5.2 in usable form**: on an operator-commuting pair of effects *every* S1–S7+S2 product already takes the standard value `b · y = Q_{√b} y`, proved by matching `sp_orthFamily_value` against `luders_of_resolution` on a simultaneous resolution with coefficients trimmed to `[0,1]` at absent idempotents. ★ Clause (ii) is **vdW Prop. 5.5**, and the same theorem closed manifest **row 16**'s entire residue. ★★ **Clause (iii)'s two halves are proved separately, deliberately.** `theta_mul` cancels `Q_{√(b•b')} = Q_{√b}Q_{√b'}` after moving `Θ_b` past `Q_{√b'}` (legal because `Θ_b` is a Jordan automorphism fixing `√b'`); `theta_comm` is **not** obtained by exchanging `b` and `b'` in it, because that exchange would have to transport the effect proof for `b•b'` along `b•b' = b'•b` — the symmetric computation avoids the dependent transport, and both composites are carried onto `L_bL_{b'}` and `L_{b'}L_b`, which agree because S5 makes each `L_{b•b'}`. ★ Statements are resolution-relative, exactly as row 13's are. Axioms on every declaration named here: `[propext, Classical.choice, Quot.sound]`. No `sorryAx`. Prior record: PARTIAL — ★★★ **CLAUSE (a), THE FIXING PROPERTY, IS ALSO PROVED, 2026-08-24** (`EJA/ThetaAbstract.lean`'s `theta_fixes_of_opCommute`): `Θ_b` fixes every effect that operator-commutes with `b`. That is **van de Wetering's Proposition 5.5** (`main.tex:791`, and the row's own "Θ_a fixes every b that operator-commutes with a"). ★ It runs on a new theorem worth naming separately — `sp_eq_quadJ_of_opCommute`, **vdW Prop. 5.2 in usable form**: on an operator-commuting pair of effects *every* S1–S7+S2 product already takes the standard Lüders value `b · y = Q_{√b} y`, proved by matching `OrthFamily.sp_orthFamily_value` against `luders_of_resolution` on a simultaneous resolution, with the coefficients trimmed to `[0,1]` at absent idempotents. Then `Q_{√b}(Θ y) = Q_{√b} y` and `Q_{√b}` is injective. ★★ **Residue is now clause (b) alone**: `Θ_{b•b'} = Θ_b Θ_{b'} = Θ_{b'} Θ_b` for operator-commuting invertibles. ★ Note it is statable without a global choice of `Θ`, because the defining identity `L_b = Q_{√b} ∘ Θ` determines `Θ` uniquely. Prior record: PARTIAL — ★★★ **THE EXISTENCE CLAUSE IS PROVED AT ABSTRACT EJA GENERALITY, 2026-08-24** (`EJA/ThetaAbstract.lean`'s `exists_theta`), which is the item this cell recorded as "the whole residue": *the abstract-generality derivation, now that its Koecher input is available by name, is not written*. It is written. For an invertible effect `b` and any S1–S7+S2 product there is a **Jordan automorphism** `Θ` of `J` with `Θ 1 = 1` and `L_b = Q_{√b} ∘ Θ`, i.e. `b · y = Q_{√b}(Θ y)` — `main.tex:766-770` on the nose. ★★ **Why it became available.** `Θ := Q_{√b}⁻¹ ∘ L_b` is a linear *equivalence* because row 13's `sp_orderReflection` makes `L_b` injective, hence bijective in finite dimension, and `quadJ_jsqrt_jinv_cancel` inverts `Q_{√b}` at an invertible element (`quadJSqrtEquiv`); it is **unital** because `L_b 𝟙 = b = Q_{√b} 1`; it is an **order** isomorphism because `L_b` both preserves and reflects the cone while `Q_{√b}` and `Q_{√b⁻¹}` preserve it; and `EJA/OrderAuto.lean`'s `map_jordan_of_orderIso` — the Koecher / Alfsen–Shultz theorem this cell found proved-and-unused — upgrades a unital linear order isomorphism to a Jordan automorphism. ★ `isSoS_jmulₗ_iff_mulLₗ` was needed to feed it: the class's cone vocabulary and `OrderAuto`'s are the same cone but not the same term. ★★ **What is still open, and it is now two named clauses rather than a whole derivation:** **(a)** `Θ_b` fixes every `y` that operator-commutes with `b` — this is the vdW 5.2 value law (`OrthFamily.sp_orthFamily_value`) saying `b · y = Q_{√b} y` for compatible pairs, so `Θ_b y = Q_{√b}⁻¹(Q_{√b} y) = y`; the inputs exist and the assembly is not written. **(b)** `Θ_{b•b'} = Θ_b Θ_{b'} = Θ_{b'} Θ_b` for operator-commuting invertibles. ★ Statements are resolution-relative, exactly as row 13's are. Axioms: `[propext, Classical.choice, Quot.sound]`, no `sorryAx`. Prior record: PARTIAL — ★★★ **THE ITEM THIS CELL CALLED OPEN IS PROVED IN THE TREE, AND HAS BEEN ALL ALONG.** The cell read: "What is open is the classical unital-linear-order-isomorphism theorem (Koecher / Alfsen–Shultz 2.80) at f.d. EJA generality." That theorem is `EJA/OrderAuto.lean`'s `map_jordan_of_orderIso` — a unital linear order isomorphism of a finite-dimensional formally real Jordan algebra is a Jordan homomorphism — stated at `[NonUnitalNonAssocCommRing] [IsCommJordan] [Module ℝ] [IsScalarTower] [IsFormallyReal] [Module.Finite ℝ]`, which **is** f.d. EJA generality. It is sorry-free and **nothing in the tree consumes it** (grep, 2026-08-23): proved, and never connected. ★★ The EXTERNAL tag is separately dead as of the 2026-08-23 scope reversal recorded at the head of this file, but that is not why this line changes — the item was not external, it was done. ★ **What is actually still open**, and the row stays PARTIAL for it: the row's own content is `Θ_a` for invertible effects — existence with `a·b = Q_{√a}Θ_a(b)`, fixing of everything operator-commuting with `a`, and multiplicativity — which this cell records as derived **on both concrete carriers** from in-tree Kadison rigidity. The abstract-generality derivation, now that its Koecher input is available by name, is not written. That is the whole residue. Prior record: PARTIAL (derived on both concrete carriers from in-tree Kadison rigidity; the abstract-generality step open) — ★★★ **"vIR's JB-algebra generality open" CORRECTED 2026-08-22**: the article does not ask for vIR's generality here. Its proof derives linearity from van de Wetering Prop 5.3 *before* invoking vIR, and names Alfsen–Shultz as the classical reference for exactly that step (`main.tex:775-789`); the Lean interface makes the same commitment, `ComparisonSetup.Θ : J → (J ≃ₗ[ℝ] J)` being linear by **type** (`MasterTheorem/Interface.lean:264`). What is open is the classical unital-linear-order-isomorphism theorem (Koecher / Alfsen–Shultz 2.80) at f.d. EJA generality. **EXTERNAL by pre-registration, unchanged — this corrects the reason, not the decision.** | — |
| 15 | `lem:frame-fix` | 908 | lemma | $\Theta_r$ fixes each frame atom and the diagonal pointwise, preserves each Peirce block, and lies in $\Stab(F)^\circ$; hence $L_{a(r)}$ is Peirce-block-diagonal. | FORMALIZED — ★★★ **2026-08-24. All five clauses proved at abstract EJA generality, for any S1–S7+S2 product** (`EJA/ThetaAbstract.lean`). **(1) `Θ_r` fixes each frame atom** — `theta_fixes_frame_atom`: `pᵢ` is an effect that operator-commutes with `a(r) = ∑ e^{r_k}pₖ` (both resolved by the frame family), so vdW Prop. 5.5 applies. **(2) and the diagonal pointwise** — `jordanAut_fixes_frameDiag`, by linearity. **(3) preserves each Peirce block** — `jordanAut_maps_frameBlock`. **(4) and lies in `Stab(F)°`** — `theta_mem_stabFrame_connectedComponent`. **(5) hence `L_{a(r)}` is Peirce-block-diagonal** — `seqLeftMul_mapsTo_frameBlock`, via `quadJ_frameDiag_block` (`Q` of a frame-diagonal element acts on `V_{ij}` by `√fᵢ√fⱼ`). ★★★ **Clause (4) was the long-standing obstruction — "the stabilizer as a group with an identity component, vocabulary two other certificates already record as absent" — and it is now built, not assumed.** `stabFrame` realises `Stab(F)` inside the operator space `J →L[ℝ] J`, so it carries a topology; the path `t ↦ Θ_{(t clamped to [0,1])·r}` runs inside it from the identity to `Θ_r`; its range is connected, so both endpoints share a component. ★★★ **What made the path continuous, and it is the reusable part:** `Θ` had to become a *function* (`thetaOf`, with `thetaOf_spec`); on the twist family it has the **closed form** `Θ_r = Q_{√a(−r)} ∘ L_{a(r)}` (`thetaOf_twistElt_apply`) with no `Classical.choose` in it, because `jinvOfResolution` of `k ↦ e^{r_k}` is `a(−r)` on the nose; and both factors are **operator**-continuous — `continuous_toCLM_quadJ`, and `continuousOn_toCLM_seqLeftMulAbs`, which is **paper S2 upgraded from pointwise to operator form**. That upgrade needed no basis of effects to be constructed: continuity of `x ↦ L_{g x}v` holds at effects by S2, is stable under linear combinations because `L` is linear in `v`, and the effects span. ★ The 2026-08-10 EJA-GATED withdrawal is now moot in both directions: every clause it named is proved. Axioms on every declaration named here: `[propext, Classical.choice, Quot.sound]`. No `sorryAx`. Prior record: PARTIAL — ★★★ **THE ANALYTIC GATE IS NOW MOSTLY BUILT, 2026-08-24.** Landed today, in order: `exists_effect_basis` and `continuous_toCLM_of_basis` (`Normality.lean`); `continuous_toCLM_quadJ` and **`continuousOn_toCLM_seqLeftMulAbs`** (`EJA/ConeTopology.lean`) — **paper S2 upgraded from pointwise to operator continuity**, with no basis of effects actually constructed: continuity of `x ↦ L_{g x} v` holds at effects by S2, is stable under linear combinations because `L` is linear in `v`, and the effects span; and `stabFrame` with `theta_mem_stabFrame` (`EJA/ThetaAbstract.lean`) — **`Stab(F)` named as a subset of the operator space `J →L[ℝ] J`, so that it carries the topology `Stab(F)°` needs, with `Θ_r` proved to lie in it.** ★★ **What is left for `Θ_r ∈ Stab(F)°` is one construction, no longer any missing idea**: the path `t ↦ Θ_{t·r}` from `id` (`thetaOf_one`) to `Θ_r`, whose continuity is now assembled from `continuousOn_toCLM_seqLeftMulAbs`, `continuous_toCLM_quadJ`, and the explicit frame formulae `√a(r) = ∑ e^{r_k/2}p_k` and `Q_{√a(r)}⁻¹ = Q_{√a(−r)}` (the latter because `jinvOfResolution` of `k ↦ e^{r_k}` is `a(−r)` on the nose). Prior record: PARTIAL — ★★ **TWO MORE PIECES OF THE SHARED ANALYTIC GATE LANDED 2026-08-24** (`Normality.lean`): `exists_effect_basis` — a finite-dimensional order-unit space has a **basis consisting of effects**, off `span_isEffect_eq_top`; and `continuous_toCLM_of_basis` — a parametrised family of linear endomorphisms of a finite-dimensional normed space is continuous as an *operator*-valued map as soon as its values on one basis are, because reconstruction from basis values is a linear map out of a finite-dimensional space. ★★★ **Together these are exactly what turns paper S2 into operator continuity**: S2 gives continuity of `a ↦ a · b` for each fixed *effect* `b`, and a basis of effects is where that applies. With `thetaOf`, `thetaOf_one` and the explicit frame formulae `√a(r) = ∑ e^{r_k/2}p_k` and `Q_{√a(r)}⁻¹ = Q_{√a(−r)}`, the only step still missing for `Θ_r ∈ Stab(F)°` is assembling the path `t ↦ Θ_{tr}` and naming `Stab(F)` with its topology. Prior record: PARTIAL — ★★ **`Θ` IS NOW A DEFINITION, 2026-08-24** — `EJA.thetaOf` (`Q_{√b}⁻¹ ∘ L_b`) with `thetaOf_spec` re-deriving unitality, the Jordan-morphism property and the defining identity, plus `thetaOf_one` giving `Θ = id` at the unit. Until now `Θ` existed only existentially (`exists_theta`), which sufficed for every algebraic clause but not for the analytic ones: continuity of `r ↦ Θ_r`, the identity component, and `dχ` all need `Θ` to be a **function of its data**. That prerequisite is now in place, and `thetaOf_one` is the base point the path argument starts from. Prior record: PARTIAL — ★★★ **FOUR OF THE FIVE CLAUSES PROVED AT ABSTRACT EJA GENERALITY, 2026-08-24** (`EJA/ThetaAbstract.lean`), for any S1–S7+S2 product. **(1) `Θ_r` fixes each frame atom** — `theta_fixes_frame_atom`: `pᵢ` is an effect and operator-commutes with `a(r) = ∑ fₖ•pₖ` (both are resolved by the frame family), so the fixing property (vdW Prop. 5.5, proved for row 14) applies directly. **(2) and the diagonal pointwise** — `jordanAut_fixes_frameDiag`, by linearity. **(3) preserves each Peirce block** — `jordanAut_maps_frameBlock`: a Jordan automorphism fixing every atom carries `V_{ij}` into itself, since `Θ(pᵢ∘x) = pᵢ∘Θx`. **(5) hence `L_{a(r)}` is Peirce-block-diagonal** — `seqLeftMul_mapsTo_frameBlock`, because `L_{a(r)} = Q_{√a}∘Θ_r` and `quadJ_frameDiag_block` shows `Q` of a frame-diagonal element acts on `V_{ij}` by the scalar `√fᵢ√fⱼ`. ★★★ **The open clause is (4), `Θ_r ∈ Stab(F)°`, and the obstruction is exactly what this cell always said it was**: the stabilizer as a *group with an identity component*. That is vocabulary, not a missing proof — `Stab(F)` is easy to define, but its identity component needs a topology on the automorphism group and, to place `Θ_r` in it, a path from the identity, i.e. continuity of `t ↦ Θ_{a(tr)}`. **That is row 17's analytic machinery**, so clauses (4) here and row 17 are one piece of work, not two. Recording the entanglement rather than pricing them separately. ★ The 2026-08-10 EJA-GATED withdrawal stands and is now moot in the other direction: every non-EJA clause it named as blocking is proved above, and what is left is the single stabilizer clause. ★ Axioms: `[propext, Classical.choice, Quot.sound]`, no `sorryAx`. Prior record: PARTIAL — ★★★ **EJA-GATED CLAIM WITHDRAWN 2026-08-10, the same day it was made**, by the certificate-refutation review. EJA-GATED requires that *every non-EJA clause* be closed in-tree first, and this row's residue includes the article's `\Stab(F)^\circ` clause, which needs the stabilizer as a group with an identity component — vocabulary two other certificates already record as absent, and NOT the axiomatization. ★★ **The error was one error made three times (rows 5, 6, 15): I classified each row by its BIGGEST residue and let that stand for its WHOLE residue.** EJA-GATED is a claim about the complement — that nothing else remains — and a claim about a complement cannot be checked by inspecting the largest item in it. Same failure as row 35's "and that is the whole residue". The row is **WALL-CERTIFIED with this arc's attack evidence** instead (`WallCertificates/eja-gated.lean`'s WITHDRAWAL block names the non-EJA clause; the gate statements there remain the honest pricing of the *EJA part* of the residue). Prior record: **EJA-GATED 2026-08-10 (ARC-8 block 8.6)** — certificate `WallCertificates/eja-gated.lean`: every non-EJA clause is closed in-tree (itemized there per row), and the article-generality residue is **gate (E2)** stated in Lean with `sorry` exactly at the axiomatization gap. Prior record: PARTIAL (certificate for the produced setup, `MasterTheorem/Master.lean`; general statement open) | 5.1 |
| 16 | `lem:coalescence` | 940 | lemma | If $r_i=r_j$ then $\Theta_r$ fixes $V_{ij}$ pointwise; more generally an invertible $a=\lambda q+a_0$ scalar on $J_2(q)$ with no Peirce 1-part has $\Theta_a\|_{J_2(q)}=\mathrm{id}$. | FORMALIZED — ★★★ **2026-08-24. BOTH clauses proved at abstract EJA generality, for any S1–S7+S2 product** (`EJA/ThetaAbstract.lean`). **Second clause ("more generally")** — `theta_id_on_peirceTwo_all`: an invertible effect `a = μ•q + a₀` scalar on the Peirce 2-space of an idempotent `q` and with no Peirce 1-part (`q ∘ a₀ = 0`) has `Θ_a` acting as the identity on **all** of `J₂(q)`. **First clause** — `theta_id_on_frameBlock`: for a Jordan frame and `a = ∑ f k • pₖ` with `f i = f j`, `Θ_a` fixes the Peirce block `V_{ij}` pointwise. It is the second clause at `q = pᵢ + pⱼ`: coalescing the two parameters is exactly what makes `a` scalar on `J₂(q)` with no `1`-part, and `V_{ij} ⊆ J₂(q)` because an element halved by both `pᵢ` and `pⱼ` is fixed by their sum. `0 < f k ≤ 1` is the article's `r ∈ (−∞,0]ⁿ` under `f k = e^{r_k}`. ★★★ **The residue this cell had isolated was vdW Prop. 5.5, and it is now a theorem** — `theta_fixes_of_opCommute`, `Θ_b` fixes every effect operator-commuting with `b`. `main.tex:856` indexes Prop. 5.5 as "commutant fixing", which is what identified it with row 14's fixing clause; **one theorem closed both**. It runs on `sp_eq_quadJ_of_opCommute` — **vdW Prop. 5.2**: on an operator-commuting pair of effects every S1–S7+S2 product already takes the standard value `b · y = Q_{√b} y` — and `Orthogonal.opCommute_scalarOn`, whose own docstring already named itself "the cited Faraut–Korányi hypothesis that lem:coalescence (manifest row 16) rests on". ★★ **The step that was not free, and was nearly missed:** `Θ` is linear and the fixing theorem is stated on **effects**, but the elements of `V_{ij}` are not effects. `exists_effect_decomp_peirceTwo` supplies the bridge — every element of `J₂(q)` is a real combination of two effects *lying in `J₂(q)`* — because `Q_q` fixes `J₂(q)` pointwise, sends `1` to `q`, and preserves the cone, so it pushes an ambient Archimedean bound `−x ≤ t•1` into the block as `x + t•q ≥ 0`. No Peirce-subalgebra order theory is needed. ★★ **Both blockers this row carried are gone**: the `SequentialProductOnEJA` inhabitant (`ludersSequentialProduct`, built the same day) and the effect-level Prop. 5.5. The row's EJA-GATED label and its `Θ_fix` framing are both retired by proof. ★ Axioms on every declaration named here: `[propext, Classical.choice, Quot.sound]`. No `sorryAx`. Prior record: PARTIAL — ★★★ **THE "MORE GENERALLY" CLAUSE IS PROVED, 2026-08-24** (`EJA/ThetaAbstract.lean`'s `theta_id_on_peirceTwo`): an invertible effect `a = μ•q + a₀` that is scalar on the Peirce 2-space of an idempotent `q` and has no Peirce 1-part (`q ∘ a₀ = 0`) has `Θ_a` acting as the identity on `J₂(q)` — at abstract EJA generality, for any S1–S7+S2 product. ★★ **This retires the residue the cell had isolated.** That residue was "the effect-level vdW Prop. 5.5 itself", and Prop. 5.5 is the *commutant-fixing* property (`main.tex:856` lists it as exactly that); it is now `theta_fixes_of_opCommute`, proved via `sp_eq_quadJ_of_opCommute` (vdW Prop. 5.2 on an operator-commuting pair). The hypothesis shape is fed by `Orthogonal.opCommute_scalarOn`, whose own docstring already said it was "the cited Faraut–Korányi hypothesis that lem:coalescence (manifest row 16) rests on". ★★ **What remains is the row's FIRST sentence**, `rᵢ = rⱼ ⟹ Θ_r fixes V_{ij} pointwise`. It is the specialization of the proved clause to a Jordan frame with `q = pᵢ + pⱼ`, and the one genuine gap is that elements of `V_{ij}` are **not effects** — the fixing theorem is stated on effects, so reaching all of `J₂(q)` needs *the effects of the Peirce subalgebra `J₂(q)` to span it*, i.e. `EJA/PeirceSubalgebra.lean`'s EJA structure together with `span_isEffect_eq_top`. Stated rather than glossed, because `Θ` is linear and it would be easy to assume the span step is free. Prior record: PARTIAL — ★★★ **THE `SequentialProductOnEJA` INHABITANT IS BUILT, 2026-08-24** (`EJA/ConeTopology.lean`): `EJA.ludersSequentialProduct` is a genuine `SequentialProductOn J` for `a·b = Q_{√a}b` on any finite-dimensional Euclidean Jordan algebra — **all eight fields, S1 and S3–S7** — with **S2** as `ludersSequentialProduct_firstArgContinuous`. So this row's shared blocker is gone. The chain: S2 off compactness of the effect interval and continuity of the Jordan square root; S5's own hypothesis, S6a, S6b and S7 off `prop:bridge` (row 10, FORMALIZED the same day), with S7 additionally needing `opCommute_mul` — the operator-commutant of any element is a subalgebra. ★ **What remains on this row is therefore the effect-level vdW Prop. 5.5 alone**, which the previous cell had already isolated as the residue of `Θ_fix` once row 5 closed the span half. Prior record: PARTIAL — ★★★ **S2 CLOSED 2026-08-24** (`EJA/ConeTopology.lean`): `luders_continuousOn_isEffect` proves `a ↦ Q_{√a}b` continuous on the effects, i.e. **paper S2 for the Lüders product at EJA generality**. The chain built for it, none of which the tree had: self-duality of the Jordan cone (`isSoS_iff_forall_inner_nonneg`), hence the cone is **closed** and the effect interval **compact** (`isCompact_effectSet`); **uniqueness of the positive square root** (`jsqrt_mul_self_of_isSoS`, `eq_of_mul_self_eq_of_isSoS`) — `jsqrt` was known to *be* a square root, never to be *the* one; continuity of the Jordan product (`continuous_jmul`); and then `continuousOn_jsqrt`, because squaring is a continuous bijection of a compact effect interval onto itself and such a map is a homeomorphism. ★ So the inhabitant now has **S1, S2, S3, S4** outright and S5 under operator commutation. **What remains is S6, S7 and S5's own hypothesis, and all three reduce to one thing: row 10's bridge** (`a·b = b·a ⟺ ⁅L_a,L_b⁆ = 0`). Given the bridge, S6a and S6b are immediate (`T_{1-b} = id - T_b`, `T_{b+c} = T_b + T_c`) and S7 needs the operator-commutant of `a` — the Peirce-diagonal subalgebra of `a`'s spectral idempotents — to be closed under the product. ★★ **The bridge's converse is not a formality**: on the matrix carrier it is exactly "a normal product of two positive operators is self-adjoint", proved there by "the product of two PSDs has nonnegative real spectrum, and a normal operator with nonnegative real spectrum is positive". That spectral argument has no direct EJA transcription, and inventing one is the open problem. Prior record: PARTIAL — ★★★ **THE SPAN HALF OF `Θ_fix` CLOSED 2026-08-23; the status word does not move.** This cell established that what this row actually carries is `Θ_fix`, and that `Θ_fix` is vdW Prop 5.5 **in span-extended form** — Prop 5.5 at the effect level *plus* "effects span `J`", the latter being `lem:span`, **manifest row 5**. Row 5 reached FORMALIZED on 2026-08-23 (both its ball and Peirce clauses closed), so the span half of the extension is no longer an open interior debt. ★ **What remains of `Θ_fix` is the effect-level Prop 5.5 itself, which is a vdW citation** — and that is interior work nobody has priced, not one of the six pre-registered external rows. ★ Separately, the row's other dependency — the `SequentialProductOnEJA` inhabitant it shares with row 13 — advanced substantially: `jsqrt` is now a genuine function of the element and `quadJ` is defined, with S1 and S3 discharged. S4–S7 remain and are not cheap. See row 13's cell for the full chain. Prior record: PARTIAL — **terminal state EJA-GATED, 2026-08-10 (ARC-8 block 8.6)** — certificate `WallCertificates/eja-gated.lean`. ★★★ **THE RESIDUE RECORDED HERE WAS WRONG AND IS RESTATED 2026-08-22. The status word does not move; nothing below promotes or demotes this row.** This cell said "the article-generality residue is **gate (E2)+(E3)**", later amended in the certificate to "(E3) ALONE". **Both are false.** (a) **Neither clause of this row consumes `Theta_jordan`, which is what (E3) is.** `coalescence_J2q`'s entire proof term is `C.Θ_fix a ha b (C.simDiag_opCommute i j a b hsc hb)` (`MasterTheorem/Coalescence.lean:140-142`), and `coalescence_block` is its `r i = r j` specialization (`:148-151`). The article agrees: `lem:coalescence`'s proof at `main.tex:963-964` invokes `prop:theta`'s **fixing** clause and then `lem:span`, and never its Jordan-automorphism clause. `Θ_jordan` touches this row only as a `ComparisonSetup` field every *instance* must supply — a cost of the bundle, not a residue of the mathematics. (b) **What IS carried is `Θ_fix`, and `Θ_fix` is not a gate.** It is vdW Prop 5.5 in span-extended form (`MasterTheorem/Interface.lean:284-290`) — Prop 5.5 at the effect level plus "effects span `J`", which is `lem:span`, **manifest row 5**, the row withdrawn from EJA-GATED precisely because its residue is non-EJA. `EJA/InterfaceInstance.lean:54-55` has recorded this since it was written ("Row 16 rests on `Θ_fix` as much as on the FK three"). ★★ **This is the fourth instance of the biggest-residue error** the certificate's own WITHDRAWAL block documents three times: name the largest item in the residue and let it stand for the whole residue. Here it also ran backwards — the item named was not in the residue at all. ★★★ **CONSEQUENCE FLAGGED, NOT ACTED ON:** EJA-GATED requires every non-EJA clause to be closed in-tree, and `Θ_fix`'s span extension is not. **This row's EJA-GATED label is therefore OPEN on the WITHDRAWAL block's own test.** It is deliberately left standing: the pass that found this was scoped to the stated reason, and moving a terminal state is a campaign decision, not a drive-by. ★ **(E2) IS discharged** and that half of the 2026-08-20 update stands. ★ Prior record, kept: "★★ **Rows 16/17 note: gate (E3) IS manifest row 14 `prop:theta` at vIR generality, which is PRE-REGISTERED EXTERNAL.** So this row terminates at EJA-GATED behind a named citation and **cannot** reach FORMALIZED by axiomatization work alone ★★ **AND WHETHER THAT MEANS THEY CANNOT REACH FORMALIZED IS *OPEN*, not settled (diff audit, 2026-08-10): `gate_E3` as stated assumes `Φ` LINEAR, making it the classical Koecher/Alfsen–Shultz theorem rather than vIR's JB-generality version, so a Mathlib-grade f.d. EJA layer could discharge it in-tree.**" — ★★★ **that question is SETTLED 2026-08-22 (Koecher/AS; `Φ` is linear by TYPE, as is `ComparisonSetup.Θ`, `Interface.lean:264`) and it moves nothing here, because this row was never gated on (E3).** ★ `EJA-DIVIDEND.md`'s "CLOSES" verdict for this row is **retracted** the same day. ★ For this row there is **no missing mathematics** at the interface's own generality — both clauses are already proved over `CoalescenceSetup` and instantiated concretely; what is carried rather than derived is the FK fields and `Theta_jordan`. Prior record: PARTIAL — ★★★ **UNDERSTATED BY A WHOLE LEVEL; repriced 2026-08-09.** The previous text said the tree has "a differential shadow in strong pointwise form" and that the open part is "that the shadow *is* the cited lemma's differential". Both clauses are wrong. `MasterTheorem/Coalescence.lean`'s `coalescence_J2q` is the article's **second** clause — an invertible `a` scalar on $\text{range}(q)$ has $\Theta_a$ fixing $J_2(q)$ pointwise — **proved at the Θ (group) level, at abstract generality**; and `coalescence_block` is the **first** clause ($r_i = r_j \Rightarrow \Theta_{a(r)}$ fixes $V_{ij}$ pointwise), also group level. Neither is a differential shadow, and no identification-with-a-differential is needed. It is also *instantiated*: `Necessity/CoalescenceInstance.lean`'s `coalescenceSetup` builds the structure from an arbitrary product and **proves** the three Faraut–Korányi fields at the concrete carrier (`CoalescenceInstanceGen.lean` likewise for general `𝕜`). ★★ **The honest gap is different in kind**: at abstract generality the interface carries `Θ_jordan` (van Imhoff–Roelands) and the FK Peirce facts as **fields**, and `MasterTheorem/Interface.lean` says outright that it "does not encode the JB-algebra premises (the Jordan identity, formal reality, the cone-of-squares reading of `nonneg`)". So this is a **citation/axiomatization** gap, not an identification gap ★★★ **ARC-9 (2026-08-13): THE (E2) HALF OF THIS ROW'S GATE IS DISCHARGED ON THE CONCRETE CARRIER.** `EJA/ConcreteInstance.lean`'s `ejaComparison` is `Necessity.comparisonSetup` plus the five equations the interface never states (the Jordan identity for `jordan`; `p_idem`/`p_orth`/`p_sum`; `aOf r = ∑ exp(r i) • p i`), and `EJAComparison.toCoalescenceSetup` builds a `CoalescenceSetup` on `H_N(ℂ)` in which **`simDiag_opCommute`, `aOf_scalarOn` and `block_mem_J2` are THEOREMS, not citations** — proved from the Jordan identity via `EJA/`'s Peirce layer and `EJA/Bridge.lean`. ★ **Status stays PARTIAL and the terminal state stays EJA-GATED**: `lem:coalescence` rests on FOUR hypotheses, and the fourth, `Θ_fix` (vdW Prop 5.5, span-extended), is a `ComparisonSetup` field and remains cited — as does `Θ_jordan`, entering via `ThetaPreservesJordan` (milestone M3). What changed is that the gate is now **(E3) only**. ★★★ **"the gate is now (E3) only" IS WITHDRAWN 2026-08-22** — see the restatement at the head of this cell. The sentence before it is exactly right and is what made the error survive: it names `Θ_fix` **and** `Θ_jordan` as the two survivors, and the next clause silently drops the one that is actually load-bearing for this row and keeps the one that is not. ★★ And the discharge **refuted a claim in `WallCertificates/eja-gated.lean`**: its `gate_E2_peirce` is this theorem, but its `haOf` hypothesis (`∃ c > 0, aOf r = ∑ c i • p i`) does not tie `c` to `r`, so `r i = r j` gives nothing about `c i` vs `c j` — the gate is UNDER-HYPOTHESISED and its "sufficient for the purpose" note is wrong. ★★★ **PAST TENSE, 2026-08-22: THAT WAS REPAIRED ON 2026-08-20 AND THIS CELL KEPT ASSERTING IT.** `haOf` now reads `∀ r, C.aOf r = ∑ i, Real.exp (r i) • C.p i` — the coefficients **are** `Real.exp ∘ r`, so `r i = r j → c i = c j` is one `congrArg Real.exp` (`EJA/InterfaceInstance.lean:157`). `gate_E2_peirce` is a **proved, sorry-free theorem** with axioms exactly `[propext, Classical.choice, Quot.sound]`. ★★ **The manifest contradicted itself at HEAD**: the terminal-state ledger three sections above has said since 2026-08-20 that (E2) is a proved theorem, while this cell said the gate stating it was under-hypothesised. ★ **And this cell was rewritten TODAY by the correction pass** — the withdrawal appended two sentences earlier is about a different clause in the same block, and this one inherited its neighbour's freshness. **The freshest prose is the least trustworthy prose, including prose freshly written to fix something else.** | 6.2 |
| 17 | `lem:homomorphism` | 968 | lemma | $r\mapsto\Theta_r$ is a continuous homomorphism $((-\infty,0]^n,+)\to\Stab(F)^\circ$, extending to $\chi:(\mathbb{R}^n,+)\to\Stab(F)^\circ$ with real-linear differential $d\chi$ satisfying $\rho_{ij}(d\chi(r))=(r_i-r_j)T_{ij}$ for a single $T_{ij}\in\mathfrak{so}(V_{ij})$. | FORMALIZED — ★★★ **THE ROW IS CLOSED, 2026-08-24, INCLUDING THE `𝔰𝔬(V_{ij})` THE STATEMENT ASKS FOR** (`RadicalRelativity/OneParameter.lean`, `EJA/ThetaDifferential.lean`). `EJA.exists_blockGenerator_skew`: for `i ≠ j` there is a **single** `T` with `ρ_{ij}(dχ(r)) = (rᵢ−rⱼ)·T` for every `r`, and `T` maps `V_{ij}` into itself and is skew-adjoint there — i.e. `T ∈ 𝔰𝔬(V_{ij})`. `EJA.chiCLM_mem_stabFrame_connectedComponent` supplies the codomain: `χ` really lands in `Stab(F)°`. ★★★ **The obstruction the previous cell called Lie-theoretic is closed by a theorem that uses no Lie theory.** `OneParam.hasDerivAt_of_continuous`: *a continuous one-parameter subgroup of a real Banach algebra is differentiable*, with `φ'(u) = φ(u)φ'(0)`. Mathlib has no such theorem. Proof: put `F(t) = ∫₀ᵗφ`; the group law becomes `φ(u)F(t) = F(u+t) − F(u)`; `F(t)/t → φ(0) = 1` makes some `F(t₀)` a unit; and `φ(u) = (F(u+t₀) − F(u))F(t₀)⁻¹` is a difference of differentiable functions times a constant. **Differentiability is deduced, never assumed** — which the cell's own warning required, since the unknown product's `L_a` is only ever known continuous. ★★ **The four ingredients**: (a) `blockRestrict`, `ρ_{ij}` as a named map, defined for every operator so it applies before invariance is known; (b) `twistChi_id_on_frameBlock`, row 16 pushed from `Θ` to `χ`; (c) `hasDerivAt_dChi`, fed by `chiCLM` and `continuous_chiCLM` — the `Θ_{v(r)}⁻¹` factor carried by continuity of inversion at a unit of a Banach algebra, since `Θ` itself is defined only on the cone; (d) `exists_smul_of_vanishing_on_diag`. Linearity of `dχ` is the product and chain rules on the group law (`dChi_add`, `dChi_smul`, bundled as `dChiL`). ★★★ **How `𝔰𝔬` was obtained without a form-uniqueness theorem, recorded because the obvious route is a dead end.** Skewness needs `χ` to be an isometry, and "a Jordan automorphism preserves an associative form" is *not* available here (the class carries an abstract `inner_assoc` form, not the canonical trace form; uniqueness up to a positive scalar on each simple summand is not in this tree). It is not needed. `⟪u,v⟫ = ⟪u∘v,1⟫` follows from `inner_assoc` alone (`inner_mul_one`); for `x,y ∈ V_{ij}` the product `x∘y` lies in `J₁(pᵢ) ⊕ J₁(pⱼ)` — `peirceHalf_mul_half_eq_zero` (`J½∘J½ ⊆ J₁⊕J₀`) at `pᵢ` and `pⱼ`, `eigen_zero_mul_zero` at every other atom, and completeness — and `theta_id_on_peirceTwo_all` says `Θ` is **pointwise the identity** on each `J₁(pₖ)`. So `⟪χx, χy⟫ = ⟪χ(x∘y),1⟫ = ⟪x∘y,1⟫ = ⟪x,y⟫`: an isometry **on the block**, which is the only place the article's `𝔰𝔬` lives. Differentiating that constant gives skewness; `dChi_mapsTo_frameBlock` gives block-preservation, from `twistChi_mapsTo_frameBlock` plus closedness of a finite-dimensional submodule. ★ `χ ∈ Stab(F)°` likewise avoids the standard "identity component is a subgroup" lemma: the path `t ↦ χ(t·r)` is continuous, runs from `1` to `χ(r)`, and stays in `Stab(F)`, so its range is a connected subset containing both endpoints. ★★ **Rows 18, 20 and 21 no longer sit behind (c)** — the cell said one Lie-theoretic construction gated four of the remaining rows; that gate is open, and what each of those rows still needs is on its own row. Prior record: PARTIAL — ★★★ **FOUR OF THE FIVE CLAUSES ARE NOW PROVED, 2026-08-24** (`EJA/ThetaAbstract.lean`), for any S1–S7+S2 product. Beyond the homomorphism, continuity and `Stab(F)°` membership recorded below, **the extension clause is done**: `twistChi` defines `χ : (ℝⁿ,+) → J ≃ₗ[ℝ] J` by `χ(r) := Θ_{u} Θ_{v}⁻¹` on the cone decomposition `r = u − v` (`uPart`/`vPart`, with `uPart_sub_vPart`); `twistTheta_div_congr` is its **well-definedness** — `u − v = u′ − v′ ⟹ Θ_uΘ_v⁻¹ = Θ_{u′}Θ_{v′}⁻¹`, which is the homomorphism plus `twistTheta_commute`; `twistChi_add` proves `χ(r+s) = χ(r)χ(s)` on **all** of `ℝⁿ`; and `twistChi_eq_twistTheta` proves `χ` really **extends** `Θ` — they agree on `(−∞,0]ⁿ`, using `twistTheta_zero` (`Θ_0 = id`). ★ The group algebra runs in `J ≃ₗ[ℝ] J`'s automorphism group, where `(a*b)z = a(b z)`, so `twistTheta_add` reads as `Θ_{r+s} = Θ_r * Θ_s` and the cancellations are ordinary group manipulation once the relevant elements are known to commute. ★★★ **The single remaining clause is the differential**: `dχ` real-linear with `ρ_{ij}(dχ(r)) = (rᵢ−rⱼ)T_{ij}` for a single `T_{ij}`. That needs the Peirce-block action `ρ_{ij}` as a named map, differentiability of `t ↦ χ(t·r)` at `0`, and `T_{ij}` shown independent of `r` — the genuinely Lie-theoretic part, and the same machinery **row 18**'s stabilizer classification sits behind. Nothing else on this row is open. Prior record: PARTIAL — ★★★ **THREE OF THE FIVE CLAUSES ARE PROVED, 2026-08-24** (`EJA/ThetaAbstract.lean`), for any S1–S7+S2 product: **homomorphism** (`twistTheta_add`, `Θ_{r+s} = Θ_r ∘ Θ_s`), **continuity** (`continuous_toCLM_thetaOf_twistElt`), and **landing in `Stab(F)°`** (`theta_mem_stabFrame_connectedComponent`) — so `r ↦ Θ_r` is a continuous homomorphism `((−∞,0]ⁿ,+) → Stab(F)°`, which is the row's first assertion in full. ★★ **The plumbing that made the homomorphism statable cleanly is worth naming**, because it recurred all day: `Θ` at `a(r)·a(s)` and `Θ` at `a(r+s)` are the same map but carry *different effect proofs*, and every attempt to compose them tripped on dependent transport. `seqLeftMulAbs_congr` settles it once (elements differ, proofs are irrelevant), `twistTheta` packages `Θ` as a function of the parameter alone, and `twistTheta_unique` identifies any map satisfying the defining identity with it — so `twistTheta_add` is now transport-free. `quadJ_twistElt` (`Q_{√a(r)}a(s) = a(r+s)` for the **standard** product) is the element-level companion. ★★ **What remains is clauses (4) and (5)**: the extension `χ` to `(ℝⁿ,+)` — every `r` is `u − v` with `u, v ≤ 0`, and well-definedness is `u + v' = u' + v ⟹ Θ_uΘ_{v'} = Θ_{u'}Θ_v`, which is the homomorphism plus `theta_comm`; and the differential `dχ` with `ρ_{ij}(dχ(r)) = (rᵢ−rⱼ)T_{ij}`, which needs the block action `ρ_{ij}` and `T_{ij}` well-defined, and is the genuinely Lie-theoretic part. ★ Clause (4) is now bounded construction; clause (5) is not. Prior record: PARTIAL — ★★★ **THE ANALYTIC GATE IS NOW MOSTLY BUILT, 2026-08-24.** Landed today, in order: `exists_effect_basis` and `continuous_toCLM_of_basis` (`Normality.lean`); `continuous_toCLM_quadJ` and **`continuousOn_toCLM_seqLeftMulAbs`** (`EJA/ConeTopology.lean`) — **paper S2 upgraded from pointwise to operator continuity**, with no basis of effects actually constructed: continuity of `x ↦ L_{g x} v` holds at effects by S2, is stable under linear combinations because `L` is linear in `v`, and the effects span; and `stabFrame` with `theta_mem_stabFrame` (`EJA/ThetaAbstract.lean`) — **`Stab(F)` named as a subset of the operator space `J →L[ℝ] J`, so that it carries the topology `Stab(F)°` needs, with `Θ_r` proved to lie in it.** ★★ **What is left for `Θ_r ∈ Stab(F)°` is one construction, no longer any missing idea**: the path `t ↦ Θ_{t·r}` from `id` (`thetaOf_one`) to `Θ_r`, whose continuity is now assembled from `continuousOn_toCLM_seqLeftMulAbs`, `continuous_toCLM_quadJ`, and the explicit frame formulae `√a(r) = ∑ e^{r_k/2}p_k` and `Q_{√a(r)}⁻¹ = Q_{√a(−r)}` (the latter because `jinvOfResolution` of `k ↦ e^{r_k}` is `a(−r)` on the nose). Prior record: PARTIAL — ★★ **TWO MORE PIECES OF THE SHARED ANALYTIC GATE LANDED 2026-08-24** (`Normality.lean`): `exists_effect_basis` — a finite-dimensional order-unit space has a **basis consisting of effects**, off `span_isEffect_eq_top`; and `continuous_toCLM_of_basis` — a parametrised family of linear endomorphisms of a finite-dimensional normed space is continuous as an *operator*-valued map as soon as its values on one basis are, because reconstruction from basis values is a linear map out of a finite-dimensional space. ★★★ **Together these are exactly what turns paper S2 into operator continuity**: S2 gives continuity of `a ↦ a · b` for each fixed *effect* `b`, and a basis of effects is where that applies. With `thetaOf`, `thetaOf_one` and the explicit frame formulae `√a(r) = ∑ e^{r_k/2}p_k` and `Q_{√a(r)}⁻¹ = Q_{√a(−r)}`, the only step still missing for `Θ_r ∈ Stab(F)°` is assembling the path `t ↦ Θ_{tr}` and naming `Stab(F)` with its topology. Prior record: PARTIAL — ★★ **`Θ` IS NOW A DEFINITION, 2026-08-24** — `EJA.thetaOf` (`Q_{√b}⁻¹ ∘ L_b`) with `thetaOf_spec` re-deriving unitality, the Jordan-morphism property and the defining identity, plus `thetaOf_one` giving `Θ = id` at the unit. Until now `Θ` existed only existentially (`exists_theta`), which sufficed for every algebraic clause but not for the analytic ones: continuity of `r ↦ Θ_r`, the identity component, and `dχ` all need `Θ` to be a **function of its data**. That prerequisite is now in place, and `thetaOf_one` is the base point the path argument starts from. Prior record: PARTIAL — ★★★ **THE ALGEBRAIC CORE IS PROVED AT ABSTRACT EJA GENERALITY, 2026-08-24** (`EJA/ThetaAbstract.lean`), for any S1–S7+S2 product. **(a) The group law** — `sp_twistElt`: for the twist family `a(r) := ∑ e^{rₖ}pₖ` of a Jordan frame, `a(r) · a(s) = a(r+s)` in the **unknown** product, not merely the standard one, because `a(r)` and `a(s)` are operator-commuting effects and `sp_eq_quadJ_of_opCommute` pins the value. **(b) The homomorphism clause** — `theta_twistElt_hom`: `Θ_{r+s} = Θ_r Θ_s` and `Θ_rΘ_s = Θ_sΘ_r`, off `theta_mul`/`theta_comm` (row 14). **(c) Landing in the stabilizer** — `theta_fixes_frame_atom` (row 15) shows every `Θ_r` fixes every frame atom, which is what `Stab(F)` membership means. ★★ **What is open is the analytic half, and it is all one package**: *continuity* of `r ↦ Θ_r`, the *identity component* `Stab(F)°` (which needs a topology on the automorphism group — `J ≃ₗ[ℝ] J` is finite-dimensional so one exists, but it is not set up here), the extension `χ` to `(ℝⁿ,+)`, and the differential condition `ρ_{ij}(dχ(r)) = (rᵢ−rⱼ)T_{ij}`. ★★★ **This is the same package that manifest row 15's one open clause needs** (`Θ_r ∈ Stab(F)°`), so rows 15 and 17 are gated on a single piece of work; that entanglement is recorded on both rows rather than priced twice. ★ Axioms: `[propext, Classical.choice, Quot.sound]`, no `sorryAx`. Prior record: PARTIAL — ★★★ **TERMINAL STATE CORRECTED 2026-08-22: WALL-CERTIFIED, not EJA-GATED** (certificate `WallCertificates/differential-trio.lean`, which covers rows 16, 17 and 18 with zero gaps). **The status word PARTIAL does not move and no coverage count changes.** This cell carried EJA-GATED from 2026-08-10 while `WallCertificates/eja-gated.lean`'s own row-17 line said, from the same day, that row 17 "should be read as WALL-CERTIFIED, not EJA-GATED" — **a live contradiction between two of the campaign's decision instruments, at HEAD, for twelve days.** Resolved in the certificate's favour because the residue is checkable and is non-EJA on **three** independent counts, none of them (E1)/(E2)/(E3): **(i)** `DiagonalHomSetup` (`MasterTheorem/DiagonalHom.lean:180-193`) carries `ρ`, `ρ_skew`, `dχAdd`, `dχAdd_cont` and `coalescence_diff` as **fields**, whose own docstrings call them "interface data; the paper's analytic differentiation of `χ̃`, not constructed here" and "a cited hypothesis" — no Jordan axiomatization produces an analytic differential or a continuity hypothesis; **(ii)** the article's statement includes `\to\Stab(F)^\circ`, needing the stabilizer as a group with an identity component, which is the same absent vocabulary that withdrew row 15; **(iii)** `Θ_r` is the comparison map of the unknown sequential product, and no gate supplies the order-unit/effect layer that product lives on (`SequentialProductOn` is declared over `[OrderUnitSpace V]`, `SequentialProduct.lean:244`; `RadicalRelativity/EJA/` has zero occurrences of `OrderUnitSpace` and zero of `IsEffect`, measured per file across all 15 files, 2026-08-22). ★★ **Same error as rows 5, 6 and 15: classify a row by its BIGGEST residue and let that stand for its WHOLE residue** — and this time both instruments recorded the diagnosis correctly in one place while the other kept asserting the label. ★ Prior record, superseded, kept for provenance: "the article-generality residue is **gate (E2)+(E3)** stated in Lean with `sorry` exactly at the axiomatization gap. ★★ **Rows 16/17 note: gate (E3) IS manifest row 14 `prop:theta` at vIR generality, which is PRE-REGISTERED EXTERNAL.** So this row terminates at EJA-GATED behind a named citation and **cannot** reach FORMALIZED by axiomatization work alone ★★ **AND WHETHER THAT MEANS THEY CANNOT REACH FORMALIZED IS *OPEN*, not settled (diff audit, 2026-08-10): `gate_E3` as stated assumes `Φ` LINEAR, making it the classical Koecher/Alfsen–Shultz theorem rather than vIR's JB-generality version, so a Mathlib-grade f.d. EJA layer could discharge it in-tree.**" — ★★★ **that question is SETTLED 2026-08-22 (Koecher/AS; `Φ` is linear by TYPE) and it is moot for this row, which is not EJA-gated at all.** ★ `EJA-DIVIDEND.md`'s "CLOSES" verdict for this row is **retracted** the same day; its whole stated reason had been the four words "same shape as row 16". Prior record: **PARTIAL — repriced from ABSENT 2026-08-08 (ARC-6 rung 6.0), and the stated reason was FALSE.** "Θ is never differentiated" described the abstract skeleton and was generalized to the whole tree; in fact `Necessity.chiTilde` *constructs* the extension by the article's own `min(x,0)` decomposition, `chiTilde_add` proves the homomorphism law, and `chiTilde_eq_exp` **proves** `∃!` real-linear `dχ` with `χ̃ = exp ∘ dχ` (via `multiParameter_eq_exp`, importing no Lie theory, needing only line continuity). The one genuinely missing clause — the hyperplane factorization — was proved this arc, rank-free, in `Necessity/PhaseAnchor.lean`: `rhoChi_eq_smul_generator` gives the article's `ρ_ij(dχ(r)) = (r_i−r_j) • T_ij` with `T_ij = ρ_ij(dχ(e_i))`, skew because `dχ` lands in `blockSkewSubmodule`; `tvalLm_eq_coef_mul` is the coordinate form. Remaining gap is generality alone: the article states this over a simple EJA with a Jordan frame, Lean has it on the concrete `H_n(ℂ)` carrier — the same gap as `prop:theta`. ★★ **"the same gap as `prop:theta`" is struck 2026-08-22**, on two counts: `prop:theta`'s own residue was restated the same day (it is the classical Koecher/Alfsen–Shultz theorem, not vIR's JB-generality version — see row 14), and this row's port to EJA generality is *analytic and group-theoretic* work (`χ̃`'s differential, the identity component of `Stab(F)`), which is a different kind of gap from a cited rigidity theorem. **"Generality alone" is true and it is not a synonym for "the EJA axiomatization"** — which is exactly the reading that put this row in EJA-GATED for twelve days. Detail in `THEOREM-MAP.md` §3c | 6.2 |
| 18 | `prop:stabilizers` | 1020 | proposition | For each of the four simple types of rank $n$, the identity component of the frame stabilizer and its induced action on each Peirce block $V_{ij}$: $\{1\}$/0 for ℝ, $T^{n-1}$/$i(\theta_i-\theta_j)z$ for ℂ, $Sp(1)^n/\{\pm1\}$/$\xi_ix-x\xi_j$ for ℍ, $\Spin(8)$/triality for 𝕆. | PARTIAL — ★★★ **RE-PRICED 2026-08-24: THIS IS NOW THE ONLY ROW THAT NEEDS THE STABILIZER CLASSIFICATION.** Rows 20 and 21 closed the same day *without* it — row 17's `(rᵢ−rⱼ)T_{ij}` is linear in `r`, and that alone forces the block coordinate to be multiplication by a central element, so the twist dies without ever identifying `Sp(1)^n/{±1}` or `Spin(8)`. This row, by contrast, **states** the identity component and its Peirce action, so it cannot be reached that way: `{1}`/`0` for ℝ, `T^{n-1}`/`i(θᵢ−θⱼ)z` for ℂ, `Sp(1)^n/{±1}`/`ξᵢx − xξⱼ` for ℍ, `Spin(8)`/triality for 𝕆. ★ The 𝕆 clause is Yokota, a citation `MasterTheorem/Branches/Albert.lean` says cannot be derived in the abstract interface — so this row, unlike row 21, really is gated on it. ★ Superseded record: PARTIAL — ★★★ **SUBSTANTIALLY REPRICED 2026-08-09; the previous status text was wrong in both of its clauses.** It said the coupling identity is only "a conclusion of the `StabilizerCoupling` constructor" with "construction of the representation from Θ open". Both are false. `Necessity/StabilizerInstance.lean` **builds every piece from `(P, hS2, hjord)`**: `rhoField` (the block compression), `dChiStab` landing in the stabilizer by `dChi_mem_blockSkew`, the carrier `blockSkewSubmodule`, `diagonalHomSetup` with `ρ_skew` and `coalescence_diff` proved, and `stabilizerCoupling` producing the whole structure — its own docstring says it "**produces** the coupling". And `PhaseAnchor.lean`'s `complex_perFrame_concrete` proves $\exists t_F,\ \rho_{ij}(d\chi(r)) = (t_F(r_i-r_j))\cdot\text{rotJ}$ from an *arbitrary* product, rank-free above 3 — that is the article's $z\mapsto i(\theta_i-\theta_j)z$ in Lie-algebra form. ★★★ **And the group-level statement, which a wall certificate had called UNSTATABLE ("needs the stabilizer group as a Lie group with an identity component"), is now PROVED**: `Necessity.tvalCoef_realized_by_stabilizer` and `stabilizer_group_action_complex` — the torus element `torusU (tvalCoef …) r` fixes every frame projection (`torusU_fixes_frameProj`, so it *is* in the frame stabilizer) and rotates each Peirce block by exactly the predicted phase (`torusU_block`). Four lines, no Lie-group vocabulary. ★★★ **CORRECTED 2026-08-09 (ARC-8 checkpoint 1): the sentence that stood here was STALE, and it was citing a grep as its evidence.** It read "what remains for the ℂ row is only the CONVERSE … writable today … looks like a short matrix argument (grep for `fixes_frameProj\|frameStabilizer\|stabilizes.*frame`: every hit is Θ/χ̃ *fixing* the frame, none the converse)". The converse **was already in the tree** as `Necessity.offdiag_eq_zero_of_fixes_frameProj`, landed the same arc the sentence was written in; a cold reviewer caught the row still instructing readers to build it. This is the "fix the row, not just the footnote" failure in its purest form — `LEDGER.md` had already retracted the corresponding work order while this row went on issuing it. ★★ **And the arc's own block 8.2 was written from this row, so the staleness propagated into the orders.** Standing consequence: **a status cell that carries a grep as its absence evidence must be re-checked against the tree, not against its own prose.** What is now proved for the ℂ row: `offdiag_eq_zero_of_fixes_frameProj` (frame-fixing ⟹ diagonal) plus `Necessity.normSq_diag_eq_one_of_fixes_frameProj` (the diagonal entries are unimodular, added at checkpoint 1 because the reviewer noticed the certificate's name `frame_stabilizer_is_torus` claimed the torus while the theorem gave only diagonality) — together, the frame stabilizer in $U(n)$ is **exactly** the diagonal torus $T^n$. ★ **What is still NOT stated is the article's "identity component $T^{n-1}$"**: $T^n$ is connected, so the article's $T^{n-1}$ is the stabilizer's image in the *automorphism* group, i.e. $T^n$ modulo the globally-acting phase, and that quotient appears nowhere in the tree (grep `identityComponent\|IsConnected\|ConnectedComponent\|identity component` over `RadicalRelativity/`, 2026-08-09: three hits, two docstring prose in `FrameConstancy.lean`, one unrelated). Its mathematical content *is* proved — the stabilizer's action on the $(i,j)$ Peirce block depends only on the phase **difference** $\theta_i-\theta_j$ (`torusU_block`), and phase differences are exactly what a global phase cannot move — so what is missing is group-theoretic packaging, not mathematics. ★★ **The ℝ row's elementwise content landed 2026-08-09 (ARC-8 block 8.5)**: `Necessity.offdiag_eq_zero_of_fixes_frameProjG` and `Necessity.normSq_diag_eq_one_of_fixes_frameProjG` are the ℂ pair generalized to every `RCLike 𝕜` — the proofs use nothing but `adU`, `frameProj` and matrix algebra — so the frame stabilizer is elementwise the diagonal torus at ℝ as well as ℂ (over ℝ: entries $\pm1$). ★ **This is why the ℝ row was never the work this row's status implied**; what remained was the *identity component* packaging, which is a connectedness statement, not a type-specific computation. Residue: the identity-component packaging for ℝ and ℂ (`{1}` and $T^{n-1}$ — both quotient/connectedness statements), plus the ℍ and 𝕆 rows and their Peirce actions. ★★★ **Re-priced 2026-08-24, and the ℍ/𝕆 half is no longer about missing carriers.** `HermMat (Fin n) (Quaternion ℝ)` and `HermMat (Fin 3) Octonion` both carry `EuclideanJordanAlgebra` (see row 20's correction), and `EJA.dChi_jordanDeriv` / `EJA.dChi_frameProj` now exhibit the object to be classified — a **derivation of the Jordan algebra annihilating every frame atom** — at both carriers. So the ℍ row is the single statement "such a derivation acts on `V_{ij} ≅ ℍ` by `x ↦ ξ_i x − x ξ_j`", an elementary block-coordinate computation shared with row 20, and the 𝕆 row is Yokota triality, shared with row 21. ★ The vocabulary complaint about connectedness is also stale: `connectedComponentIn` is now used in `EJA/ThetaAbstract.lean` and `EJA/ThetaDifferential.lean` | 6.2 |
| 19 | `prop:real` | 1086 | proposition | On $\MnKsa{n}{\mathbb{R}}$, $n\geq2$: $\Theta_r=\mathrm{id}$, so $a\cdot b=Q_{\sqrt a}b$; in particular the real qubit is rigid. | **FORMALIZED** — `Necessity.real_classification` (+ `_ouNorm`) | — |
| 20 | `thm:quaternionic` | 1102 | theorem | On $\MnKsa{n}{\mathbb{H}}$, $n\geq3$: $\Theta_r=\mathrm{id}$, so $a\cdot b=Q_{\sqrt a}b$. | FORMALIZED — ★★★ **CLOSED 2026-08-24 ON THE CONCRETE CARRIER** (`EJA/CarrierInstances.lean`, `EJA/QuaternionicLuders.lean`). `EJA.quaternionic_twistTheta_id`: on `H_n(ℍ)` with `n ≥ 3`, `Θ_r = id` for **every** S1–S7 (+S2) sequential product; `EJA.quaternionic_luders` states the row's literal conclusion, `a·b = Q_{√a}b`. ★★★ **THE CELL'S RECORDED OBSTRUCTION WAS FALSE, AND THAT IS THE TENTH DEFECT OF THIS FILE'S OWN SPECIES.** It said "no concrete quaternionic carrier", blaming `RCLike` (which extends the *commutative* `DenselyNormedField`, so `ℍ` can never be an instance). Right about the field-general `Gen` layer; **false about the tree** — `EJA/HermMatCarrier.lean`'s `instEuclideanJordanAlgebraHermMat` has carried `EuclideanJordanAlgebra` on `HermMat ι C` for every *associative* composition coefficient, i.e. `ℝ`, `ℂ` and `ℍ`, and `#synth` fires all four required instances at `HermMat (Fin n) (Quaternion ℝ)`. The cell was written before the abstract EJA layer existed and was never re-checked against it. ★★★ **AND THE ROUTE IS NOT THE ARTICLE'S.** The article classifies the frame stabilizer (`Sp(1)^n/{±1}` acting by `ξ_i x − x ξ_j`) and then applies `Z(ℍ) ∩ Im ℍ = 0`. **No classification was needed.** Row 17 delivers `ρ_{ij}(dχ(r)) = (rᵢ−rⱼ)T_{ij}` — *linear in `r`, with that exact shape* — and `EJA.dChi_jordanDeriv` makes `dχ(r)` a derivation, giving the cross-block relation `d_{ij}(xy) = d_{ik}(x)y + x·d_{kj}(y)` at a spectator index `k` (available at `n ≥ 3`). Reading that at the two standard basis vectors `r = e_i` and `r = e_j` gives `t_{ij}(xy) = t_{ik}(x)·y` **and** `t_{ij}(xy) = x·t_{kj}(y)`; setting `x = 1` in the first and `y = 1` in the second exhibits `t_{ij}` as **both left and right multiplication by one element `γ`**, so `γ` is central — `EJA.exists_central_blockCoord`, and it holds for any associative composition coefficient. `EJA.central_blockCoord_im` then makes `γ` imaginary from the derivation property alone: two block elements multiply into `ℝpᵢ ⊕ ℝpⱼ` (`hermOff_mul_hermOff_one`), where `dχ(r)` vanishes because it annihilates the frame atoms. **No inner product on the block, no skewness, no Lie theory.** ★★ **The general theorem is `EJA.twistTheta_id_of_center_im_trivial`**, whose only type-specific hypothesis is `Z(C) ∩ Im C = 0` — so it fires at `ℝ` as well, and the ℂ case is exactly where it *fails*, which is why the complex type carries a twist. At `ℍ` the hypothesis is `MasterTheorem.Quaternionic.central_im_zero`, already in the tree. ★★ **The supporting concrete layer, all new the same day**: `EJA.hermIdem_isPrimitive` (the one missing `JordanFrame` field) gives `EJA.hermFrame`, the **standard frame** of `H_ι(C)` rather than the abstract `exists_jordanFrame`; `EJA.eq_hermOff_of_mem_frameBlock` with `hermOff_injective` is the **block-coordinate identification `V_{ij} ≅ C`**; `EJA.hermOff_mul_hermOff` is the cross-block product rule; and `EJA.twistTheta_eq_one_of_dChi_eq_zero` turns `dχ = 0` into `Θ_r = id` with no exponential map, since `OneParam.hasDerivAt_of_continuous` makes `φ'(0) = 0` force `φ' ≡ 0`. ★ **What this does NOT settle**: row 18's ℍ row asks for the *classification* of the stabilizer, which is a different statement and is still open — this row was closed by going around it. Superseded record follows: PARTIAL (`MasterTheorem.luders_quaternionic_produced` at skeleton level, with $Z(\mathbb{H})\cap\operatorname{Im}\mathbb{H}=\{0\}$ computed; no concrete quaternionic carrier). **Priced 2026-08-08 (ARC-6 rung 6.5)** — The obstacle is that this tree's field-general `Gen` layer is written for `RCLike 𝕜`, and **ℍ can never be `RCLike`**: the class extends `DenselyNormedField` — commutative — and its `re_add_im_ax` demands a *two*-dimensional real decomposition $z = re(z) + im(z)\,I$, whereas ℍ is noncommutative and four-dimensional. That is an impossibility, not a missing instance, so no amount of work makes the `Gen` machinery instantiate at ℍ. **Note the scope: this blocks the `Gen` LAYER from reaching ℍ, not the row itself** — the row remains reachable by another route, which is why it is not called blocked. The route is therefore forced to be the one the orders named: $H_n(\mathbb{H}) \hookrightarrow H_{2n}(\mathbb{C})$ as the fixed points of a conjugate-linear involution, with the quaternionic structure carried by the involution rather than by a scalar field. Not attempted in ARC-6. ★★★ **THE SUFFICIENCY DIRECTION LANDED 2026-08-09 (ARC-8 block 8.3)**: `HermitianMat.quatLuders` is a `SequentialProductOn (QuatCarrier n)` — the Lüders product on $H_n(\mathbb{H})$, with S1 and S3–S7 — and `HermitianMat.quatSp_eq_quatQuadRep` identifies it with the tree's own `quatQuadRep`, i.e. with the article's $a\cdot b = Q_{\sqrt a}b$. This **falsifies the wall certificate's remaining absence claim** (`grep 'SequentialProductOn (QuatCarrier'` → no hits): the carrier now has a product. ★ **Nothing new was proved about S1–S7 to get it.** Every axiom is the ambient one at $t=0$ read through the subtype coercion; the only content is closure of the quaternionic set under $Q_{\sqrt a}$, which was already in the tree (`IsQuaternionic.quadRep` = `IsQuaternionic.cfc_of_effect` + `IsQuaternionic.conj`). Recording that, rather than the eight field names, is the honest description of the work. ★★★ **THE ROW'S OWN STATEMENT IS UNTOUCHED, and it is the necessity direction**: `thm:quaternionic` says an *arbitrary* S1–S7 product on $H_n(\mathbb{H})$, $n\geq3$, **must** be this one ($\Theta_r=\mathrm{id}$). That transfer — whether the article's $Z(\mathbb{H})\cap\operatorname{Im}\mathbb{H}=\{0\}$ mechanism survives the embedding — is still the only honest residue and is still genuinely open. The row stays PARTIAL; never read the existence of the product as the theorem | 6.5 banked |
| 21 | `thm:albert` | 1131 | theorem | On $H_3(\mathbb{O})$: $\Theta_r=\mathrm{id}$, so $a\cdot b=Q_{\sqrt a}b$. | FORMALIZED — ★★★ **CLOSED 2026-08-24, AND WITHOUT YOKOTA TRIALITY** (`EJA/AlbertLuders.lean`). `EJA.albert_twistTheta_id`: on `H₃(𝕆)` the twist automorphism of **every** S1–S7 (+S2) sequential product is the identity; `EJA.albert_luders` states the row's literal conclusion, `a·b = Q_{√a}b`. ★★★ **THE CELL'S RESIDUE ITEM (1) IS NOT NEEDED, AND THAT IS THE POINT.** It recorded Yokota's Spin(8)-triality faithfulness, carried as `IsAlbertModel`'s `block_injective`, whose own docstring in `MasterTheorem/Branches/Albert.lean` says it is a *cited* hypothesis and that "`Stab` carries no Lie structure in the abstract interface, so simplicity of `𝔰𝔭𝔦𝔫(8)` is not available to prove it internally". **True of that route; the conclusion does not need that route.** Row 17 delivers `ρ_{ij}(dχ(r)) = (rᵢ−rⱼ)T_{ij}`, *linear in `r`*; with `dChi_jordanDeriv` making `dχ(r)` a derivation, reading the cross-block relation at `r = e_i` and `r = e_j` exhibits the block coordinate as both left and right multiplication by one element, hence by a **central** one (`exists_central_blockCoord`), which the derivation property then forces **imaginary** (`central_blockCoord_im`). What remains is `Z(𝕆) ∩ Im 𝕆 = 0`. ★★ **The whole argument is ASSOCIATIVITY-FREE**, which is why it reaches `𝕆` at all. The only thing `[Ring C]` was ever doing in `EJA/CarrierInstances.lean` was *finding* the `EuclideanJordanAlgebra` instance; replacing it by the fidelity class `EJA.HermMatFid` (the carrier's class product and unit are `jmul` and `hermOne` — `rfl` at every carrier) lets `ℝ`, `ℂ`, `ℍ` and `𝕆` share every proof. `EJA/AlbertBridge.lean`'s own docstring had already said the octonionic instance's fields are "the general lemma `EJA/HermMatCarrier.lean` uses, at the weakest tier — none of them needed associativity"; this row is what that sentence was worth. ★★ **`Z(𝕆) ∩ Im 𝕆 = 0` is proved here** (`EJA.octonion_center_im_trivial`) from the Clifford relation `xy + yx = −2⟪x,y⟫·1` (`CompositionAlgebra.pure_mul_pure_add`, already general in the tree): centrality of a pure `γ` turns it into `γx = −⟪γ,x⟫·1` for every pure `x`; feeding two **orthogonal unit imaginaries** and using multiplicativity of the norm form gives `nf γ = ⟪γ,e₁⟫² = ⟪γ,e₂⟫²` together with `nf γ·(⟪γ,e₁⟫² + ⟪γ,e₂⟫²) = 0`, so `nf γ = 0`. ★ The same shape works at `ℍ`, where the tree already had `MasterTheorem.Quaternionic.central_im_zero`. ★ Residue item (2), the shared inhabitant, is `EJA.ludersSequentialProduct`; item (3), instantiation at the concrete exceptional carrier, is this file. Superseded record follows. PARTIAL — ★★★ **THE `SequentialProductOnEJA` INHABITANT IS BUILT, 2026-08-24** (`EJA/ConeTopology.lean`): `EJA.ludersSequentialProduct` is a genuine `SequentialProductOn J` for `a·b = Q_{√a}b` on any finite-dimensional Euclidean Jordan algebra — **all eight fields, S1 and S3–S7** — with **S2** as `ludersSequentialProduct_firstArgContinuous`. So this row's shared blocker is gone. The chain: S2 off compactness of the effect interval and continuity of the Jordan square root; S5's own hypothesis, S6a, S6b and S7 off `prop:bridge` (row 10, FORMALIZED the same day), with S7 additionally needing `opCommute_mul` — the operator-commutant of any element is a subalgebra. ★★★ **TWO OF THE THREE RESIDUE ITEMS ARE NOW CLOSED, 2026-08-24.** Item (2), the shared inhabitant, is `EJA.ludersSequentialProduct` above. Item (3), *the instantiation of the abstract skeleton at the concrete exceptional carrier*, is closed too: `EJA/AlbertBridge.lean` carries `EuclideanJordanAlgebra` on `HermMat (Fin 3) Octonion`, row 17's analytic machinery landed the same day, and `EJA/CarrierInstances.lean` **fires** the instantiation — `albertLuders` and `albert_dChi_deriv`, the latter exhibiting `dχ(r)` there as a **derivation of the Jordan algebra annihilating every frame atom** (`EJA.dChi_jordanDeriv`, `EJA.dChi_frameProj`). ★ **What remains is item (1) alone**: Yokota's Spin(8) triality faithfulness, carried as `IsAlbertModel`'s `block_injective` field. `MasterTheorem/Branches/Albert.lean`'s own docstring is explicit that this is a *cited* hypothesis and not derivable in the abstract interface — "`Stab` carries no Lie structure … so simplicity of `𝔰𝔭𝔦𝔫(8)` is not available to prove it internally". With `dChi_jordanDeriv` the object it is a statement *about* is now in the tree, which narrows the citation to exactly: a frame-fixing derivation of `H₃(𝕆)` acting trivially on one off-diagonal Peirce block is zero. That is still Yokota, and it is still open. Prior record: PARTIAL — ★★ **HALF THIS CELL'S RESIDUE WENT STALE 2026-08-23 and is struck here.** It read "no concrete exceptional carrier". There is one: `EJA/AlbertBridge.lean` gives `instEuclideanJordanAlgebraHermMatOctonion` (H₃(𝕆) as a Euclidean Jordan algebra), `toHermMat` identifying `h3O` with `HermMat (Fin 3) Octonion` as a linear equivalence carrying the Jordan product (`toHermMat_mul`) and the trace form (`toHermMat_hermIp`), and `finrank_hermMat_octonion = 27`. The exceptional carrier is concrete and live. ★ **What remains, exactly, and none of it is the carrier.** (1) `MasterTheorem.Master`'s `luders_albert_produced` concludes `T i j = 0` only from `hAlb : IsAlbertModel …`, whose single field `block_injective` **is** Yokota's Spin(8)-triality faithfulness, carried as an interface field because `Stab` has no Lie structure in the abstract interface — a citation, on the tree's own `ComparisonSetup` contract. (2) The row's conclusion `a·b = Q_{√a}b` needs the quadratic representation and a sequential product at EJA generality, which is the **same** `SequentialProductOnEJA` inhabitant rows 13 and 16 wait on; the square root itself is *not* the blocker (`EJA/Order.lean`'s `isSoS_iff_exists_sq` has produced one since 2026-08-22 — canonicity is what is missing). (3) Nothing instantiates the abstract skeleton at the concrete carrier, which needs a `DiagonalHomSetup` there, i.e. row 17's analytic machinery. ★ So this row's residue is now **one citation plus two shared interior blocks**, not a missing carrier. Prior record: PARTIAL (`MasterTheorem.luders_albert_produced` at skeleton level, from cited $\Spin(8)$ block injectivity; no concrete exceptional carrier) | — |
| 22 | `lem:orientation` | 1172 | lemma | On $\Mnsa{n}$, $n\geq3$, with $q$ rank-two and $p_k$ an orthogonal atom, $\mathcal{J}_{q,k}(x)=iz-i z^*$ ($z=qxp_k$) is a complex structure on the cross-coherence space depending only on $q,p_k,i$ and not on any splitting of $q$; it commutes with stabilizing inner automorphisms; and $\Ad_{a^{it}}\|_X=\exp(t(\log\lambda-\log\lambda_k)\mathcal{J}_{q,k})$. | FORMALIZED — ★★★ **CLAUSE (iii)'S RESIDUE IS CLOSED, 2026-08-24.** The row's standing gap was that $\Ad_{a^{it}}$ was known to rotate $X$ only when $a$ was **diagonal** — the tree could exhibit the required scalar action $a^{it}q=\lambda^{it}q$ only for the standard frame's own family (`torusU_framePair`). It now holds at an **arbitrary** spectral decomposition, by three new theorems. ★★ **(a) The general scalar action.** `HermitianMat.mat_cfc_mul_of_eigen` (`Hermitian/CfcPoly.lean`): for **every** real $f$ and **every** $q$ with $aq=\lambda q$ — no relation between $q$ and $a$'s own spectral family, no continuity, no positivity, and *no hypothesis $\lambda\in\sigma(a)$* — one has $f(a)\,q=f(\lambda)\,q$. Proof: a matrix spectrum is **finite**, so $f$ agrees on $\sigma(a)\cup\{\lambda\}$ with its Lagrange interpolant $p$; `cfc_congr` replaces $f$ by $p$; `mat_cfc_polynomial` turns $\mathrm{cfc}\,p$ into the matrix polynomial $p(a)$; and **`aeval_mul_of_eigen`** (with `pow_mul_of_eigen`) propagates the eigenrelation through $p$. Adding $\lambda$ to the interpolation nodes is what removes the membership hypothesis. ★★ **(b) The unitary, from the *real* calculus.** `cfcPhase a g := \cos(g(a))+i\sin(g(a))` (`Necessity/ComplexRowUnconditional.lean`) builds $e^{ig(a)}$ out of two `HermitianMat.cfc` applications, so **no unitary functional calculus is needed** and (a) applies to each summand: `cfcPhase_mul_of_eigen` gives $e^{ig(a)}q=e^{ig(\lambda)}q$. `cfcPhase_mul_conjTranspose` certifies it really is unitary ($\cos^2+\sin^2=1$ inside the calculus, via `sq_add_sq_of_commute`). ★★ **(c) The article's statement.** `adU_cfcPhase_eq_rotation` fires clause (iii) at $e^{ig(a)}$ for orthogonal idempotents $q,p$ with $aq=\lambda q$, $ap=\mu p$, giving rotation angle $g(\lambda)-g(\mu)$; and `adU_cfcPow_eq_rotation` specialises $g=t\log$ to the article's own $t(\log\lambda-\log\lambda_k)$, for an arbitrary $a=\lambda q+\sum_l\lambda_l p_l$. `torusU_framePair` survives as an independent check that at the standard frame $a^{it}$ **is** $U_t(r)$. ★ Note the mechanism `adU_eq_rotation_on_crossCoherent` never assumed unitarity of $u$, so the instantiation is strictly a specialisation of a stronger theorem. Prior record: **PARTIAL 2026-08-22, from ABSENT** — clauses (i) and (ii) at article generality, clause (iii) restricted. ★★★ **The "$X$ is a CARRIER the tree does not have" retraction in `WallCertificates/frame-geometry.lean` OVER-CORRECTED.** `main.tex:1176` defines $X=\{x: q\circ x=\tfrac12x=p_k\circ x\}$ — a **predicate** — and every clause is pointwise under it. `Necessity.IsCrossCoherent` is that predicate verbatim, and `Necessity.isCrossCoherent_iff` **proves** the article's identification of $X$ with the corner form $x=z+z^*$ rather than assuming it. **(i)** `orientationJ_corner`, `orientationJ_isCrossCoherent`, `orientationJ_sq`, for orthogonal idempotents $q,p_k$ at arbitrary $n$. The clause's remaining phrase — "depends only on $q,p_k,i$ and not on any splitting of $q$" — is discharged by the **form of the definition**, `orientationJ` taking $q$ and $p$ and nothing else, and **no theorem is stated for it**, because every Lean rendering is `rfl` and a `rfl` theorem here would record awkwardness and read as depth. **(ii)** `orientationJ_adU`, proved **unrestricted** — for every $x$, not only on $X$ — since the article's computation $q(uxu^*)p_k=u(qxp_k)u^*$ never uses the coherence condition; stronger than the article's clause. ★★★ **(iii) is why the row is PARTIAL, and the restriction is precisely nameable.** `adU_eq_rotation_on_crossCoherent` proves the article's own proof step at its own generality: any $u$ acting on $q$ and on $p_k$ by scalars whose ratio is a phase $e^{i\varphi}$ rotates $X$ through $\varphi$ — unitarity of $u$ is not even assumed, so the hypothesis is weaker than "unitary". What is **not** proved is that $a^{it}$ is such a $u$ for an arbitrary $a=\lambda q+\sum_{l\geq3}\lambda_lp_l$; that needs `cfc f a` composed with $q$ equal to $f(\lambda)\cdot q$ for a spectral projection $q$ of $a$, which this tree has only for the diagonal family. `torusU_framePair` supplies the identification at the standard frame and `adU_torusU_eq_rotation` **fires** clause (iii) there, so the hypothesis class is certified inhabited by composition rather than by inspection — but the passage from an arbitrary spectral decomposition is open, and that is a restriction to a special case. `blockHerm_isCrossCoherent` with `blockHerm_ne_zero` certify $X\neq\{0\}$. ★ **Placement, so nobody reads more into it:** nothing in this development consumes `lem:orientation` — the complex row reaches its conclusion by a different route (`frameTwistConst`, via `AdjAxis`) — so this is formalized as an article row, not as a dependency. | 5.1 |
| 23 | `prop:per-frame` | 1225 | proposition | On $\Mnsa{n}$, $n\geq3$, for each Jordan frame there is a single $t_F\in\mathbb{R}$, depending only on the unordered frame, with $\Theta_r=\Ad_{a(r)^{it_F}}$ for every $r$. | **FORMALIZED** — `Necessity.complex_perFrame_unconditional` | — |
| 24 | `lem:adjacent` | 1269 | lemma | For $n\geq3$, adjacent frames — differing by a rotation inside a rank-two block $q=p_1+p_2=p'_1+p'_2$ and sharing $p_3,\dots,p_n$ — have $t_F=t_{F'}$. | **FORMALIZED 2026-08-08** — `Necessity.frameTwist_eq_of_adjBlock`, at the article's *own* adjacency (`AdjBlock`: the connecting unitary is diagonal outside one index pair). The axis-adjacency version `frameTwist_eq_of_adjAxis` remains as the engine; `adjAxis_of_adjBlock` is the bridge ($n\geq3$ ⟹ a pair cannot exhaust the indices, so a shared axis exists). Relation pinned from both sides: `adjBlock_one_house_pair` (holds of a pair with $F\neq G$) and `not_adjAxis_one_house` (not total) | 5.2 ✓ |
| 25 | `thm:complex` | 1315 | theorem | On $H_n(\mathbb{C})=\Mnsa{n}$, $n\geq3$: there is a single $t\in\mathbb{R}$ with $a\cdot b=a^{1/2+it}b\,a^{1/2-it}$ for all effects. | **FORMALIZED** — `Necessity.complex_classification_unconditional` (+ `_ouNorm`), uniqueness as $\exists!$ | — |
| 26 | `lem:frame-connectivity` | 1331 | lemma | For $n\geq2$ the graph on Jordan frames of $\Mnsa{n}$ with $F\sim F'$ iff they share $n-2$ atoms is connected. | FORMALIZED — ★★★ **THE GIVENS SWEEP LANDED 2026-08-24** (`Necessity/Givens.lean`). `Necessity.adjBlock_connected`: for every `N ≥ 2` the article's graph — `F ∼ F'` iff they share all but two atoms — is connected. ★★ **The old cell's diagnosis was exactly right and is worth keeping**: `AdjBlock` is *strictly finer* than `AdjAxis` (all-but-two axes fixed vs. some axis fixed), so this does **not** follow from `adjAxis_connected`; it needs every unitary to factor into rank-two block rotations (Givens/Jacobi), not into the three axis-fixing Householder factors the tree already had. That factorization is now proved. ★★ **How it is organised, since the naive route is an entrywise case explosion**: the rotation is written `G = (1 − P) + B` with `P = E_ii + E_jj` the block projection and `B` supported in the block, so unitarity is three `single`-algebra identities (`P·B = B`, `Bᴴ·P = Bᴴ`, `Bᴴ·B = P`) rather than a four-way index split, and `match_scalars` discharges the scalar side. The sweep is a **double induction**: `exists_clear_col` runs over the rows of one column, `isBlockProd_of_colsCleared` over the columns. ★ **The invariant is what makes it work**: the sweep only ever rotates in planes disjoint from the already-cleared set `S`, so cleared columns are never disturbed; and the entries of a *new* column at rows in `S` vanish for free by orthogonality of the columns of a unitary (`cleared_col_orthogonal`), not by rotation. ★ `IsBlockProd` packages "moves every frame along a walk" without a subgroup, and inversion comes from symmetry of the walk relation rather than from closure properties. Non-vacuity of the relation is `Necessity.adjBlock_one_house_pair` (already in the tree). Prior record: PARTIAL (connectivity for axis-adjacency via a Householder factorization, `adjAxis_connected`; the article's graph open) — **and this row does NOT come along with `lem:adjacent`**: `AdjBlock` is *strictly finer* than `AdjAxis`, so  | 5.2 banked |
| 27 | `prop:singular` | 1356 | lemma | In each type row the stated formula extends from invertible to all effects by S2-continuity. | **FORMALIZED** — `MasterTheorem.prop_singular`, applied by both flagship rows | — |
| 28 | `lem:twist-sufficiency` | 1386 | lemma | For every $n\geq2$ and $t\in\mathbb{R}$ the twisted product $\circ_t$ is a norm-continuous S1–S7 sequential product on the effects of $H_n(\mathbb{C})$. | **FORMALIZED** — `HermitianMat.twistSequentialProduct`, all seven clauses, S2 in both norms | — |
| 29 | `prop:n2-necessity` | 1488 | proposition | For a norm-continuous S1–S7 product on $\Mnsa{2}$ and each ordered frame $(P_n,P_{-n})$ there is a single real $\widetilde t(n)$ with $\Theta_a\|_{W_n}=\exp(\ell\,\widetilde t(n)\,\mathcal{J}_n)$, $\ell=\log(\lambda_+/\lambda_-)$ — one parameter per ordered frame, independent of which diagonal effect is used; no cross-frame constancy follows. | **FORMALIZED 2026-08-22** — gap (b) closed, and closed **in the article's own $\mathcal{J}_n$**. `Necessity.n2_theta_eq_rotation`: for an invertible effect $a=\Ad_U(\mathrm{diag}(e^{r_0},e^{r_1}))$, the comparison map on that frame's coherence block is $\cos\varphi\cdot\mathrm{id}+\sin\varphi\cdot\mathcal{J}_n$ with $\varphi=\widetilde t(U)(r_0-r_1)=\ell\,\widetilde t$ — i.e. $\exp(\ell\,\widetilde t(n)\,\mathcal{J}_n)$ written in the $\cos+\sin\cdot\mathcal{J}$ form the article itself uses, licensed here by `orientationJ_sq`. ★★ **Both of the article's objects are the article's own, not stand-ins.** $W_n$ is `Necessity.IsCrossCoherent`, and $\mathcal{J}_n(x)=iP_nxP_{-n}-iP_{-n}xP_n$ (`eq:J-ordered`, `main.tex:1468`) is `Necessity.orientationJ`, with `orientationJ_mat_eq` putting the article's formula on the page. So rows 22 and 29 are no longer about two unlinked encodings — the failure mode this file already records for rows 30/31. `adU_blockHerm_isCrossCoherent` certifies $W_n$ is inhabited. **Route:** `Necessity.theta_eq_conjLinear_torus` identifies $\Theta_a$ with conjugation by $U\cdot U_t(r)\cdot U^*$, the $Q_{\sqrt a}^{-1}$ half cancelling the twist factor's modulus exactly; it rests on `seqLeftMul_eq_conjLinear_twistFactor`, which pins the left multiplication **everywhere** rather than only on effects — necessary, since $W_n$ contains no nonzero effect. ★ **Not proved here, and inherited:** the descent of `n2FrameTwist` from $U(2)$ to the frame is gap (a)'s fibre-constancy (`n2FrameTwist_eq_of_frameMap_eq`), and `n2_every_posDef_effect` is what makes the $(U,r)$ form cover every invertible effect. Superseded record follows: PARTIAL — **substantially advanced 2026-08-08**: the frame function is now extracted from an *arbitrary* product (`Necessity.n2FrameTwist`) and the product is proved equal to the twist product with that parameter at every ordered frame (`n2_sp_eq_twistSeq_frame`). The lifting step is **discharged** — `Necessity.tvalLm` is a constructed ℝ-linear functional with no rank hypothesis, so nothing is assumed; the article's universal-cover argument is replaced by the exact fact that a linear functional on ℝ² vanishing on the diagonal factors through $r_0-r_1$. `n2_every_posDef_effect` records that the $(U,r)$ form covers **every invertible effect**, as the article states it. Of the two gaps the arc-5 cold review identified — and it was right that they are not merely presentational — **gap (a) is now CLOSED (2026-08-08, ARC-6 rung 6.1a)**: `Necessity.n2FrameTwist_mul_diagonal` proves the frame function constant on the diagonal-phase fibres of $U(2)\to S^2$, so the frame function is **constant on the diagonal-phase fibres**, and with `n2FrameTwist_reverse` also swap-invariant. ★ **Wording narrowed 2026-08-08 after the third cold review:** what Lean proves is invariance under right multiplication by the diagonal torus and by the swap. That those cosets *exhaust* the stabilizer of the unordered frame — hence that the function descends to a point of $\mathbb{RP}^2$ — is true (monomial matrices are exactly that stabilizer at rank two) but is **prose, not a Lean statement**, and no $S^2\to\mathbb{R}$ or $\mathbb{RP}^2\to\mathbb{R}$ object is constructed for an arbitrary product. Never say Lean "has a function of the ordered frame". Both come from one engine, `n2FrameTwist_eq_of_base_eq` (same base points ⟹ same parameter), and neither needs a $2\pi$ argument. ★ **The descent inference is itself machine-checked now** (2026-08-08, prompted by the third cold review asking whether the two generators suffice): `n2FrameTwist_mul_diagonal_swap` proves invariance under `U ↦ U·D·swap`, and for $\mathrm{Fin}\,2$ the monomial group is generated by the diagonal torus and the single transposition, so that is the *full* stabilizer of the unordered frame — not merely two of its elements. **Gap (b) remains**: the article's conclusion is about $\Theta_a\|_{W_n}$, Lean's is the product-level identity, and their equivalence is the route rather than a proved statement | 6.1a ✓ |
| 30 | `prop:n2-sufficiency` | 1580 | proposition | For every continuous $t:\mathbb{RP}^2\to\mathbb{R}$ the operation $a\cdot b:=a^{1/2+it_a}b\,a^{1/2-it_a}$ ($t_a=t(\mathrm{fr}\,a)$, $0$ for scalar $a$) is a norm-continuous S1–S7 sequential product on $\Mnsa{2}$. | **FORMALIZED 2026-08-09 (ARC-8 block 8.1(b))** — the whole row, S1–S7 including S2, for an arbitrary continuous `t`. Detail: `RankTwo.n2SequentialProduct` is a `SequentialProductOn (HermitianMat (Fin 2) ℂ)` built from an **arbitrary** `t : C(ℝP², ℝ)`, so S1 and S3–S7 hold at the article's own generality, and `RankTwo.exists_sequentialProduct_of_continuous_moduli` states it in the article's existential form with the operation pinned: at every spectral effect `Ad_U(diagFamily r)` the product **is** the twist product with parameter `t` evaluated at that effect's frame, where the frame is the tree's own `blochFrame (colFrame U)` — the ℝP² image of the first-column ray — not a fresh lookalike (`blochHerm_frameMap`, `blochHerm_adU_diagFamily`, `n2Tau_adU_diagFamily`). **Mechanism, and it is not the certificate's route:** a Hermitian $2\times2$ is $\alpha\mathbb{1}+n\cdot\sigma$ and its unordered spectral frame IS the ℝP² point of $n$; two such matrices commute exactly when their axes are parallel, and the direction this row runs on is the entrywise identity `blochHerm_parallel_of_commute` (the three components of $n_a\times n_b$ are the $(0,0)$ and $(0,1)$ entries of the commutator) — no eigenvectors, no simultaneous diagonalization. ★ **Direction pinned 2026-08-22:** that lemma, and its two consumers `blochPoint_eq_of_commute` / `n2Tau_eq_of_commute`, all take `Commute` as a **hypothesis**; the converse (parallel axes $\Rightarrow$ commuting) is not in the tree, and nothing here needs it. The biconditional is true mathematics, not a formalized statement. The certificate's two load-bearing worries are both answered: "compatible ⇒ same frame ⇒ same parameter" is `n2Tau_eq_of_commute`, and the scalars cost nothing because `HermitianMat.twistSeq_smul_one_left` shows a scalar left argument acts by scalar multiplication *for every* parameter — so the article's convention $t_a=0$ there is free rather than a case to check. Positive homogeneity (`twistSeq_smul_left`), needed for S5 at a scalar left argument, is a **corollary of the constant-parameter S5**, so this file needs no functional-calculus scaling identity anywhere. ★★★ **CORRECTED AT CHECKPOINT 2, and the correction matters more than the original.** Checkpoint 1's non-collapse theorems (`exists_n2Sp_tau_ne_twistSeq` and friends) are true, but a reviewer found their **separating witness is not an effect**: the probe takes $\delta = \pi/(t_1-t_2)$, positive whenever $t_1>t_2$, so the first argument has an eigenvalue $e^\delta>1$ and lies outside the effect interval. Since S1–S7 and the article's operation constrain **effects only**, those statements separate the *total extensions*, not the operations. The statement to cite is now `RankTwo.not_forall_effects_eq_twistSeq` / `not_forall_effects_tau_eq_twistSeq`, which are at the effects and go through `n2FrameTwist_unique_param` (whose quantifiers are already $r\leq0$ and `IsEffect b`) rather than an entry probe. ★ **Transferable: a separation theorem is only as strong as the class its witnesses live in** — "∃ a b" with no `IsEffect` guard reads as a separation of operations and is a separation of extensions. ★★ **ALSO CORRECTED AT CHECKPOINT 2 (fidelity):** S2 above is proved in the carried **Frobenius** topology, and the article's S2 is the **order-unit** norm. The bridge was already generic in the tree (`Necessity.firstArgContinuousOu_iff`) but nothing connected the rank-two product to it; `RankTwo.n2SequentialProduct_firstArgContinuousOu` now does, so "norm-continuous" here is the article's norm. ★ A related pin nit, recorded not fixed: `PaperA/AuditPins.lean` freezes the *class-side* S2 predicate, not the `SequentialProductOn` spelling this row proves; the two bodies were read and are identical, but the pin does not cover this spelling. ★★★ **NON-COLLAPSE, added at checkpoint 1 because a cold reviewer named its absence the most dangerous thing about this row.** Everything above certifies that the *axioms* hold; nothing above ruled out that `n2Sp t` is secretly some single `twistSeq s`, in which case the row would carry none of its intended force. And the tree's existing frame-dependence theorem could not supply it: `RankTwo.sp_luders_ne_unit_twist` is `thm:qubit-boundary`(iii) in the **entry-level encoding** `MasterTheorem.RankTwo.sp = Fdiag·b·Fdiagᴴ`, and **no theorem in the tree identified that family with `HermitianMat.twistSeq`** — so rows 30 and 31 were statements about two objects the tree never linked, and a reader combining them would have concluded that the qubit's escape from `mthm:master` was machine-checked when the two halves did not meet. Now proved, in the `twistSeq` encoding: `RankTwo.exists_twistSeq_diagFamily_ne` (the twist product separates its parameter — choose the spectral gap so the phase difference is exactly $\pi$, so no $2\pi$ ambiguity survives), `RankTwo.surjective_colFrame` (every $\mathbb{C}P^1$ point is a unitary's first-column ray, via `Necessity.frameU`), hence `RankTwo.exists_n2Sp_ne_twistSeq_of_nonconstant` and `RankTwo.exists_n2Sp_tau_ne_twistSeq` — for the article's own $\tau$ (which is defined *through* `MasterTheorem.RankTwo.tau`), the product is not any constant twist product. ★ Scope: this is "not **literally** a constant twist". The article's clause (iii) is stronger — no pair $(\Phi,t)$ with $\Phi$ a unital order automorphism conjugates it to a constant twist — and that is not proved. ★★ **Two further scope notes from the same review, recorded so they are not overread:** (a) the operation is pinned on the **open positive-definite cone** (`adU U (diagFamily r)` is exactly the posdef effects, `Necessity.eq_adU_diagFamily`) and the statement is silent at singular effects and at non-psd first arguments — strong enough to exclude `badP`-style witnesses, but not a statement about the whole effect set; (b) the frame identification is proved at posdef effects with **distinct** eigenvalues, and — ★★ **this clause is STALE as of 2026-08-22 and is struck** — it said `blochFrame` "appears nowhere in `Necessity/`, so nothing ties row 30's frame coordinate to row 29's `n2FrameTwist` (both use column 0 / `frameProj 0`, but that agreement is prose)". **Four theorems now tie them**: `n2Tau_adU_diagFamily` (`RankTwo/Sufficiency.lean:678`) at distinct eigenvalues, `n2Sp_eq_twistSeq_at_frame` (`:693`) extending it **unconditionally in `r`** with degenerate and scalar spectra handled in an explicit branch, `frameRP2 U := blochFrame (colFrame U)` (`:1172`), and they meet at `sp_eq_n2Sp_of_moduli` (`:1449`). **The agreement is no longer prose.** ★ Found in passing by an adversarial pass aimed at a different row, which is the second time an absence claim in this file was refuted by someone not looking for it. ★★★ **S2 LANDED THE SAME DAY, so this row is FORMALIZED**: `RankTwo.n2SequentialProduct_firstArgContinuous`, and `exists_sequentialProduct_of_continuous_moduli` now carries the `FirstArgContinuous` conjunct — i.e. the word "norm-continuous" in the article's statement. The wall certificate's `sorry` for this row is **discharged**; that file is down to row 35's gap alone. ★★ **AND THE CERTIFICATE'S PREDICTION ABOUT WHY S2 IS HARD WAS WRONG IN THE USEFUL DIRECTION.** It said S2 "needs the deviation to vanish fast enough at the scalars, which is a genuine estimate rather than plumbing". There *is* one genuine analytic input — **joint** continuity of the twist product in (parameter, matrix), via the vendored `continuous_cfc_joint` plus joint continuity of $\sqrt{x}\cos(s\log x)$ where the squeeze at $x=0$ is uniform in $s$ — but the predicted near-the-scalars comparison is **not needed at all**: at a scalar the product does not depend on the parameter (`twistSeq_smul_one_left`), so the function is *constant in the parameter* there, and joint continuity plus compactness of $[-K,K]\times\text{effects}$ (Heine–Cantor; the effect set's compactness was already in the tree, and $K$ exists because ℝP² is compact) gives a modulus of continuity in the matrix that is **uniform in the parameter**. **The parameter never has to converge.** The same scalar lemma that made the algebra free made the analysis free. ★ Transferable: a certificate can correctly identify *which* clause is hard and still be wrong about *why*, and the "why" is what a reader prices from. ★ Two scope facts about the *operation-pinning* clause survive unchanged (they are about fidelity, not about S2): it is pinned on the open positive-definite cone, and the frame identification is proved at posdef effects with distinct eigenvalues | 5.3 |
| 31 | `thm:qubit-boundary` | 1718 | theorem | With $\tau(a)=(2\operatorname{tr}(PR)-1)^2$ for the spectral frame of $a$, the family $a\cdot b=a^{1/2+i\tau(a)}b\,a^{1/2-i\tau(a)}$ (i) maps effects to effects with the stated rotation on $W_n$, (ii) satisfies all seven axioms, and (iii) is genuinely frame-dependent — no $(\Phi,t)$ conjugates it to a constant twist, so the complex conclusion of `mthm:master` fails at rank two. | FORMALIZED — ★★★ **CLAUSE (i)'S COHERENCE HALF IS PROVED, 2026-08-24, and the row returns from the 2026-08-22 demotion.** The demotion was correct and its diagnosis was exact: clause (i)'s second half — `main.tex:1720–1758`, that on the *ordered* spectral frame of a nonscalar $a=\lambda_+P_n+\lambda_-P_{-n}$ the coherence action on $W_n$ is $\sqrt{\lambda_+\lambda_-}\exp(\ell\,\tau(a)\,\mathcal{J}_n)$, $\ell=\log(\lambda_+/\lambda_-)$ — was not in the tree, for **three encoding reasons** the cell below enumerated at source. `RankTwo.n2Sp_coherence_action` (`RankTwo/Sufficiency.lean`) now states it, and each of the three is settled: ★★ **(1) family** — the statement is in the `twistSeq` encoding row 30 uses, via `Necessity.twistSeq_diagFamily_blockHerm`, *not* the entry-level `MasterTheorem.RankTwo.sp`; ★★ **(2) frame** — it is at $\Ad_U$ of the standard frame with the parameter slot filled by $\tau(a)$ itself, because `n2Sp t a` **is** `twistSeq (n2Tau t a) a` by definition, and `Necessity.twistSeq_adU_mat` together with the new `Necessity.orientationJ_conj` (unitary covariance of $\mathcal{J}$, distinct from `orientationJ_adU`, which is about a $u$ that *fixes* $q,p$) transports both sides; ★★ **(3) vocabulary** — the conclusion is written in `Necessity.orientationJ`, the article's $\mathcal{J}_n$, and the link to the scalar coefficient is the new `Necessity.orientationJ_frameProj_blockHerm` ($\mathcal{J}$ **is** multiplication by $i$ on the block coordinate) plus `Necessity.blockHerm_rotation` ($\cos\varphi+\sin\varphi\,\mathcal{J}=e^{i\varphi}$ there). The ordered frame is passed as four defining hypotheses ($a=\Ad_U\mathrm{diag}$, $q=\Ad_Up_0$, $p=\Ad_Up_1$, $x=\Ad_U$ of the block) rather than reconstructed inside the statement, so the theorem reads as the article does. ★★ **The Lüders end is proved too, not waved at.** $\lambda=e^r$ cannot reach $\lambda_-=0$, so `RankTwo.n2Sp_coherence_action_singular` states that case at the singular effect itself: a frame atom annihilates the coherence block for **every** value of the frame function (`Necessity.twistSeq_frameProj_blockHerm`, resting on `twistFactor_of_idem`: at a projection the twist factor *is* the projection, whatever $t$, since the calculus sends $1\mapsto1$ and $0\mapsto0$). That in turn falls out of `HermitianMat.mat_cfc_of_idem`, itself a two-line consequence of row 22's `mat_cfc_mul_of_eigen` — the same theorem proved earlier the same day. ★ **On the rendering of $\exp(\varphi\mathcal{J})$**: the conclusion is $\cos\varphi\cdot x+\sin\varphi\cdot\mathcal{J}x$, which is what $\exp(\varphi\mathcal{J})$ *means* when $\mathcal{J}^2=-\mathrm{id}$ (`orientationJ_sq`), and is the same rendering row 22's `adU_eq_rotation_on_crossCoherent` uses for the same article formula — so the two rows are now in one vocabulary rather than two. ★ Clauses (ii) and (iii) are unchanged and stand as recorded below. Superseded record follows, and its clause-(ii) and clause-(iii) content STANDS: **FORMALIZED 2026-08-22** — clause (iii) closed in the article's **literal $(\Phi,t)$-conjugation form**, and the row's other recorded residue was a misattribution. `RankTwo.not_exists_jordanAuto_const_twist`: no pair $(\Phi,t)$ with $\Phi$ a Jordan automorphism of $V$ and $t$ constant reproduces the $\tau$ family on effects. $\Phi$ is taken linear, Jordan-multiplicative and **surjective** — over a finite-dimensional space surjectivity gives bijectivity, so that is exactly the article's "automorphism" — and $\Phi^{-1}$ is eliminated from the statement by applying $\Phi$ to both sides, which is equivalent because $\Phi$ is bijective. ★★★ **The order-automorphism reading would have been WEAKER than the article's, not stronger, and that is the trap this row was sitting in.** The tree's only implication ran `orderAuto_preservesJordan` (order implies Jordan), so a negated existential quantified over unital order automorphisms does **not** deliver the article's negated existential over Jordan automorphisms. The missing direction is supplied here — `Necessity.jordanAuto_orderAuto`, resting on `Necessity.nonneg_iff_exists_symmMul` (the cone **is** the set of Jordan squares) — and `Necessity.preservesJordan_iff_orderAuto` records that for a unital surjective linear map the two readings coincide, retiring the open choice the ARC-8 pricing flagged. Non-vacuity: `exists_jordanAuto_const_twist_of_twistSeq`. ★★★ **RESIDUE CORRECTION, VERIFIED AT SOURCE:** "the unimodular cocycle subcases" listed below **are not part of this row**. The $\zeta$ case-verification is Step 6 ("S5; the phase cocycle") of the proof of `prop:n2-sufficiency`, running to `main.tex:1718` and closing immediately before the rank-two boundary theorem at `main.tex:1719` — i.e. it belongs to **row 30, already FORMALIZED**. `thm:qubit-boundary`'s own statement (`main.tex:1720–1758`) has clauses (i)/(ii)/(iii) and says nothing about a cocycle. With the misattribution removed and clause (iii) closed, the row has no residue. ★★★ **THAT SENTENCE IS FALSE AND IS THE ONE THE DEMOTION OVERTURNS** — the row's *recorded* residue was empty, which is not the same fact, and clause (i)'s coherence-action half was never in the record at all. Read the head of this cell. Superseded record follows: PARTIAL (parts (i) and (iii), plus the cocycle and backward compatibility; the bundled S1–S7 verification of the $\tau$ family and the unimodular cocycle subcases open) — ★★ **clause (ii) ADVANCED 2026-08-09 (ARC-8 block 8.1(b)/checkpoint 1)**: `RankTwo.n2SequentialProduct RankTwo.tauModuliRP2` supplies S1 and S3–S7 for the article's own $\tau$ family, since `tauVec` is defined *through* `MasterTheorem.RankTwo.tau` (`RankTwo/FrameFunction.lean`) and `tauRP2_blochFrame` bridges the two presentations. **S2 for it is now IN**, since row 30's S2 is proved for an arbitrary `t` and `tauModuliRP2` is one — so clause (ii) of this row ("satisfies all seven axioms") is complete for the article's own $\tau$ family. ★★★ **And the ENCODING SPLIT this row shared with row 30 is now closed on the frame-dependence side**: clause (iii) previously existed only as `sp_luders_ne_unit_twist`, about the entry-level family `MasterTheorem.RankTwo.sp`, which no theorem identified with `HermitianMat.twistSeq`; `RankTwo.exists_n2Sp_tau_ne_twistSeq` is now clause (iii) in the `twistSeq` encoding. The two rows finally speak about one object. Residue: the unimodular cocycle subcases, and clause (iii) in the article's stronger $(\Phi,t)$-conjugation form | 5.3 |
| 32 | `lem:n2-bounded` | 1788 | lemma | For any norm-continuous S1–S7 product on $\Mnsa{2}$, $M:=\sup_{n\in S^2}\lvert\widetilde t(n)\rvert<\infty$. | **FORMALIZED 2026-08-09 (ARC-7 block 7.1a)** — `Necessity.exists_n2FrameTwist_bound`: for every S1–S7 product with the article's S2 on $H_2(\mathbb{C})$ there is an $M>0$ with $\lvert\widetilde t(U)\rvert\le M$ at *every* frame. Carries S1–S7 + S2 and nothing else; no located hypothesis. ★★ **The indexing caveat this row carried is now DISCHARGED, and the checkpoint-1 review is why.** The caveat used to say fibre-constancy plus surjectivity of "the frame map" made this the article's frame-indexed statement. The reviewer found that **no map from $U(2)$ to any frame space existed anywhere in the tree** (grep scoped to the whole tree incl. `Vendor/`, 2026-08-09), so the surjectivity clause was prose about an undefined object; and that `n2FrameTwist_mul_diagonal`/`_reverse` prove invariance at two group *words*, not constancy on fibres. Both are answered by construction rather than by re-wording: `Necessity.frameMap` (a unitary to its first spectral projection — the article's frame, concretely), `Necessity.FrameSpace`, `Necessity.n2Moduli`, and **`Necessity.exists_n2Moduli_bound`**, which is the bound for the *frame-indexed* parameter. The enabling fact is `Necessity.n2FrameTwist_eq_of_frameMap_eq`: genuine fibre-constancy, from `basePt_mat_eq` ($\text{basePt}\,x = \id + (e^{-x}-1)\cdot\text{frameProj}\,0$), so equal first spectral projections force equal base points at every scale. ★ **The reviewer found this stronger fact already latent in `n2FrameTwist_eq_of_base_eq` and it deleted the expensive step of my own wall certificate** (which had priced the descent's first leg at ~200 lines of $\mathbb{C}^2$ ray algebra): one never needs rays if one descends along the projection. **Residual — THREE items, not one.** ★★ The sentence that stood here said "and it is the whole residue" and named only (a). The checkpoint-1 verification pass caught that, and the catch matters more than the omission: **it is the same construction that made the caveat this one replaced wrong** — the content got upgraded and a totalizing word survived the upgrade. A totalizing phrase in a residual claim is exactly where this project's errors live. **Two remain** (the third is closed). The three as originally listed: **(a)** ~~surjectivity onto all rank-one projections~~ **— DONE**. ★★ **CLOSED 2026-08-09** — `Necessity.exists_frameMap_eq_rankOne` (with `frameU`, `frameU_unitary`, `frameMap_frameU`): every unit-vector rank-one projection is `frameMap U` for the explicit unitary $!![\psi_0,-\bar\psi_1;\psi_1,\bar\psi_0]$. ~25 lines, no mathematics, and `RankTwo.orthoVec` is **not** needed. ★★★ **A RETRACTION ABOUT EVIDENCE, kept because it is the more useful half.** This row previously read "its substantive half is COMPILED" — recorded on a reviewer's word, and **false at the time**: that proof had an unsolved goal, Lean's error recovery inserted a `sorry`, and the top-level theorem carried `sorryAx`. The cause was reading `lake env lean` output through a `head` window already filled by earlier failures — **the truncating-pipe failure this project has a standing rule about, committed by the reviewer and then propagated by me without independent check.** The conclusion survived; the evidence did not exist when it was claimed. ★ Standing protocol for scratch work now: count errors over the FULL output (never a `head` window) and `#print axioms` the final theorem looking for `sorryAx`. For the library this is automatic and stronger — `AxiomAudit` requires every tracked closure to lie in `[propext, Classical.choice, Quot.sound]`, which excludes `sorryAx` — which is why the tree was never at risk even while a claim about it was wrong. ★ Splitting note that survives: what this gives is surjectivity onto $\{\mathrm{rankOne}\,\psi : \lVert\psi\rVert=1\}$; "onto *all* rank-one projections" would need a rank notion or the spectral theorem, and **that is avoidable** by phrasing the identification against `RankTwo.QubitFrame`, which is *defined* as rays and is where `blochFrame`/`tauModuli`/`RP2` already live. The construction is the explicit unitary `!![ψ₀, -conj ψ₁; ψ₁, conj ψ₀]`, and `frameMap` of it really is `rankOne ψ` (compiled). ★ It also splits: what that gives is surjectivity onto `{rankOne ψ : ‖ψ‖ = 1}`, and "onto *all* rank-one projections" would additionally need a rank notion or the spectral theorem — **but that second half is avoidable**, because the target one actually wants is `RankTwo.QubitFrame`, *defined* as rays, so phrasing the identification against it makes the extra step evaporate and matches where `blochFrame`/`tauModuli`/`RP2` already live. Not landed: the assembly hit simp-normal-form friction on the two unitarity goals (both verified in isolation), so this is **banked as confirmed-cheap rather than claimed**; **(b)** a *homeomorphism* from the rank-one projections to $S^2$ (or to the tree's ray-based `QubitFrame`/`RP2`). Surjectivity alone buys a set-level identification; transporting continuity needs the topological one — cheap (compact-to-Hausdorff again) but not free, and `grep -rn Homeomorph RadicalRelativity/` returns **zero** hits, whole tree incl. `Vendor/`; **(c)** for $\mathbb{RP}^2$ specifically — i.e. row 34's sentence, not this row's — the further quotient by $p\mapsto\id-p$. The frameMap-level identity `frameMap (V·swapU) = 𝟙 − frameMap V` is not in the tree; its ray-language analogue is `RankTwo.rankOne_orthoVec` and the matching invariance is `n2FrameTwist_reverse`. ★ Also verified by that pass: `FrameSpace` carries exactly the subspace topology (no instance diamond — an honest restatement over `{q // q ∈ Set.range frameMap}` typechecks with no bridging), the descent is **not vacuous** (`frameMap_swap_ne_one`, so the space has at least two points and is a continuum as the image of compact connected $U(2)$), and `n2Moduli_toFrameSpace` is the right well-definedness certificate — not because `Classical.choose` could be ill-defined (proof irrelevance forbids that) but because the constructed function could have agreed with `n2FrameTwist` at the chosen preimage **and nowhere else**, which is the failure mode that would have made the continuity meaningless. `n2_sp_eq_twistSeq_n2Moduli` now ties `n2Moduli` back to the product directly. ★★ **The route is not the article's**: the article argues by contradiction from operator-norm continuity of $a\mapsto\Theta_a$ in the matrix argument; this never mentions $\Theta$, never chooses a branch of a logarithm, and uses compactness of $U(2)$ at exactly one point (the weight bound). ★★ **Both of the project's own pricings of this row were wrong, in opposite directions.** "One uniform-continuity step, nothing but plumbing" (ARC-6) was too cheap and was retracted; "four steps" (2026-08-09, after the review that retracted it) was too expensive — the real count is **three**, and *none of them is a uniform-continuity step*. The step that fell away is "assemble continuity of `n2Comb`": the argument needs only a two-point comparison at $x$ against $x=0$, which the one-point S2 argument delivers directly, so `continuousOn_n2Readout` is **not on the critical path** (ARC-7 review, confirmed by the compiled proof). The three real steps, as executed: (i) `exists_uniform_sp_close` — S2 at the *single* effect $a=\id$ yields a scale valid at every frame, because conjugation is a Frobenius isometry (`norm_adU`, new, by trace cyclicity) so the frame drops out of $\lVert \mathrm{Ad}_U(\text{basePt}\,x)-\id\rVert$; **no compactness, and not even the explicit value of that norm** — continuity of `basePt` at $0$ plus `basePt 0 = \id` suffices; (ii) `norm_phase_sub_one_le` — stripping the modulus, the genuinely analytic step that had been missed, bridging $\lVert\sqrt{e^{-x}}e^{-itx}-1\rVert$ to $\lVert e^{-itx}-1\rVert$ for the cost of a defect `exists_sqrtExp_close` makes small; (iii) the budget spent in order — $\varepsilon:=\varepsilon_0/(4(C+1))$ chosen *after* the weight bound, each defect eating a quarter. ★ Two earlier plan errors stay visible: there is no "product metric" argument available (`PseudoMetricSpace` does not synthesize on `Matrix.unitaryGroup`, only `UniformSpace` does) and **none is needed** — the uniformity is algebraic, not a compactness or uniform-continuity fact; and `Ici 0 ×ˢ univ` is not compact, which is why nothing here extremizes over it. ★ A reviewer's advice to route the ε–δ extraction through `ouNorm` (`Necessity/OrderUnitS2.lean`) was **not taken**, and the checkpoint-1 pass confirmed the rejection while **sharpening the reason** — the original wording justified only *convenience* ("`Metric.continuousWithinAt_iff` applies directly"), which is the weaker argument. The actual certificate is that `Necessity.firstArgContinuousOu_iff` is an **iff on the hypothesis**: the article's order-unit-norm S2 and the tree's Frobenius-topology `FirstArgContinuous` are *equivalent*, so which side the proof works on cannot change what is proved. The reviewer also compiled the row with `FirstArgContinuousOu` as its literal hypothesis, so that restatement exists on demand — and advised **against** landing it, since `hS2` appears inside these rows' conclusions (unlike the flagship rows), making the converted form less readable than the iff it rests on. Not landed, for that reason. ★★ **`n2Readout_eq`'s `hb` and `hx` are now CERTIFIED load-bearing in the STRONG form** (`hb_is_load_bearing`, `hx_is_load_bearing`, via `badP` — a genuine S1–S7+S2 product agreeing with the twist product on effects and $0$ off them, contributed by the ARC-7 review). That discharges the ledger's whole "four hypotheses needing a bespoke product" debt: one construction certifies *any* hypothesis whose only job is to place an argument in the effect set. Also new: `sign_check`, a sign cross-check that bypasses `twistSeq_diagFamily_entry`, which `readout_direct` could not. Superseded record follows | 6.1b ✓ |
| 32′ | *(superseded status text for row 32, kept for provenance)* | — | — | — | ABSENT — **but every ingredient is now proved (2026-08-08, ARC-6); one uniform-continuity step remains.** The route taken avoids the article's entirely: rather than operator-norm continuity of $a\mapsto\Theta_a$, it reads the parameter off the product as a pure phase. Proved: `n2Readout_eq` (the readout identity, with an independent cross-check `readout_direct`); `continuousOn_n2Readout` (joint continuity in $(x,U)$ — the step that could have killed the route, and it survives because the test effect is *fixed*, so S2's first-argument-only continuity suffices); `n2Comb_eq` (the two-effect weighted readout equals weight $\times$ pure phase); `n2Weight_pos` and `exists_n2Weight_lower_bound` (the weight never vanishes and, by compactness of $U(2)$, has a positive minimum — via `frameProj_pairProj_not_commute`, `isDiag_of_herm_offdiag_zero`, `commute_of_isDiag`, entirely matrix-level); and `abs_lt_of_phase_near_one` (the numerical step, **sharp**, with its positivity hypothesis certified necessary by counterexample). ★ **One recorded step of the plan was REFUTED by the third cold review and is retracted:** there is no *fixed* test effect whose frame coefficient never vanishes — every Hermitian $b$ is blind at its own eigenframe — which is exactly why the construction uses a **pair** with no common eigenbasis. **Remaining — FOUR steps, not one; the "one step / no missing theorem" wording was retracted 2026-08-09 after the third cold review.** (i) ★ **strip the modulus**: `n2Comb_eq`'s factor is $\sqrt{e^{-x}}e^{-itx}$, so bounding the deviation controls $\lvert\sqrt{e^{-x}}e^{-itx}-1\rvert$, while the numerical step needs $\lvert e^{-itx}-1\rvert$ — a triangle inequality plus a $\delta$ small enough that $1-\sqrt{e^{-\delta}}$ eats only part of the budget. **Genuinely analytic, and absent from the tree.** (ii) state the comparison without a metric — `PseudoMetricSpace` does **not** synthesize on the unitary group (only `UniformSpace` does), so this is an entourage/filter argument, or better a tube-lemma argument from continuity at each $(0,U)$. (iii) assemble continuity of `n2Comb` itself (the sum of products; only the summands are done). (iv) restrict to the compact $\mathrm{Icc}\,0\,\delta_0\times\mathrm{univ}$ and choose $\varepsilon$ *after* the weight bound. ★ A reviewer banked a cheaper route for (ii): S2 at the single point $a=\id$ gives $\delta$ uniform in $U$ free, since $\lVert \mathrm{Ad}_U(\text{basePt}\,x)-\id\rVert=\lvert e^{-x}-1\rvert$ by Frobenius conj-invariance. ★ Mathlib's `continuousOn_cfc` is no longer needed on this route | 6.1b |
| 33 | `lem:n2-continuity` | 1820 | lemma | The ordered-frame parameter $\widetilde t$ is continuous on $S^2$. | **FORMALIZED 2026-08-09 (ARC-7 block 7.1b)** — `Necessity.continuous_n2FrameTwist`, carrying S1–S7 + S2 and nothing else. ★★ **This row's caveat was REFUTED by the checkpoint-1 review, and the refutation was correct — so the work was done instead of the wording changed.** The caveat said "same indexing caveat as row 32". That is **false**: for boundedness the transfer along a surjection is valid, but **continuity of a pullback along a bare surjection does not give continuity downstairs** — it needs a quotient map — and neither the map nor that property was in the tree. The reviewer recommended downgrading the label. Instead the row now holds for the frame-indexed function outright: **`Necessity.continuous_n2Moduli`**, via `Necessity.isQuotientMap_toFrameSpace` — and that is *free*, because $U(2)$ is compact (vendored in-tree) and the frame space is Hausdorff, so a continuous surjection between them is closed hence a quotient map. Same residual as row 32 (`FrameSpace` is the range, not yet identified with $S^2$). ★★ **The route is the article's ORDER but not its ARGUMENT.** The article needs a principal branch of $\mathrm{Log}$ on $SO(3)$ plus the trace formula $\widetilde t=-\tfrac{1}{2s}\mathrm{tr}_Y(\mathcal{J}_nK_n)$; this uses neither. Fix one scale $\delta$ with $M\delta=\pi/4$ ($M$ from row 32); at that fixed scale the combined readout exhibits $U\mapsto e^{-i\widetilde t(U)\delta}$ as an explicit **quotient of continuous functions** (`continuous_n2Comb_fixed` over the never-vanishing weight), hence continuous; the a priori bound then confines the exponent to $[-\pi/4,\pi/4]$, where `Complex.arg` is continuous and inverts the exponential, giving $\widetilde t=-\arg(\text{phase})/\delta$ (`continuous_of_continuous_phase`, stated generally). ★ **This is NOT the lifting problem this row previously refuted**, and the distinction is the content: the refutation was of recovering $\widetilde t$ from its phase *without* a bound (a discontinuous section of a covering need not equal a continuous lift; two incommensurable $\delta$'s pin the value but not continuity). **The bound removes the covering** — the phase never leaves one sheet, so `arg` is an honest inverse rather than a choice of branch. So this row's recorded pricing was RIGHT that boundedness gates it and RIGHT that all three bypass routes fail; what it under-stated is that once the bound exists the remainder is cheap. ★ `continuousOn_n2Readout` **is** load-bearing here, though the ARC-7 review correctly found it is *not* on the critical path of row 32 — same theorem, two rungs, opposite verdicts, which is why "is X needed?" must be asked per rung. Superseded record follows | 6.1c ✓ |
| 33′ | *(superseded status text for row 33, kept for provenance)* | — | — | — | PARTIAL (continuity of the frame function and of the $\mathbb{RP}^2$ moduli element for one concrete distinguished element; an arbitrary product's moduli function open) — now a statement about a function that **exists** (`n2FrameTwist`), which it was not before 2026-08-08. **Priced 2026-08-08 (ARC-6 rung 6.1c): this row cannot be taken before `lem:n2-bounded`, and the ARC-6 orders' suggestion to try the reverse order is refuted.** Three distinct routes were examined and all funnel through boundedness: (1) the article's — principal $\mathrm{Log}$ on $SO(3)$ plus the trace formula $\widetilde t = -\tfrac{1}{2s}\mathrm{tr}_Y(\mathcal{J}_n K_n)$, which needs $s M<\pi$ to have a principal branch at all; (2) solving for the parameter from the product values — `S2` makes $U\mapsto\exp(i\,\widetilde t(U)\delta)$ continuous, but recovering $\widetilde t$ from it is a lifting problem, and although fibre-constancy (6.1a) plus reversal now put the domain on the simply-connected $S^2$, a *non-continuous* section of a covering still need not equal a continuous lift; (3) two incommensurable $\delta$'s — pins the value but not continuity, since $t\mapsto(e^{it\delta_1},e^{it\delta_2})$ is a dense non-proper injection into $T^2$. Compactness of $U(2)$ **is** in-tree (`Vendor/Wigner/UnitaryCompact.lean`), so continuity would give boundedness for free — but not conversely, which is why the article's order is forced | 6.1c banked |
| 34 | `lem:n2-descent` | 1852 | lemma | $\widetilde t(-n)=\widetilde t(n)$ — the two sign changes cancel — so the frame parameter descends to a single continuous bounded $t:\mathbb{RP}^2\to\mathbb{R}$ with no residual line-bundle twist. | **FORMALIZED 2026-08-09 (ARC-8 block 8.1(d))** — ★ this cell led with "PARTIAL" for one commit after the row closed, because the update was APPENDED instead of rewriting the status word; caught by a keyword scan of the manifest's own status fields. That is the third instance this arc of "fix the row, not just the footnote", and the first committed by the same agent that wrote the rule down. Prior record: **advanced 2026-08-09 (ARC-7 block 7.1c): the descent's missing ingredient is now proved.** `RankTwo.blochFrame_eq_iff` — `blochFrame p = blochFrame q ↔ (p = q ∨ p = orthoFrame q)`: the Bloch map on the frame space is **exactly two-to-one with complementary fibres**. This is the converse of `blochFrame_orthoFrame`: the tree already knew `blochFrame` identifies a ray with its complement, and what a descent needs is that it identifies *nothing else*. With it, a complementation-invariant function on $\mathbb{C}P^1$ is constant on the fibres of `blochFrame`, which is precisely the hypothesis a quotient descent requires. Proof content: one positive-scalar computation (`exists_smul_of_blochVec_eq_pos_smul`), with the negative case free from `blochVec_orthoVec`; the step that makes it go is `blochVec_normSq` pinning the *scale* ($\lVert B(v)\rVert=\mathrm{nsq}\,v$), which converts "proportional" into $\|u_i\|^2=\mu\|v_i\|^2$ together with $\bar u_0u_1=\mu\bar v_0v_1$ — and those determine the ray. Also: boundedness and continuity of the frame function itself are now **both proved** (rows 32/33), so of the three things this row waited on (bounded, continuous, complementation-invariant) all three are in hand. ★★★ **ASSEMBLED 2026-08-09 (ARC-8 block 8.1(d)); THIS ROW IS FORMALIZED.** `RankTwo.n2QubitModuli P hS2 : C(ℝP², ℝ)` — continuous (`continuous_n2ModuliRP2`) and bounded (`exists_n2QubitModuli_bound`, automatic from ℝP²'s compactness rather than a separate estimate), with `n2QubitModuli_apply` giving `t(fr U) = ` the frame parameter and `sp_eq_twistSeq_n2QubitModuli` giving that it represents the product. The `t̃(-n) = t̃(n)` clause is the complementary-ray half of `n2FrameTwist_eq_of_blochFrame_colFrame_eq`. Mechanism: the presentation map `blochFrame ∘ colFrame : U(2) → ℝP²` is continuous and surjective from a **compact** group to a **Hausdorff** space, hence a quotient map, so a function on ℝP² is continuous exactly when its pullback is — and the pullback is `n2FrameTwist`, i.e. row 33. ★★ **AND THE PRICED-BUT-UNNEEDED STEP IS WORTH RECORDING.** This row's residue was "a map $U(2)\to\mathbb{C}P^1$, the step *same ray ⟹ the unitaries differ by a monomial matrix*, and the quotient construction". The monomial-matrix step is **not needed at all**: what the transfer requires is that equal rays give the same *first spectral projection*, which follows from `rankOne`'s quadratic homogeneity plus the fact that a unitary's column is a unit vector (`frameMap_eq_of_colFrame_eq`, `nsq_firstCol`). No statement about monomial matrices appears in the assembly. This is the third time this arc that a correctly-sized price named a longer route than the one that worked. Superseded record follows | 5.3 |
| 34′ | *(superseded status text for row 34, kept for provenance)* | — | — | — | PARTIAL — **the frame-reversal clause is now proved for an ARBITRARY product** (`Necessity.n2FrameTwist_reverse`, 2026-08-08: `n2FrameTwist (U * swapU) = n2FrameTwist U`), so the frame function is order-blind and is a function of the unordered frame. Also machine-checked for one concrete distinguished element: the complementation involution, invariance of the frame function and the Bloch map, and the $\mathbb{RP}^2$ bridge. Open: **continuity** of an arbitrary product's frame function, which is what the descent to a *continuous* $\mathbb{RP}^2$ function still needs | 5.3 |
| 35 | `cor:qubit-classification` | 1888 | corollary | $t\mapsto\circ_t$ is a bijection from $C(\mathbb{RP}^2,\mathbb{R})$ onto the norm-continuous S1–S7 products on $\Mnsa{2}$ — where a *product* is, by `def:sp` (`main.tex:367`, typed $\eff{V}\times\eff{V}\to\eff{V}$) and by `rem:moduli` (`main.tex:1417`, $\mathcal{M}_{\mathrm{SP}}(J)$ = the products on the fixed labelled **effect algebra**), an operation on effects, so two products are equal exactly when they agree on effect pairs. ★★★ **This qualifier was dropped from this cell until 2026-08-22 and the omission was consequential — see the status column.** | **FORMALIZED 2026-08-22** — the bijection, at the encoding this row's own statement column identifies as the article's. `RankTwo.restrictSp_n2_bijective`: $t\mapsto$ `restrictSp (n2SequentialProduct t)` is **injective** on $C(\mathbb{RP}^2,\mathbb{R})$, and every norm-continuous S1–S7 product $P$ has a **unique** $t$ with `restrictSp P = restrictSp (n2SequentialProduct t)`. `PaperA.restrictSp_eq_iff` proves the equality criterion the statement column states — two products are equal exactly when they agree on effect pairs — so the target really is products-as-effect-operations and not products-as-total-functions. On this carrier "norm-continuous" is the article's own order-unit-norm condition, by `Necessity.firstArgContinuousOu_iff`, and `n2SequentialProduct_firstArgContinuousOu` places the family inside the target. ★★★ **The `badP` obstruction dissolves, and is PROVED to dissolve rather than argued away**: `RankTwo.restrictSp_badP` shows `Necessity.badP t` and the constant-twist product — which differ as total operations, which is exactly what made "onto the products" false in the total encoding — restrict to the **same** `PaperA.EffectSequentialProduct`. So the recorded obstruction was an artefact of the encoding, and the article's "onto" half is true and now proved. ★★ **ONE STEP IN THE SURJECTIVITY READING IS UNSTATED, named 2026-08-22 so the cell stops claiming a seamless bijection; it does not move the status word.** `restrictSp_n2_bijective`'s second conjunct quantifies over `P : SequentialProductOn (HermitianMat (Fin 2) ℂ)` with `P.FirstArgContinuous` — a hypothesis on the **total** `P` — while its conclusion is about `restrictSp P`, an article-level product. Reading it as "onto the norm-continuous **article-level** products" therefore needs *S2 depends only on `restrictSp P`*: if `restrictSp P = restrictSp Q` then `P.FirstArgContinuous ↔ Q.FirstArgContinuous`. **This is true and it is one line** — `FirstArgContinuous` is `ContinuousOn (fun a => P.sp a b) {a \| IsEffect a}`, which sees `P.sp` only on effect pairs, so Mathlib's `ContinuousOn.congr` closes it — **and it is not in the tree**: `firstArgContinuous_congr` and every near spelling return **0 hits across the 167 files of `RadicalRelativity/`** (2026-08-22). ★ It is the exact analogue of residue (ii) in the superseded record below, at the level of the S2 hypothesis rather than the operation, and like (ii) it is generic rather than a row-35 fact. Superseded record follows: PARTIAL — ★★★ **substantially assembled 2026-08-09 (ARC-8 block 8.1(f))**, having been ABSENT that morning. All four of the inputs this row waited on are now in: boundedness and continuity of the frame function (rows 32/33), its ℝP²-descent (row 34, `RankTwo.n2QubitModuli`), and `prop:n2-sufficiency` including S2 (row 30). What is proved: `RankTwo.exists_unique_qubitModuli` — every norm-continuous S1–S7 product determines a **unique** element of $C(\mathbb{RP}^2,\mathbb{R})$ representing its frame parameter (this is verbatim the wall certificate's gap statement, now discharged, so that file has zero gaps); `RankTwo.qubit_classification` — the forward map `t ↦ ∘_t` is **injective**; and `RankTwo.n2QubitModuli_n2SequentialProduct` — the **round trip is the identity**, via `n2FrameTwist_unique_param` (the frame parameter is the *unique* real at which the product acts as a twist there). ★★★ **RETRACTED AT CHECKPOINT 2 — THE RESIDUE RECORDED HERE WAS WRONG, AND THE TREE ALREADY CONTAINED THE REFUTATION.** This cell said the residue was "the onto half at singular effects — and that is the whole residue", implying one writable limiting argument stood between the tree and the article's bijection. A cold reviewer showed the **onto half is FALSE, not unwritten**: `Necessity.badP t` is a genuine S1–S7 product with S2 that equals the twist product *on effects* and is `0` off them (it exists precisely because every `SequentialProductOn` axiom is `IsEffect`-guarded), so it has the **same** element of $C(\mathbb{RP}^2,\mathbb{R})$ as `n2SequentialProduct (const t)` and a **different** `.sp`. Now machine-checked in-tree: `RankTwo.moduli_collide`, `RankTwo.badP_sp_differs`, `RankTwo.not_exists_moduli_of_badP`. So `product ↦ moduli` is **not injective on `SequentialProductOn` values** and no bijection onto the products-as-`.sp`-functions can exist. ★ **The honest target is products UP TO AGREEMENT ON EFFECTS** — which is what the article means and what the axioms can see. ★★ **And the lesson is one already on this project's record: a totalizing phrase inside a residual claim is where the error lives.** "…and that is the whole residue" was the false part of the sentence, exactly as it was the previous time. ★ **A second, smaller correction from the same review:** the sentence below cited `sp_eq_twistSeq_n2QubitModuli` for "the products agree at every positive-definite first argument", but that theorem equates `P.sp` with the **constant-parameter** `HermitianMat.twistSeq`, not with `n2Sp`. The two-line bridge is now `RankTwo.sp_eq_n2Sp_of_moduli`, and *that* is the citation for the positive-definite half. **So the residue, restated:** agreement on effect × effect between `P.sp` and `n2Sp (n2QubitModuli P hS2)` — proved at positive-definite first arguments (`sp_eq_n2Sp_of_moduli`), open at singular ones. ★★★ **RESIDUE DISCHARGED 2026-08-12 (ARC-9 block 9.1).** The restated residue above — agreement at singular first arguments — is closed: `RankTwo.sp_eq_n2Sp_on_effects` proves `P.sp a b = n2Sp (n2QubitModuli P hS2) a b` for **every** effect `a`, and the honest corollary is assembled as `RankTwo.qubit_classification_up_to_effects` — distinct parameters differ on a pair of *effects* (`RankTwo.n2Sp_inj_on_effects`), and every norm-continuous S1–S7 product agrees on effects with `∘_t` for exactly one `t`. Closure: the three core axioms. ★★ **The cell said the S2 limiting argument was "not written". It had been written since `LEDGER.md` 2.9**: `prop:singular` is wired on this carrier as `Necessity.sp_eq_on_effects_of_eq_on_posDef`, stated for an arbitrary index type, already consumed by the ℂ flagship row at `N ≥ 3`, and rank-free. The missing thing was the *application*, eleven lines: `Necessity.eq_adU_diagFamily` presents a positive-definite effect as `Ad_U (diagFamily r)` and `Necessity.log_eigenvalues_nonpos` makes `r ≤ 0`, which is the shape `sp_eq_n2Sp_of_moduli` consumes. **Sixth row on this project whose status was wrong on the page rather than in the tree** (after `lem:aone`, `lem:span`, `prop:pseudo-transfer`, `lem:homomorphism`, `lem:homog`(i)) — and the first found by asking, of a residue naming a *hypothesis and a conclusion*, whether a lemma with that exact signature was already in the tree. ★ **This is why the row is still PARTIAL and not FORMALIZED**: the article's literal statement is a bijection **onto the norm-continuous products**, and `not_exists_moduli_of_badP` refutes that for this encoding — no limiting argument can repair it, since `badP` agrees with a twist product on every effect and differs off them, where no axiom looks. What is proved is the bijection onto products **up to agreement on effects**; promoting the row would require the manifest to restate it, which is a manuscript decision and not a Lean one. ★ **The retracted sentence formerly occupying this position** ("WHAT IS MISSING IS THE 'ONTO' HALF AT SINGULAR EFFECTS, and that is the whole residue") stood here for two days *after* the checkpoint-2 retraction printed two sentences above it, totalizing phrase and all — the appended correction, the un-rewritten claim, in the one cell whose own text names that failure mode. ★★★ **CORRECTION 2026-08-22 — THE 'REFUTED COROLLARY' READING OF THIS ROW WAS WRONG, AND THIS CELL CAUSED IT.** The sentence above says the article's literal statement "is a bijection **onto the norm-continuous products**, and `not_exists_moduli_of_badP` refutes that for this encoding". The Lean source says the same thing with the qualifier load-bearing — `RankTwo/Sufficiency.lean:1424` reads "is FALSE **for this encoding** of 'product'", `:1409` reads "differ at a **non-effect** first argument, where the axioms say nothing", and `:1521` says the restriction to effects "is not a weakening for convenience — it is **forced**". But this row's **summary column** dropped the effect-level typing, and read against that summary the row says the article is false. It is not. `badP`'s separating witness is `(2:ℝ) • 1`, proved a non-effect by `not_isEffect_two_smul_one`; the article never defines the operation there. **`cor:qubit-classification` is correct as written and no manuscript edit is owed on this account.** The summary column is now fixed. ★★ **The defect propagated**: from this cell into two agent briefings and into a report to Bryan, each time as "the manuscript contains a corollary its own formalization disproves". **This is the recorded failure mode of appending a correction and leaving the summary asserting the old thing, and here the summary that lied was the row's own statement column** — the one field the whole frozen-denominator apparatus treats as the article's voice. Audit the *summary* columns, not only the status prose. ★★★ **AND THE PROMOTION THAT LOOKED FREE WAS REFUTED AS A MECHANISM (2026-08-22, adversarial pass).** With the manuscript freeze lifted it was proposed that one clause in `cor:qubit-classification` making the effect-level typing explicit would promote this row to FORMALIZED for zero Lean work. **The clause is a no-op**: `rem:moduli` already defines $\mathcal{M}_{\mathrm{SP}}$ over the effect algebra, so either the article already means it (it does, and no edit is needed) or a clause "making explicit what `def:sp` implies" would not change it either — and a clause that genuinely did change it would be the illegitimate weakening this manifest exists to prevent. **There is no reading in which "add the clause, then promote" is the correct sequence, and blob `205fdf5a` is not spent on it.** ★★ **THE ROW STAYS PARTIAL, and the residue is now stated exactly.** (i) The corollary asserts an *explicit bijection of sets* with the display $\mathcal{M}_{\mathrm{SP}}(\Mnsa{2})\cong C(\mathbb{RP}^2,\mathbb{R})$; there is **no Lean `def` for $\mathcal{M}_{\mathrm{SP}}$ and no `Equiv` witness** — `∃!` plus injectivity is logically the bijection, but the term does not exist. (ii) **The total-vs-partial extension step, which is SYSTEMIC and was unrecorded anywhere until now.** `qubit_classification_up_to_effects` quantifies over `P : SequentialProductOn`, i.e. operations **total** on matrices with `IsEffect`-guarded axioms; the article quantifies over operations **partial** on effects. Concluding the article's "onto" from the Lean's needs every article-level product to be the effect-restriction of some `SequentialProductOn`. True by extension-by-zero — every field is `IsEffect`-guarded and `sp_effect` keeps the nested S5/S7 occurrences inside the guarded region, and `Necessity.badP` is literally an instance of the construction — but **the general statement is not in the tree and there is no Lean type for an article-level partial product at all.** ★★★ **(ii) IS NOT A ROW-35 FACT: the ℂ and ℝ flagship necessity rows have the same shape and are already FORMALIZED** (`complex_classification_unconditional`, `real_classification`, both taking `P : SequentialProductOn`). So it cannot be used to hold this row back without demoting those, and it is recorded here only because row 35 is where it became load-bearing. **Nothing is demoted on this finding.** The disposition is to close it once, generically — define the article-level partial product and prove extension-by-zero — which promotes this row cleanly and puts the flagship rows on a stated foundation instead of an unstated convention. What is explicitly REJECTED is promoting this row by editing the article | 5.3 |
| 36 | `cor:selectors` | 1995 | corollary | On $H_n(\mathbb{C})$, $n\geq3$, the complex-type product is Lüders ($t=0$) if any of: (i) Peirce exchange covariance, (ii) trace-form symmetry, (iii) covariance under every unital order automorphism (the transpose suffices on $\Mnsa{n}$). | **FORMALIZED 2026-08-22** — all three clauses, each carrying a hypothesis at or *weaker* than the article's, with no located hypothesis anywhere. Clause (i) is `Necessity.selector_peirceExchange` / `selector_peirceExchange_luders`: $N\geq3$, S1–S7 + S2, hypothesis `Necessity.PeirceExchangeCovariant`. ★★★ **The hypothesis is imposed on the coherence blocks of the STANDARD frame only, and that makes the theorem STRONGER, not weaker — it must not be recorded as a special case.** The article asks for $E(x,y)=E(y,x)$ on *every* coherence block; assuming it on fewer blocks is a weaker antecedent with the same consequent, so this theorem *implies* the article's clause (i) by restriction. ★★ **$E(x,y)$ is exhibited, not assumed**: `Necessity.twistSeq_diagFamily_blockHerm` proves the block action is multiplication by `blockCoef` $=\sqrt{xy}\exp(it\log(x/y))$, the article's own displayed formula, so the hypothesis is a statement about $E$ with the block's complex structure held fixed and **not** the relabelling of $p_i,p_j$ that `main.tex:1990–1994` explicitly excludes — which is what the wall certificate's `exists_peirce_exchange` had built. The enabling lemma is `seqLeftMul_eq_conjLinear_twistFactor`: $E$ lives on the *linear extension* $L_a$, necessarily, because a nonzero block element is never an effect (its two nonzero eigenvalues are $\pm\lvert z\rvert$). Non-vacuity is certified both ways — `luders_peirceExchangeCovariant` (Lüders satisfies it) and `peirceExchangeCovariant_forces_zero` (no other member of the twist family does). Clauses (ii) `selector_traceSymm` and (iii) `selector_transpose` were already at article generality; (iii)'s transpose-only hypothesis is the article's own "the transpose suffices". Superseded record follows: PARTIAL — **clause (iii) CLOSED 2026-08-09 (ARC-7 block 7.3)**: `Necessity.selector_transpose` / `selector_transpose_luders`, $N\geq3$, S1–S7 + S2, no other hypothesis. This is the article's *own* sufficient condition, not a weakening — the article states clause (iii) for every unital order automorphism and notes the transpose alone suffices on $\Mnsa{n}$, which is exactly the hypothesis carried. Mechanism: **transposition flips the twist** (`transposeMap_twistSeq`: $(a\circ_t b)^{\mathsf T}=(a^{\mathsf T})\circ_{-t}(b^{\mathsf T})$), so covariance forces the classified parameter to equal its own negative and the $\exists!$ from `complex_classification_unconditional` closes it. ★ The ARC-6 record predicted this was "assembly, not an ingredient", with the sign checked on paper — **the prediction held**, and the assembly is three short lemmas (`twistFactor_transposeMap`, `transposeMap_twistSeq`, `transposeMap_isEffect`). ★ One correction to that record: the sign flip does **not** come from $\cos$ even / $\sin$ odd directly, as banked; it comes from `twistFactor_conjTranspose` together with $(M^{\mathsf T})^{\mathsf H}=(M^{\mathsf H})^{\mathsf T}$, which makes the two outer factors trade places. ★★★ **"Two clauses of three are now proved; clause (i) alone remains" is CORRECTED 2026-08-22: all three clauses are proved.** Clause (i) closed the same day as `Necessity.selector_peirceExchange` / `selector_peirceExchange_luders` (`Necessity/ComplexRowUnconditional.lean`:607/:656) against `PeirceExchangeCovariant` (:593). The prediction recorded here held — it did need the coherence-block (Peirce exchange) action, a different piece of machinery from anything (ii) or (iii) used — and that action is now **exhibited** rather than assumed (`twistSeq_diagFamily_blockHerm`, `blockAction_entry`): the block coefficient is $\sqrt{xy}\exp(it\log(x/y))$ with the complex structure held fixed, so the hypothesis is the article's fixed-orientation condition and not the relabelling of $p_i,p_j$ the article rules out. It is imposed on the **standard frame's blocks only**, hence weaker than the article's every-frame condition, so it covers the article's statement. Non-vacuity is pinned from both sides (`luders_peirceExchangeCovariant`, `peirceExchangeCovariant_forces_zero`). ★ **This correction moves NO status word and NO coverage count.** The row stays PARTIAL and whether it should promote is a deliberate judgement to be taken separately — the candidates are clause (iii)'s "every unital order automorphism" versus the transpose hypothesis carried, and clause (i)'s standard-frame-only quantifier; both are the article's own sufficient conditions and **neither is an unbuilt lemma**. Do not restate any clause as open. Superseded record follows | 6.4 |
| 36′ | *(superseded status text for row 36, kept for provenance)* | — | — | — | PARTIAL — **clause (ii) proved 2026-08-08 at the article's own generality**: `Necessity.selector_traceSymm` / `selector_traceSymm_luders` ($N\geq3$, S1–S7, S2, no other hypothesis). Clauses (i) and (iii) open — but **(iii)'s sole missing ingredient is now proved (2026-08-08, ARC-6 rung 6.4)**: `Necessity.cfc_transpose_unconditional` / `transposeMap_cfc` give $(\mathrm{cfc}\,f\,a)^{\mathsf T}=\mathrm{cfc}\,f\,(a^{\mathsf T})$ **for arbitrary $f$ and arbitrary $a$**, via entrywise conjugation as an ℝ-star-algebra hom (`conjMatStarAlg`) plus `StarAlgHomClass.map_cfc`, using $a^{\mathsf T}=\bar a$ for Hermitian $a$. ★ The hypothesised `cfc_transpose` came first; the ARC-6 cold review then showed **both its hypotheses are inert** (outside its domain `cfc` is junk-valued at $0$ and degrades on both sides together, since `spectrum_transpose` gives $\mathrm{spec}\,a^{\mathsf T}=\mathrm{spec}\,a$) and supplied the compiled hypothesis-free proof, now adopted. What remains for (iii) is assembly only: $\mathrm{transposeMap}(a\circ_t b) = (\mathrm{transposeMap}\,a)\circ_{-t}(\mathrm{transposeMap}\,b)$ — which reduces to $\mathrm{twistFactor}(a^{\mathsf T})(-t) = \overline{\mathrm{twistFactor}(a)(t)}$, checked on paper via $\cos$ even / $\sin$ odd — and then the same $\exists!$ step that closed clause (ii) | 6.4 |

---

## Ceiling arithmetic

Of the 36: **2 are cited external results** (`thm:vdw1`, `prop:bridge`) that the paper does
not claim to prove; **2 more need JvNW** to be stated at the article's abstract generality
(`mthm:master`, `mthm:omnibus`); **1 is a cited rigidity step the paper does not reprove** (`prop:theta`) — ★★★ **this read "1 needs vIR's JB-algebra generality" until 2026-08-22, and that was FALSE against the manuscript.** `prop:theta` is stated on the article's own `J`, which its proof calls "a finite-dimensional JB-algebra"; the proof gets a *unital linear* order isomorphism out of van de Wetering's Prop 5.3 **first** and only then upgrades it, naming **Alfsen–Shultz** as the classical reference for exactly that step alongside vIR (`main.tex:775-789`). The theorem the row needs is the classical unital-linear one; vIR's JB-generality version is strictly stronger than the row's own statement requires. **The row's EXTERNAL status is a pre-registered decision and is unchanged — only the reason is corrected**; see `WallCertificates/external-rows.md`'s 2026-08-22 section; and
**1 needs the unscoped Albert M2 machinery** (`thm:albert` — the octonions themselves are
*not* the wall; see `LEDGER.md`'s `H₃(𝕆)` correction and `ALBERT-KERNEL-MEMO.md`). That is
6 rows outside this arc's reach on purpose, giving an interior ceiling of **30**, and a
realistic one of **26–28** once `lem:simple-bridge` (a four-clause bridge into vdW's paper)
and the ℍ row are priced honestly.

Rung assignments above are targets, not promises. When a rung banks a remainder instead of
closing a row, the row's Status cell here is updated in the same commit as `THEOREM-MAP.md`.
